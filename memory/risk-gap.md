---
tags: [risk, countme, tax-engine]
related: "[[STATUS]] · [[decisions]] · [[progress]]"
---

# risk-gap — מפת השבריריות של מנוע המס (false positives)

> נוצר: 2026-07-05 · עודכן: 2026-09-06 (סבב-אימות שני + סבב-ביצוע, Workflow 7-סוכנים + תיקוני-קוד
> ישירים — ראו §7 למצב-האימות ו-§8 למה שתוקן בפועל; §1-§6 הם היסטוריה מ-15/07, לא נמחקו, אבל
> **§7/§8 גוברים** בכל מקום שהם סותרים) · מטרה:
> למפות איפה המוצר עלול להציג **מספר שגוי בביטחון** או מספר שנשען על הנחה לא-מאומתת — החלקים שדורשים
> עין אנושית (רועי/משפטי/CTO חיצוני) או תיקון קוד לפני משתמשים אמיתיים.
> **איך נוצר:** סריקה מרובת-סוכנים על 7 תחומי-קוד (05/07); אימות ידני-חלקי (14/07, מגבלת תקציב חסמה
> סוכני-משנה); סבב אימות מלא נוסף (15/07, 15 סוכנים אדוורסריים במקביל, אפס כשלים) שכיסה כל מה שנותר
> ב-§3 הישן. **סטטוס אימות: 35 ממצאים אומתו · 0 ממתינים · 6 הופרכו** (מהם 2 חדשים ב-15/07 — אחד מהם
> חשף ממצא מדויק-יותר תחתיו, ראה #35). **עדכון 06/09/2026: סבב-אימות שני קרא את הקוד של היום מול
> כל אחד מ-35 הממצאים + סרק מה חדש מאז — ראה §7. תמצית: 7 תוקנו במלואם, 8 תוקנו חלקית, 20 עדיין
> פתוחים בלי שינוי, ו-6 ממצאים חדשים התגלו (2 מהם HIGH וחמורים לא-פחות מהמקוריים).**
> כלל שימוש קבוע: ממצא "לא אומת" הוא **חשד בלבד** — לאמת מול הקוד לפני שפועלים עליו. גם ממצא שכבר
> "אומת" יכול היה להיות מדויק-חלקית (ראה §4 — 2 מהאימותים החדשים תיקנו את הניסוח המקורי, לא רק אישרו/הפריכו).

## 1. סיכום מנהלים — המאומתים בחומרה גבוהה

| # | הסיכון | קובץ | בעלים |
|---|---|---|---|
| 1 | **הפרומפט של איתן מפר את "LLM לא מחשב"** — מורה למודל לחשב זיכוי תרומות ("חשב: תרמת X ₪ → זיכוי של X×0.35" — בלי רצפת 207 ₪ ותקרת 30%) ומשרד-ביתי ("הוסף לחישוב"), ומכיל תקרת זעיר קשיחה "עד 120,000 ₪" לא-year-keyed | `api/coach/route.ts` | קוד בלבד — **דחוף** |
| 2 | שאלת ב"ל בוויזרד אוספת סכום משולב (ב"ל+בריאות) וה-52% מוחלים על כולו — ניכוי מנופח באלפי ₪ | `setup/page.tsx:1306` + `calculators/index.ts:289` | רועי + קוד |
| 3 | ב"ל שלא הוזן (או הוזן 0!) מוחלף בשקט באומדן מומצא 12%×רווח, מוצג כ"שובר שנתי" ב-high | `setup/page.tsx:586` | קוד בלבד |
| 4 | ‏upload: המחזור מחולץ **כולל מע"מ בהגדרה** (כך כתוב בפרומפט החילוץ) ונשפך ל-238/294 "ללא מע"מ" בלי המרה; דו"ח חלקי-שנה הופך שנתי | `api/upload/route.ts` + `setup/page.tsx:435-469` | קוד בלבד |
| 5 | "סך הוצאות מוכרות" — קלט גולמי מקבל תווית "קוטלג ע"י expense-categorizer, ניכויים חלקיים יושמו" — פרובננס שקרי | `setup/page.tsx:1242` + `index.ts:240` | קוד בלבד |
| 6 | הוויזרד לא שואל מקדמות/ניכוי-במקור/שכר/ריבית/שכ"ד — והשדות (042/115/032) מציגים 0 ב-high | `setup/page.tsx:568` | רועי + קוד |
| 7 | תאריך עלייה נאסף (שדה חובה!) אך מוגנר — כל עולה מקבל 3.0 נק' של שנה-1 לנצח; תיקון 262 לא ממומש | `calculators/index.ts:137` | רועי + קוד |

## 2. כל הממצאים המאומתים (לפי תחום)

### intake — הוויזרד (המקבץ החמור ביותר)

1. **[HIGH] פיצול ב"ל/בריאות** — השאלה "ביטוח לאומי ששילמת (שדה 030)" נענית בפועל מהשובר המשולב;
   ‏52% מוחלים על הכל (`index.ts:289`, משוכפל גם ב-`computePersonalDeductions` ב-`index.ts:823` — זולג
   לאומדן ולרווח/הפסד). ‏FLAG קיים ב-`persona.ts:141-149` אבל ה-intake לא תוקן. *תרחיש:* ‏20,000 ₪ שובר
   משולב → ניכוי 10,400 במקום ~7,500. *תיקון:* פיצול השאלה או פיצול בקוד לפי ‏blRate1/blRate2 (קיימים
   DORMANT ב-types.ts) — טעון אישור רועי; עד אז confidence=medium.
2. **[HIGH] אומדן ב"ל מומצא** — `annualPaid: bituach || Math.round(netIncome*0.12)` ‏(`setup/page.tsx:586`).
   דילוג **וגם הזנת 0 מפורשת** (falsy!) מייצרים סכום מפוברק שמוצג עם מקור "שובר שנתי" ב-high. ה-0.12
   ‏hardcoded בקומפוננטה (הפרת single-source). *תיקון:* שדה ריק → 0 + "לא הוזן" + confidence=low; אומדן
   רק כאומדן חסום-העתקה.
3. **[HIGH] הוצאות גולמיות עם תווית קטלוג** — המשתמש מזין סך הוצאות; שדה 150 מצהיר "אחרי קטלוג…
   תקרות וניכויים חלקיים יושמו" (`index.ts:240`) למרות שאף כלל מ-`regulatory/deductions.ts` לא הופעל.
   *תיקון:* מקור אמיתי לפי מסלול ההזנה + שאלת-המשך על הוצאות מעורבות + קישור ל-/business-expenses.
4. **[HIGH] שדות שלא נשאלים מוצגים כ-0 ודאי** — ‏buildPersona לא בונה mikdamot / taxWithheldAtSource /
   financialInstitutionsIncome; אין שאלת שכר (106) / שכ"ד / רווחי הון. ‏042/115/032 מחזירים 0 ב-high,
   ו"יתרה לתשלום" מתעלמת ממקדמות וניכוי-במקור. *תרחיש:* יתרה מוצגת 25,000 ₪ במקום ~5,000. *תיקון:*
   שאלות intake חדשות + "לא נשאל" ≠ 0-ודאי (confidence=low + note).
5. **[MEDIUM] off-by-one בימי מילואים** — נשמר תחת `reserveDaysByYear[selectedYear]` אבל הזיכוי קורא
   ‏`miluimServiceYear(year)=year-1` ⇒ בדוח 2026 ההזנה של המשתמש לא נמצאת והזיכוי 0 "בביטחון" עם הודעה
   שלא הוזן (`setup/page.tsx:514-517` מול `index.ts:38-43`). *תיקון:* לשאול לפי שנת-השירות הנכונה או
   לשמור תחת המפתח הנכון + golden test.
6. **[RESOLVED 19/07/2026]** ~~משתמש חוזר — שנת המס לא משוחזרת~~ — תוקן ב-commit `a4ebe32`
   (`setup/page.tsx:679-683` מצטט את #6 הזה בפירוש): שחזור מפורש של `saved.income.year`.
   שים לב: זה הפך את השנה ל"דביקה" — פרסונה שנוצרה בזמן שברירת המחדל הייתה קבועה (ראו #7 החדש)
   נשארת נעולה לשנה ההיא לצמיתות, גם אחרי שברירת המחדל עצמה תוקנה.
7. **[HIGH, 18/08/2026] פרסונות ישנות תקועות על שנת-מס לא-נוכחית, בלי שום סימון על המסך** —
   ‏`/setup` נעל ברירת מחדל ל-2025 בקוד קשיח מ-10/06 עד 17/08/2026 (commit `e82c776`); כל חשבון
   שנוצר בחלון הזה נעול ל-`income.year=2025` לצמיתות (ראו #6 למעלה — השחזור הופך את זה לדביק).
   שום מסמך חדש לא מתקן את זה: יצירת חשבונית מוסיפה ל-`totalRevenue` בלי לבדוק בכלל את
   `income.year` מול תאריך המסמך או התאריך האמיתי (`invoices/new/page.tsx:367-368`) — כך הכנסה
   אמיתית מאוגוסט 2026 מצטברת נגד תקרת 120,000 של 2025 במקום 122,833 של 2026.
   בדיקת שדה (18/08): משתמש עם מחזור 119,500 וחשבון תקוע על 2025 קיבל "נותרו 500 ₪ בלבד לתקרה" —
   נכון מתמטית ל-2025, אבל בלי שום תיוג שנה על המסך הוא קרא את זה כאילו מדובר ב-2026 (שם היו נשארים
   3,333 ₪). *תוקן חלקית 18/08:* `computeCeilingAlert` כותב מפורשות "שנת מס X" בכל headline/detail
   (`lib/alerts/ceiling.ts`), והדשבורד הקל + פתיח הצ'אט מציגים את `income.year` ליד השם. *עדיין פתוח:*
   מתג שנה נגיש למשתמש (משימה #16 בסשן ה-Claude) + החלטת מוצר האם `income.year` אמור בכלל להתקדם
   אוטומטית עם פעילות חדשה, או שזו שנת-הגשה נפרדת מ"שנת המעקב השוטפת" (ראו הסתירה בין
   ‏`decisions.md`'s ACTIVE_FILING_YEAR=2025 לברירת-המחדל הנוכחית של שנה קלנדרית).

### calculators — לוגיקת המחשבונים

7. **[HIGH] שדה 044 (עולה חדש)** — ‏aliyahDate נאסף כחובה ומוגנר; תמיד 3.0 נק' (‏newOlehCreditYear2/3 =
   ‏dead code). ‏FLAG פתוח: תיקון 262 (מסלול חודשי לעולים מ-2022) לא ממומש, אימות-רשת נעצר על 81–91%.
   משפיע על אומדן המס (8,712 ₪ לשנה) — ערך הטופס עצמו checkbox ולכן לא מושפע. *תיקון:* חישוב לפי
   ‏aliyahDate + אישור רועי לתיקון 262; עד אז אזהרה מפורשת באומדן.
8. **[MEDIUM] שדה 032 (הכנסה ממוסד כספי)** — מוצג בטופס כ-calculated ב-high, אבל הוויזרד לא שואל
   (תמיד 0) וה-estimate לא ממסה §125 בכלל (`types.ts:359` — ‏surtax הוני "Not applied"). *תיקון:* שאלת
   intake + "לא נבדק" במקום 0 + טריגר blockSubmission (זה כבר טריגר מוגדר ב-WS3).
   **חיזוק (אומת 15/07):** גם כפתור ההעתקה (`govil-section.tsx:206-222`, `rawCopyValue`) בודק רק
   ‏value≠0/false/null — לא תלוי ב-confidence כלל, ותקף לכל שדה calculated (לא רק 032). כלומר הבעיה
   כללית: **שום שדה מחושב לא נחסם מהעתקה בגלל confidence נמוך**, ולא רק 032 ספציפית.
9. **[MEDIUM] fallback שקט של שנים** — ‏getTaxYearConstants מחזיר 2024 לכל שנה<2024 ו-2026 לכל
   שנה>2026 בלי שום דגל (`types.ts:484-488`), וכל המחשבונים high קשיח (~24 מופעים). *תיקון:* ‏isFallback
   flag / חסימת שנה לא-רשומה ב-intake + golden test.
10. **[MEDIUM] תקרת בסיס זיכוי פנסיה 45א לא מיושמת** — ‏pensionCreditCap (12,804) מוגדר ולא נצרך;
    ה-golden test מקבע את ההתנהגות הלא-מוגבלת (240K→בסיס 13,200>תקרה). משפיע על האומדן (עד אלפי ₪);
    לא על ערכי הטופס. *תיקון:* הוספה ל-min + עדכון הטסט באותו commit + אישור רועי לסמנטיקה.
11. **[LOW] ביטוח חיים 45א בלי תקרת פרמיה** — ‏25%×פרמיה בלי גבול (`index.ts:599`), ‏FLAG פתוח, אין אפילו
    קבוע תקרה (⇒ גם regulatory-watch עיוור לזה). ממותן ע"י confidence=medium+note, אבל המספר מועתק.
    *תיקון:* אישור רועי + קבוע year-keyed + טסט; עד אז חסימת ערך מעל סף פרמיה.
12. **[LOW] שדה 112 (אובדן כושר)** — "100% ניכוי" בלי תקרה חוקית וב-high בלי note; במקביל האומדן
    מתעלם מהניכוי לגמרי — שני המשטחים סותרים. *תיקון:* ‏FLAG(Roy) חדש + יישור הסתירה + golden test.

### constants / regulatory-watch

13. **[MEDIUM] ‏regulatory-watch צופה בשנה הלא-נכונה ובתת-קבוצה** — ‏listTaxConstants קורא רק מ-
    ‏TAX_YEAR_2024 (‏`types.ts:698`), ‏TAX_CONSTANT_META מכסה 13 מתוך ~34 סקלרים (חסרים: vatRate,
    donationsCreditMinimum, pensionCreditCap, lifeInsuranceCreditRate, kerenExemptDepositCap,
    blMonthlyMax, כל ה-brackets), וכמה רשומות effectiveTaxYears סותרות את הקוד. ⇒ עדכון רגולטורי עתידי
    על קבוע לא-מכוסה לא ייתפס לעולם. *תיקון (קוד בלבד):* קריאה per-year מ-REGISTRY + טסט exhaustiveness
    שנכשל על סקלר בלי META.

### chat/LLM — אומת ידנית 14/07 (הפרה בפועל של "LLM לא מחשב")

14. **[HIGH] הפרומפט של איתן מורה למודל לחשב** — "תרומות סעיף 46: … חשב: תרמת X ₪ → זיכוי של X×0.35 ₪"
    (וגם בסיכום השיחה: "אם יש תרומות, חשב 35%") — בלי רצפת 207 ₪ ותקרת 30% מהכנסה שהמנוע אוכף; משתמשת
    שתרמה 150 ₪ תקבל "זיכוי 52.5 ₪" כשה-field046 האמיתי מחזיר 0. וגם "כלל 30% משרד ביתי: … הוסף לחישוב".
    *תיקון:* להחליף כל הוראת-חישוב בהפניה לכלי/מנוע; הפרומפט מסביר, המנוע מחשב.
15. **[HIGH] עותק שני של קבועי-מס בפרומפט** — "מסלול עוסק זעיר… מחזור עד 120,000 ₪" קשיח, לא-year-keyed
    וסותר את התקרה העדכנית ב-types.ts (הפרת single-source בתוך prompt). *תיקון:* להזריק קבועים מ-
    ‏getTaxYearConstants לפרומפט בזמן-ריצה, או להסיר מספרים מהפרומפט לגמרי.
16. **[MEDIUM] מסלול coach בלי persona = אפס עיגון** — ‏`useTools = !!persona` וה-snapshot מוזרק רק עם
    persona; משתמש לפני setup מקבל מודל בלי כלים, בלי נתונים, עם פרומפט שמעודד חישוב. *תיקון:* בהיעדר
    persona — הוראה מפורשת "אל תציג מספרים, הפנה להשלמת setup" (+לשקול חסימת המסלול).

### upload/OCR — אומת ידנית 14/07 (שלב 0 של הוויזרד)

17. **[HIGH] מחזור כולל-מע"מ נשפך לשדות "ללא מע"מ"** — פרומפט החילוץ מבקש במפורש "totalRevenue — סך
    הכנסות **כולל מע"מ**", ו-`applyExtracted` שופך אותו ל-s4.totalRevenue (שמזין את 238/294 המוגדרים
    ללא מע"מ) בלי שום המרה — ניפוח 18% לעוסק מורשה. *תיקון:* המרה לפי osekType+vatRate או שינוי הפרומפט
    לחלץ ללא-מע"מ + ולידציה.
18. **[HIGH] דו"ח חלקי-שנה הופך שנתי** — ‏dateRangeStart/End מחולצים וממופים ב-whitelist, אבל
    ‏applyExtracted מתעלם מהם לחלוטין — דו"ח תקופתי של חודשיים נרשם כמחזור שנתי. *תיקון:* בדיקת טווח;
    אם חלקי — לחלץ בהתאמה או לחסום עם הודעה.
19. **[MEDIUM] אקסל: שלוש דליפות שקטות** — ‏fallback לעמודת "כולל מע"מ" כשאין עמודת ללא-מע"מ
    (`useCol = amountNoVatCol !== -1 ? … : amountCol`) · ‏parseFloat קוטע פסיקי-אלפים ("12,500.00"→**12 ₪**;
    הטענה המקורית "1 ₪" לא מדויקת אבל הבאג אמיתי) · שורות זיכוי שליליות ותאי-נוסחה מדולגים בשקט
    (`amount <= 0 continue`; ערך-נוסחה הוא אובייקט→NaN). *תיקון:* נרמול מחרוזות-מספר, טיפול בזיכויים,
    קריאת `cell.result` לנוסחאות, ודיווח כמה שורות דולגו.
20. **[MEDIUM] טופס 106 נזרק + דריסת קלט ידני** — ‏salaryGross/employerName מחולצים ומוצגים "חולץ
    בהצלחה" אבל ל-`applyExtracted` אין ענף form-106 בכלל — השכר לא מגיע לפרסונה (ומדרגות המס מחושבות
    בלעדיו); במקביל osekType ו-totalRevenue מחולצים **דורסים** ערכים שהמשתמש כבר הזין (בניגוד לשם,
    שנשמר רק אם ריק), ואין שום גבול-שפיות על גדלים. *תיקון:* מיפוי 106 לפרסונה (או הסרת ה-slot),
    מיזוג לא-דורס + אישור משתמש, גבולות סבירות.

### תצוגה + טסטים — אומת ידנית 14/07

21. **[HIGH] ‏confidence נראה רק בתוך ה-tooltip** — צ'יפ "מבוסס על נתונים חלקיים" מרונדר רק בתוך פורטל
    ה-tooltip (נפתח בלחיצה); הערך המכווץ וכפתור ההעתקה זהים לחלוטין בכל רמת confidence — משתמש מעתיק
    ‏medium/low בלי לדעת. *תיקון:* אינדיקציה על הערך המכווץ עצמו (צבע/אייקון) — משתלב ב-WS3.
22. **[מאושר-כצפוי] ‏blockSubmission לא קיים בקוד** — אפס מופעים ב-src/tests. זה ה-DoD של WS3; מתועד
    כאן כדי שסשן WS3 יֵדע שהוא מתחיל מאפס.
23. **[HIGH→רועי] ‏golden test מקבע כפל-הטבה פנסיוני** — אותם 30,000 ₪ הפקדה מקבלים גם ניכוי §47 מלא
    (25,608) וגם זיכוי §45א (4,620) באותו אומדן (`deductions-credits.test.ts:160-183` — אומת שכך הקוד
    מתנהג והטסט נועל). האם הדין מתיר כפל על אותם שקלים — שאלת רועי; אם לא, נדרש פיצול ההפקדה בין
    המסלולים + עדכון golden test.

### calculators/schema — סבב 15/07 (Sonnet 5, 15 סוכנים אדוורסריים, אפס כשלים)

24. **[MEDIUM] אומדן המס לא יורש דגלי-הנחה מהקאלקולטורים הבודדים** — `estimateTaxLiability`
    (`index.ts:874`) לא קורא ל-`field044OlehHadash`/`field068Soldier` (שכן מורידים confidence+notes
    כשחסר תאריך) — הוא עוקף אותם וקורא לפונקציות-עזר מקבילות (`newOlehCreditPoints` שורות 137-143,
    `soldierEligibleMonthsInYear` שורה 101) שאין להן שום מנגנון דיווח. `TaxEstimate`
    (`types.ts:44-78`) נבדק שדה-שדה — **אין בו בכלל שדה confidence/flags/assumptions**. *תיקון:*
    להוסיף `assumptions: string[]` ל-`TaxEstimate` ולאכלס מהקאלקולטורים הבודדים; להציג ליד "יתרה לתשלום".
25. **[MEDIUM] hints בסכימה לא-מסונכרנים עם קבועים — דפוס שיטתי, לא מקרה בודד** — שלושה hints קשיחים
    ב-`schema.ts` נמצאו שגויים ביחס למחשבון בפועל: שדה 048 (שורה 709, "48%") מול `index.ts:520-542`
    שמצהיר "אין זיכוי 48%" ומחזיר 0 (תוקן ביוני 2026 בקוד אך לא ב-hint) · שדה 072 (שורה 693, "5%")
    מול `lifeInsuranceCreditRate=0.25` שתוקן 2026-07 · שדות 045/046 (שורות 685,701, "מינימום 200 ₪")
    מול `donationsCreditMinimum=207` (200 היה ערך-תקוע של 2023). השורש: `hint?: string` הוא מחרוזת
    חופשית ללא קשר מבני לקבועים ב-types.ts — כל תיקון-קבוע דורש זיכרון ידני לעדכן גם את ה-hint.
    *תיקון:* לגזור hints דינמית מ-`getTaxYearConstants` בזמן רינדור, או golden test שמשווה טקסט-hint
    מול הקבוע בפועל לכל שדה עם `%`/₪ בתוכן.
26. **[LOW] פוטנציאל לשני ייצוגים של אותו מספר בצ'אט** — `ils()` המקומי ב-`agent/tools.ts:30` מעגל
    (`Math.round`) לפני הזרקה ל-system prompt (`buildRichContext`), אבל `get_form_value` (הכלי שהמודל
    קורא) מחזיר `res.value` גולמי-לא-מעוגל מהקאלקולטור (חלקם, כמו שדה 150/238, לא מעגלים כלל —
    `index.ts:227,257`). היום לא מתבטא (persona הדמו שלמה) — latent bug שיתעורר עם נתונים לא-שלמים
    (אגורות) בפרודקשן. *תיקון:* עיגול עקבי בכל הקאלקולטורים או ב-tools.ts.
27. **[MEDIUM] טקסט מוזרם לפני tool_result לא ניתן-לביטול; כשל-כלי לא חוסם** — `chat/route.ts:171-179`
    שולח כל `text_delta` ל-client מיידית, גם אם הבלוק הזה מקדים tool_use (אין `tool_choice` שכופה
    כלי-קודם). `runEitanTool` (`agent/tools.ts:141-192`) **לעולם לא זורק** — מחזיר תמיד JSON (גם על
    שגיאה), כך שהזרם ממשיך תמיד ללא חסימה, וההקשר (כולל טקסט שכבר הוצג) נשמר להמשך השיחה בלי מנגנון
    תיקון. *תיקון:* buffer לטקסט-לפני-כלי עד לתוצאת הכלי, או תיוג ברור ב-UI שטקסט "בטעינה".
28. **[MEDIUM] אימות persona רדוד → "NaN ₪" מוזרק לצ'אט בביטחון** — `coach/route.ts:188-194` בודק רק
    ‏`typeof persona === "object"`, בלי zod/schema. פרסונה פגומה (למשל `totalRevenue: undefined`)
    זולגת דרך `computeBusinessIncome`/`computeTaxableIncome` (`calculators/index.ts:789-845`, JS
    מחזיר `NaN` בלי לזרוק) ומוצגת ב-snapshot כ-"NaN ₪" (`utils.ts:31-33`). לא פוגע בערכי הטופס עצמם —
    רק בקונטקסט לצ'אט, אבל מפר "LLM לא מחשב" ברמת המצגת ועלול להיראות רע בדמו. *תיקון:* ולידציית
    שדות מספריים לפני `buildRichContext`.
29. **[MEDIUM] פרומפט dashboard-insights "מזמין" השוואות שאין לו נתונים לחשב** — `useTools = !!persona
    && mode !== "dashboard-insights"` (`coach/route.ts:332`) חוסם כלים במצב הזה, אבל
    `SYSTEM_DASHBOARD_INSIGHTS` (שורות 91-97) מכיל דוגמאות מפורשות ("עלו ב-15% לעומת הרבעון הקודם")
    שדורשות נתון תקופתי — ו-`buildRichContext` מזריק רק תמונת-מצב נקודתית (לא `monthlyBreakdown`,
    שקיים בפרסונה אך לא מגיע לכלי). המודל אף מקבל הנחיה "יש לך כלים... השתמש בהם" (`tools.ts:96`) גם
    כש-useTools=false. סיכון קונקרטי ל-hallucination. *תיקון:* להסיר דוגמאות תקופתיות מהפרומפט או
    להזריק monthlyBreakdown בפועל.

### intake/data — סבב 15/07

30. **[MEDIUM] תקציר נק' הזיכוי בוויזרד סוטה מהמנוע הקנוני** — `setup/page.tsx:637-643` מחשב נוסחה
    hardcoded מקומית (לא קורא ל-`totalCreditPoints`), עם סטייה קונקרטית: ילד בגיל 1-5 מוצג 0.5 נק'
    בוויזרד מול 2.5 נק' במנוע (`types.ts:132-137`) — פער ~5,808 ₪ בתצוגה. לא משפיע על ערכי הטופס
    (אלה קוראים למנוע מחדש), רק על אמינות התצוגה החיה בזמן מילוי. *תיקון:* לקרוא ל-`totalCreditPoints`
    ישירות בפריוויו.
31. **[MEDIUM] שמירת פרסונה fire-and-forget + DB-wins בלי בדיקת טריות** — `persona-store.ts:18`:
    `void upsertPersona(persona)` — לא ממתין, לא בודק הצלחה. `syncPersonaFromDb` (שורות 32-63) דורס
    בשקט cache מקומי בערך מה-DB בלי שום `updatedAt`/timestamp בכל שכבת הפרסונה (מאומת: grep ריק).
    תרחיש: עריכה ב-`/setup` → ניווט ל-`/setup/assets` לפני שה-upsert הגיע ל-DB → `syncPersonaFromDb`
    דורס את העריכה הטרייה. *תיקון:* להוסיף `updatedAt` + last-write-wins לפי timestamp, או await+retry.

### טסטים — סבב 15/07 (כולל תיקון לממצא שהופרך, ראה #35)

32. **[MEDIUM] כיסוי golden-test ל-2024 כמעט אפסי, כולל קבוע-בייצור** — אין אף טסט שקורא
    ‏`getTaxYearConstants(2024)` ישירות ב-`constants.test.ts`/`deductions-credits.test.ts` (כולם
    מתויגים "(2025)"). **תיקון לניסוח המקורי:** `DEFAULT_VIEW_YEAR=2024` לא קיים בקוד בפועל (concept
    ישן מ-`decisions.md` שבוטל ב-10/06 לטובת 2025 — `setup/page.tsx:207` מאשר `useState(2025)`); 2024
    היום הוא בעיקר יעד ה-fallback (`getTaxYearConstants`, שנה<2024). **פער אמיתי שנחשף:** `vatRate`
    שונה 2024 (0.17) מול 2025 (0.18) ונצרך בפועל ב-`invoice-generator/index.ts:35` — אפס טסטים על
    ‏invoice-generator בכלל. *תיקון:* טסט ל-`getTaxYearConstants(2024)` + טסט ל-invoice-generator.
33. **[MEDIUM] ‏pensionDeductionRate (11%) לא מעוגן כ-binding constraint באף טסט** — הנוסחה
    (`index.ts:827-831`) היא `min(הפקדה, 11%×הכנסה, תקרה 25,608)`. בכל הטסטים הקיימים
    (`deductions-credits.test.ts:70-77,150-182`) התקרה היא המגבילה (26,400>25,608), לא ה-11% — כלומר
    שינוי שקט של הקצב (ל-0.10 או 0.12) לא ישבור אף טסט קיים. *תיקון:* טסט עם הכנסה עסקית מתחת ל-
    ‏232,800 ₪ (25,608/0.11) והפקדה גבוהה, כך שה-11% הוא ה-min בפועל.
34. **[MEDIUM] ‏19 מחשבוני הצהרת-הון (טופס 1219) — אפס unit tests** — `capital.ts:126-146` מגדיר 19
    מחשבונים (סה"כ-נכסים/התחייבויות/הון-נקי + 9 asset + 6 liability), פעילים בפועל
    (`index.ts:20,770`, מזינים `app/file/1219/`), אך `grep -ri capital tests/` מחזיר אפס. מנוע חישוב
    שלם מחוץ לחוזה הבדיקות (`package.json` `test: vitest run` = כל `tests/unit/`). *תיקון:* חבילת
    golden tests ל-capital.ts, באותה רמה כמו calculators/index.ts.
35. **[MEDIUM, ניסוח מתוקן] סף 6111 ל-2026 (NEEDS-ROY) מוצג ב-confidence "high" קבוע — ומבחן-הגבול
    הקיים מכסה רק 2025** — `types.ts:404`: `form6111Threshold: 256410 // carried from 2025... NEEDS-ROY`.
    `field297Form6111` (`index.ts:499-518`) מחזיר פסק-דין חד-משמעי עם `confidence: "high"` קבוע,
    ‏ללא תלות בסימון NEEDS-ROY של הקבוע שממנו הוא נגזר. **יש** golden test לגבול
    (`deductions-credits.test.ts:143-148`, 256,410/256,411 ₪) — אבל `makePersona` ברירת-המחדל
    ‏(`persona-factory.ts:83`) היא `year: 2025`, והטסט בכותרתו "(2025)" — **אין טסט-גבול ל-year:2026**
    סביב הסף המסומן כלא-מאומת. *תיקון:* טסט-גבול ייעודי ל-2026 + הורדת confidence ל-medium כש-
    ‏effectiveTaxYears של form6111Threshold מסומן NEEDS-ROY.

**הערה (לא ממצא-סיכון, מכוונת ומתועדת):** ה-fallback ל-12 חודשי-זכאות-מלאה לחייל משוחרר בלי תאריך
שחרור (`index.ts:101`) **כן** מקודש ע"י golden test ייעודי (`credit-points.test.ts:82-85`, שם הטסט
כולל "documented fallback" — הורץ ואומת ‏PASS) ומתועד ב-JSDoc כהחלטה מכוונת. שינוי עתידי בהתנהגות
יישבר-CI במכוון, לא ישתנה בשקט — לכן זה לא סיכון פתוח, רק תיעוד להקשר.

## 4. ממצאים שהופרכו — ולמה זה חשוב

| טענה | סיבת ההפרכה |
|---|---|
| נק' זיכוי ילדים לפי טבלה טרום-2024 (×2 ניסוחים) | **הציטוט המרכזי מפוברק** — ההערה 'PRE-2024 base schedule' לא קיימת בקוד |
| ‏schema מציג 256,410 מול types.ts "מתוקן" 254,237 | הפרמיסה שגויה — בענף הנבדק types.ts עדיין 256,410; התיקון חי רק ב-PR ‏#28 (שמעדכן גם את ה-hint) |
| שדה 068 — ברירת-מחדל שקטה לחייל בלי תאריך | נכון ברמת הספרייה אך לא נגיש מה-UI (השדה חובה בוויזרד) + ממותן ב-notes |
| ההסתייגות המשפטית היחידה = footer 10px ב-form-preview.tsx | שגוי — `demo/page.tsx:92-95` מציג `<LegalNote variant="full">` בולט הרבה יותר (קופסה, 11px) **מעל** הטופס; ה-footer הוא רק ההסתייגות היחידה *בתוך* הקומפוננטה הפנימית, לא בעמוד כולו |
| ‏"אין golden test לגבול סף 6111" | שגוי — קיים טסט-גבול מדויק (256,410/256,411), אבל רק ל-year:2025; התיקון המדויק יותר נכנס כממצא #35 |

הלקח (מאשש את מדיניות 03/07): **גם ממצא "עם ציטוטים" של מודל אינו אמת עד אימות מול הקוד** — כולל
ממצאים שכבר "אומתו" בסבב אחד ומתגלים כמדויקים-חלקית בסבב הבא (form-footer, threshold-6111).

## 5. חיבור ל-WS3 (שער-מורכבות) — טריגרים מעודכנים

הרשימה מ-03/07 (‏032≠0 · ‏isNewResident · מילואים-ב"ל) + מהסריקה: **ב"ל שלא הוזן/אומדן** (ממצא 2) ·
**שנת-מס מחוץ ל-REGISTRY** (ממצא 9) · **פרמיות ביטוח מעל סף** (ממצאים 11–12) · **קלט upload בלי
אישור משתמש** (תחום 3-upload) · **שנת-מס מסומנת NEEDS-ROY** (ממצא 35, סף 6111 ל-2026) · **אומדן-מס
עם הנחת-ברירת-מחדל שקטה** (ממצא 24, עולה/חייל ללא תאריך).

## 6. פערים של המסמך עצמו

**המסמך כעת מכסה את כל 7 התחומים שנסקרו במלואם** (05/07 סריקה ראשונית + 14/07+15/07 אימות מלא —
אפס ממצאים ממתינים). לא נסרקו כלל: billing, ‏auth/proxy, ‏e2e coverage, ‏Supabase RLS policies —
תחומים למועמדות לסריקה עתידית אם רוצים להרחיב את המפה. החומרות הן שיפוט-מודל (medium/high/low), רועי
טרם תיעדף מבחינת דחיפות-דין. הקובץ נכון לענף `claude/tax-rules-roadmap-setup-sj36jw` ב-15/07 —
לעדכן אחרי מיזוג ‏#28 (מתקן לפחות ממצא אחד בטבלת ההפרכה: hint 6111 יתעדכן ל-254,237).

## 7. סבב אימות 06/09/2026 (Sonnet 5, Workflow 7-סוכנים: 3 קבוצות-אימות + סריקה-חדשה + מבקר) — **המצב הנוכחי**

> **הקשר:** יוני ביקש הבנה עמוקה של "מה דורש אימות נוסף מכל הדאטה החשבונאי" (בעקבות מחקר על מסמך-
> מילואים). במקום לענות רק על השאלה הנקודתית, הרצתי סבב שקורא כל אחד מ-35 הממצאים למעלה **מול הקוד
> של היום** (לא ה-snapshot מיולי — כמעט חודשיים ופיצ'רים משמעותיים מאז: רפורמת מורשה-זעיר, דוח מע"מ
> תקופתי, מנגנון-מתג-שנים) + סורק מה חדש ולא-מתועד. סוכן-מבקר קרא את כל 4 הדוחות + אימת עצמאית 3
> טענות ישירות מול הקוד (כולן נמצאו מדויקות).

### 7.1 מה תוקן במלואו (7)

‏intake #2 (אומדן ב"ל מומצא 12%) · intake #5 (off-by-one שנת-מילואים) · intake #6 (שחזור-שנה למשתמש
חוזר, עדיין בסדר) · calculators #10 (תקרת-זיכוי-פנסיה 45א) · chat/LLM #14a (הפרומפט לא מחשב תרומות
בעצמו יותר) · chat/LLM #14b (הפרומפט לא מחשב משרד-ביתי בעצמו) · chat/LLM #15 (תקרת-זעיר כבר לא
hardcoded בפרומפט, מוזרקת חי מ-`getTaxYearConstants`).

### 7.2 מה תוקן חלקית (8, כל אחד עם הפער-שנשאר)

- **intake #1** (פיצול ב"ל/בריאות) — נוסף קופי-אזהרה בוויזרד ("הזינו רק את רכיב הביטוח הלאומי"),
  אבל **מבנית עדיין לא תוקן**: `field030BituachLeumi` עדיין מכפיל 52% על מה שהוזן, `blRate1/blRate2`
  עדיין DORMANT. משתמש/ת שמתעלם/ת מהקופי עדיין מקבל/ת ניכוי-מנופח.
- **intake #4** (שדות-לא-נשאלים=0 ודאי) — "יתרה לתשלום" כבר מפחיתה מקדמות (פיצ'ר 18/08), אבל שום
  UI לא כותב ל-`income.mikdamot` בפועל (רק ל-`mikdamotPlan` העתידי) — השדה נשאר 0 בפועל. ניכוי-במקור
  עדיין מתעלם לגמרי.
- **intake #7** (18/08, פרסונות תקועות על שנה) — `YearSwitch` נגיש נבנה, אבל `invoices/new` עדיין לא
  בודק תאריך-מסמך מול `income.year` לפני צבירה — הבאג הקונקרטי חי. שאלת-המוצר (`income.year` דביק
  מול YTD) עדיין נעולה-כפתוחה ב-decisions.md.
- **calculators #9** (fallback שקט לשנה) — נוסף שדה `isFallbackYear` (19/08) — אבל **מת**: שום
  מחשבון/פרומפט לא קורא אותו.
- **upload/OCR #17** (מחזור כולל-מע"מ) — כיוון-הפרומפט התהפך (מבקש עכשיו ללא-מע"מ), אבל אם המסמך-
  מקור מציג רק כולל-מע"מ, הערך השגוי עדיין נוחת בשדה עם הערת-פרוזה בלבד, לא חסימה מובנית.
- **upload/OCR #19** (3 דליפות-אקסל) — 1/3 תוקן (fallback-כולל-מע"מ מציג עכשיו אזהרה גלויה); 2
  נשארים: `parseFloat` קוטע פסיקי-אלפים, שורות-זיכוי/נוסחה מדולגות בשקט בלי מונה.
- **intake/data #31** (שמירה fire-and-forget) — מסלול-הסיום הראשי (DoneScreen) עכשיו `await` אמיתי
  עם זיהוי-קונפליקט. נשאר race באותו דפוס בדיוק בשני מקומות: `handleSubmit` (לפני DoneScreen) ו-
  `setup/assets/page.tsx`.
- **tests #32/#35** — 2024 מקבל טסטים ישירים עכשיו (תלונה כללית נסגרה), אבל הפער הספציפי
  (invoice-generator/vatRate בלי שום טסט) נשאר; סף-6111-2026 עצמו כבר לא NEEDS-ROY (אומת זהה ל-2025)
  אבל מנגנון "confidence לא משקף provenance" עדיין מבני ולא תוקן.

### 7.3 מה עדיין פתוח בלי שום שינוי (20)

intake #3 (תווית-קטלוג כוזבת על הוצאות גולמיות) · calculators #7 (044 עולה — אין תיקון 262) ·
calculators #8 (032 confident-0, אין §125, `blockSubmission` עדיין לא קיים בכלל בקוד) ·
calculators #11 (ביטוח-חיים בלי תקרה) · calculators #12 (112 בלי תקרה, סותר את האומדן) · constants
#13 (**הממצא היחיד שאפס זז מ-15/07, למרות המון עבודת-מנוע אחרת שנכנסה באותה תקופה**) · chat/LLM #16
(coach בלי persona = אפס עיגון) · upload/OCR #18 (דו"ח חלקי-שנה → שנתי) · upload/OCR #20 (טופס 106
נזרק; osekType/מחזור נדרסים בשקט בהעלאה-חוזרת) · תצוגה #21 (צ'יפ-ודאות רק בתוך tooltip) · #22
(`blockSubmission` — baseline צפוי, לא רגרסיה) · #23 (כפל-הטבה פנסיוני — שאלת-חוקיות לרועי עדיין
פתוחה) · schema #24 (`TaxEstimate` עדיין בלי שדה assumptions — **ההצעה מ-15/07 עצמה מעולם לא
מומשה**) · schema #25 (3 hints שגויים ב-schema.ts — 048/072/045-046) · schema #26 (כפל-עיגול
אפשרי) · schema #27 (טקסט-זרם לפני tool_use לא-הפיך) · schema #28 (ולידציה רדודה → "NaN ₪") ·
schema #29 (פרומפט dashboard-insights מזמין השוואות-תקופה בלי נתונים) · intake/data #30 (תצוגה-חיה
בוויזרד עם נוסחת-נק'-זיכוי נפרדת ושגויה — **ראו 7.4, זה חמור יותר משנראה**) · tests #33 (11% פנסיה
אף פעם לא ה-binding constraint בטסט) · tests #34 (19 מחשבוני טופס-1219, אפס unit tests).

### 7.4 ⚠️ 6 ממצאים חדשים — לא היו ב-35 המקוריים

1. **[HIGH] נקודות-זיכוי-ילדים חסרות בשתי שנות-המס החיות (2025+2026)** — הטבלה בקוד מתויגת
   במפורש **"PRE-2024 base schedule"**, עם `FLAG(Roy)` פתוח (75-88% ודאות, מתחת לסף-95%): תוספת
   קבועה-מ-2024 שמעלה את הערכים בפועל (למשל גיל 1-2: 4.5 נק' לא 2.5) **טרם יושמה**. תת-ספירה של עד
   2 נק' מלאות (₪5,808) לילד בחלק מטווחי-הגיל, מוצג ב-confidence **"high"**. **אפס השפעה היום רק כי
   לפרסונת-הדמו אין ילדים — זה מוקש, לא "לא-רלוונטי".** מחמיר ע"י #30 למעלה: התצוגה-החיה בוויזרד
   משתמשת בנוסחה **שלישית**, שונה גם מהטבלה-הרשמית וגם מהתיקון-הפוטנציאלי (0.5 נק' שטוח לכל ילד,
   בלי תלות בגיל בכלל) — **אלה 2 באגים נפרדים שמצטברים, לא אחד**.
2. **[HIGH] כללי-ניכוי-חלקי לפי קטגוריה (רכב 45%, טלפון 80%, משרד-ביתי יחסי) לא מיושמים בפועל על
   אף הוצאה אמיתית** — לא ב-`effectiveDeductibleExpenses` (המקור היחיד ששדה 150/P&L/אומדן-המס
   קוראים ממנו: מנטרל מע"מ בלבד, אפס-ניכוי-חלקי) · לא ב-`/expenses/new` (**ה-reference implementation
   של CLAUDE.md עצמו**, מקבע `deductionRule:'full'` לכל קטגוריה) · וגם `israeli-report.ts`'s
   התיעוד-הפנימי טוען ש-`recognizedRate` מיושם, בזמן שה-branch של "הוצאה-תפעולית" (בניגוד ל"ניכוי
   מהכנסה") **מתעלם ממנו לגמרי**. משמעות: **כל הוצאה עסקית שמוזנת דרך מסלול-האיסוף האמיתי נספרת
   כ-100% ניכוי, תמיד** — מנפח ניכויים/ממעיט מס לכל עצמאי/ת עם רכב/טלפון/משרד-ביתי, בדיוק קהל-היעד
   המוצהר של המוצר.
3. **[HIGH] דוח מע"מ תקופתי + תזכורות-הגשה קוראים תמונת-מצב קפואה** — `annualTurnoverWithoutVat`
   נכתב **פעם אחת בלבד**, ב-setup, ולעולם לא מתעדכן (בניגוד ל-`income.totalRevenue` שמתעדכן בכל
   חשבונית). עסק שחוצה 1.5M ₪ (סף חודשי-לעומת-דו-חודשי) או 500K ₪ (חובת טופס 874 מפורט) **באמצע
   השנה** ימשיך לקבל תדירות/דרישת-דיווח שגויה, בלי שום סימון-התיישנות. **סיכון-אמיתי, לא רק דיוק-
   אומדן — קנסות אמיתיים על תדירות/מועד-הגשה שגויים.**
4. **[MEDIUM] קודי-שדה בטופס 1219 (הצהרת-הון) מתויגים בקוד עצמו `TODO(Roy)` כ-placeholders** שטרם
   אומתו מול הטופס האמיתי ב-secapp.taxes.gov.il — מנוגד ישירות להבטחת-המוצר "התאמה נאמנה ל-gov.il".
5. **[MEDIUM] מע"מ-תשומות (קלט) מחושב שטוח לכל הוצאה** — בלי רשימת-קטגוריות-חסומות/מוגבלות (רכב-
   פרטי, אירוח-לקוחות) שהחוק מגביל עליהן קיזוז-תשומות; כל הוצאה נחשבת 100%-ברת-קיזוז.
6. **[LOW] תיקון-תיעוד ל-CLAUDE.md** — מתוך 3 פריטי "FLAG(Roy) burn-down" שם, **2 כבר נפתרו בפועל**
   (תקרות-פנסיה 2025/2026, רצפת-תרומות 2026 — שניהם CONFIRMED בקוד היום) — רק תקרת ביטוח-חיים §45א
   עדיין פתוחה. הרשימה שם לא עודכנה, ומחסירה את ממצא #1 החדש (ילדים) שראוי-יותר להיות עליה. **תוקן
   ב-CLAUDE.md באותו קומיט כמו העדכון הזה.**

### 7.5 ⚠️ פער-מתודולוגי שהמבקר תפס — לא לפספס

נוסחת-הימים-לנקודות של זיכוי-המילואים החדש (`MILUIM_CREDIT_TIERS_2026`) **מעולם לא הושוותה** מול
הכללים המשפטיים שאומתו באותו סבב (תיקון 283: 30 ימים=0.5, 50=1.0, ‎+0.25/5-ימים, ומ-2028 הסף יורד
ל-20 ימים). מה שכן אומת בממצא intake #5 הוא רק **תיקון-האינדקס-לפי-שנה** (קריאה נכונה של
`reserveDaysByYear[year-1]`), **לא נכונות-הנוסחה-עצמה**. לא הוכרז כשבור — רק לא-נבדק עדיין. בעדיפות
הבאה לאימות, לפני שהשדה נסמך מעבר לתחזית/דמו (רלוונטי ראשונה בפועל רק לדוח 2026, שלא מוגש עד 2027).

### 7.6 סיכון-מערכתי משולב (3 ממצאים בני-15/07 שהם בעצם סיפור אחד)

‏#11 (ביטוח-חיים בלי תקרה, confidence=medium) + #21 (צ'יפ-הודאות מוצג רק בתוך tooltip שנפתח בקליק)
+ #24 (‏`TaxEstimate` בלי שדה `confidence`/`assumptions` בכלל) יחד אומרים: **משתמש/ת יכול/ה להעתיק
ערך לא-ודאי לטופס האמיתי בלי שום אזהרה נראית בשום שכבה** — קופה, אומדן, או כפתור-ההעתקה. שלושתם
תועדו כממצאים נפרדים; ראוי תיקון-אחד-משותף (הצג ודאות על האומדן עצמו + שער-העתקה לפי confidence),
לא 3 תיקונים בבידוד.

### 7.7 סדר-עדיפות מומלץ (מהמבקר, לפי סיכון-אמיתי למספר-שמוצג-בביטחון-גבוה)

1. נק'-זיכוי-ילדים חסרות (7.4 #1) — 2 באגים נפרדים שמצטברים, אפס-נראות היום רק כי לדמו אין ילדים.
2. ניכוי-חלקי-לפי-קטגוריה לא מיושם בפועל (7.4 #2) — פוגע בדיוק בקהל-היעד המוצהר.
3. ב"ל/בריאות עדיין "structurally unfixed" (intake #1) — התיקון האמיתי (blRate1/blRate2) כבר קיים
   DORMANT, מאמץ-נמוך יחסית לסיכון (מספר שנוחת ישירות על הטופס האמיתי).
4. דוח-מע"מ קפוא (7.4 #3) — היחיד כאן עם סיכון-קנס אמיתי, לא רק דיוק.
5. נוסחת-מילואים לא-מושווית לחוק (§7.5) — לפני שהשדה נסמך מעבר לתחזית.

**הערת-אגב מתודולוגית:** המבקר תפס גם שגיאת-ציטוט חוצת-דוחות (ממצא-#2-החדש הפנה בטעות ל-"risk-gap
#5" כשהוא מתכוון ל-#3) — לא משפיע על המסקנות, אבל שווה זהירות בהפניות-מספר עתידיות. גם אימת ש-3
טענות "תוקן"/"פתוח" שנבדקו ידנית באופן עצמאי — כולן נמצאו מדויקות, לא מפוברקות.

## 8. סבב-ביצוע 06/09/2026 (המשך אותו יום) — 4 מ-5 בעדיפות תוקנו בקוד, #5 אומת (עקבי, אין באג)

יוני ביקש לתקן לפי סדר-העדיפות ב-§7.7. כל תיקון: tsc נקי + טסטים-חדשים + build ירוק לפני commit.

1. **✅ תוקן — נק'-זיכוי-ילדים (§7.4 #1 + #30):** `childCreditPointsByAge` עודכן לטבלה
   המתוקנת-מ-2024 (age-banded, מפריד אמא/אבא לפי `personal.gender`) בכל שלוש שנות-המס.
   **תויג STRONGLY-SUPPORTED, לא CONFIRMED** (לפי המתודולוגיה מה-06/09) — עדיין ממתין לאישור-רועי
   סופי, אבל מדויק-יותר משמעותית מהטבלה הישנה-ודאית-לא-שלמה. תצוגת-הוויזרד (#30) חוברה ישירות
   ל-`totalCreditPoints(buildPersona())` במקום נוסחה שלישית נפרדת. 3 טסטים חדשים/מעודכנים.
2. **✅ תוקן — ניכוי-חלקי-לפי-קטגוריה (§7.4 #2):** `effectiveDeductibleExpenses` מיישם עכשיו
   `deductionRule`/`partialPercent` בפועל (היה מתעלם לגמרי); ה-branch של "הוצאה-תפעולית" ב-P&L
   מיישם `recognizedRate` (היה סותר את התיעוד-הפנימי של אותו קובץ); נוסף רישום "רכב" (45%,
   convention-קבועה) למאגר; `/expenses/new` (ה-reference-implementation של CLAUDE.md) כבר לא
   מקבע `deductionRule:"full"` — פותר דרך `classifyExpensePLImpact`. **משרד-ביתי נשאר במכוון
   לא-ממודל** — דורש שדה-יחס-שטח שלא קיים בפרסונה; להמציא אחוז-קבוע היה חוזר בדיוק על מחלקת-הבאג
   הזו. 8 טסטים חדשים.
3. **✅ תוקן — ב"ל/בריאות (§7.7 #3, היה "structurally unfixed"):** נוסף שדה `healthTaxAnnualPaid`
   אופציונלי (תוספתי, בלי migration) + `bituachLeumiDeductibleBase()` כמקור-יחיד ש-3 הקריאות
   הישנות ל-`annualPaid` הועברו אליו. שדה-קלט שני אופציונלי בוויזרד. ללא `healthTaxAnnualPaid` —
   התנהגות זהה-לישן (ללא רגרסיה). 5 טסטים חדשים.
4. **✅ תוקן — דוח-מע"מ קפוא (§7.4 #3):** `determineVatCadence`, סף-874-המפורט, ותזכורת-ההגשה
   קוראים עכשיו `income.totalRevenue` החי במקום `vatAndTurnover.annualTurnoverWithoutVat` הקפוא
   (נכתב פעם אחת ב-setup, לעולם לא מתעדכן). השדה הישן נשאר בטיפוס (לא הוסר — סיכון-migration),
   פשוט לא נקרא יותר. 6 טסטים חדשים כולל regression מפורש (סנאפשוט-קפוא-גבוה מול-חי-נמוך ולהפך).
5. **✅ אומת — נוסחת-מילואים (§7.5):** השוואה ישירה בין `miluimCreditPoints()` (types.ts:651-660)
   לבין הכללים המשפטיים שאומתו בסבב-3010 (30 ימים=0.5, 50=1.0, ‎+0.25/5-ימים, תקרה 4.0 ב-110 ימים,
   מדרגת-2028 בכוונה לא-ממומשת כי היא לא-רלוונטית-עדיין) — **תואם במדויק, אין פער.** מדרגת-הביניים
   40-49→0.75 לא נסתרה ולא אוששה ישירות בסבב-3010 (התקציר שם היה גס-מדי), אבל כן אומתה בנפרד
   מוקדם-יותר (מקור: gov.il pa181225-1 + kolzchut, 03/07). **אין שינוי-קוד נדרש.**

**שער מלא לכל 4 התיקונים:** tsc נקי, 417/417 unit tests (מ-402 בתחילת הסבב), build ירוק. 5 קומיטים
נפרדים על `claude/cpa-bng-count-collab-h25ga3`. **טרם דחוף/PR** — ממתין להוראת יוני.

**נשאר פתוח מכוונה (לא בעדיפות הזו):** כל שאר 20 הממצאים ב-§7.3 (constants-coverage-gap, field-032
confident-0, TaxEstimate בלי assumptions, hint-strings שגויים, ועוד) + הפערים החדשים #4/#5/#6
(טופס-1219 placeholders, מע"מ-תשומות שטוח, CLAUDE.md burn-down) — לא נגעתי בהם, לא היו בסדר-
העדיפות שנקבע.

## 9. ✅ נתפס ותוקן — `/loop בדיקה חוזרת מול טבלאות` (06/09, איטרציה 2)

**התיקון של §8 #2 (רכב 45%) עצמו יצר סתירת-טבלאות אמיתית, לא ידועה קודם:** הרישום החדש
ל-"רכב" ב-`getDeductionsTable()` הוא **שיעור-שטוח אחיד של 45%** לכל המשתמשים — אבל מאגר
113-המקצועות הקיים כבר (`business-expenses/data/regulatory-dataset-2026.json`, מוצג באמצעות
`business-expenses/profiles.ts` בעמוד הייעוצי `/business-expenses`) מכיל שיעורי-רכב **שונים
מהותית לפי מקצוע**, מאומתים בקוד (`pctVehicle`): **25%** (שליח), **45%** (עו"ד/רו"ח — התאמה
מקרית לשיעור החדש), **77.5%** (מורה נהיגה), **80%** (מדריך טיולים), **90%** (נהג מונית/הסעות),
**100%** (מוביל/הובלות, חקלאי).

**המשמעות:** עמוד-הייעוץ `/business-expenses` כבר מציג למשתמש/ת (נניח) נהג-מונית את השיעור הנכון
(90%), אבל אם אותו נהג-מונית מקליד/ה בפועל הוצאת-רכב ב-`/expenses/new`, המנוע-האמיתי (אחרי תיקון
§8 #2) יחיל 45% בלבד — **תת-ניכוי של פי-2 בדיוק במספר שמשפיע על שדה 150/P&L/אומדן-המס בפועל**,
בזמן שהעמוד הייעוצי הצדדי מבטיח למשתמש/ת מספר אחר לגמרי. עבור מוביל/חקלאי הפער חמור אף יותר
(45% מול 100% — תת-ניכוי של יותר מפי-2).

**✅ תוקן (06/09, אותו יום, לאחר "המשך" מיוני):** `classifyExpensePLImpact` מקבל עכשיו פרמטר
שלישי אופציונלי `primaryOccupation` — כשקטגוריית-רכב מזוהה, מחפש קודם ב-`matchProfessionByFreeText`
(מאגר 113-המקצועות, אותה פונקציה ש-`profiles.ts` כבר משתמש בה) את ה-`pctVehicle` הספציפי; נופל
חזרה ל-45% הגנרי רק אם המקצוע לא נמצא/אין לו רשומת-רכב, או שלא הועבר `primaryOccupation` כלל
(תאימות-לאחור). שני הקוראים (`/expenses/new`, `p-and-l/index.ts`) מעבירים עכשיו את
`persona.business.primaryOccupation` — כלומר גם ה-P&L המוצג וגם הניכוי-בפועל בשדה 150 מיושרים
לאותו מספר, ולעמוד הייעוצי `/business-expenses`. 9 טסטים חדשים (שליח=25%, מוביל=100%, מונית=90%,
עו"ד=45%, מקצוע-לא-קיים→45%, וללא-occupation-בכלל→45%). tsc נקי, 423/423 unit tests, build ירוק.

### 7.8 ⚠️ תיקון-תיוג לאחור (06/09, בעקבות מתודולוגיית רמת-ודאות החדשה — ראו [[decisions]])

כל ה-"confirmed_facts"/"אומת"/"CONFIRMED" ב-§7 (מסמך-מילואים "אישור מצה"ל", תיקון 283, וכל 5
המסמכים ב-§ documents guidance) הושגו **ללא fetch ישיר למקור-ראשי** (ה-proxy הסביבתי חוסם
gov.il/btl.gov.il/miluim.idf.il) — הם synthesis של חיפושים, לא ציטוט-ישיר. לפי המתודולוגיה החדשה
(4 דרגות, נעולה ב-[[decisions]] היום) — **כולם דרגה 2 ("תמיכה-נסיבתית-חזקה"), לא דרגה 1
("CONFIRMED")**, גם כשהניסוח למעלה משתמש במילה "אומת"/"מאושר". לא לשנות שוב עד fetch-ישיר אמיתי
או אימות מול רו"ח. ה-`/loop` שהוקם היום (ראו STATUS) נועד בדיוק לנסות לשדרג אלה לדרגה 1 לאורך זמן.
