---
tags: [decisions, countme]
related: "[[STATUS]] · [[progress]]"
---

# decisions — יומן החלטות נעולות

> החלטות שלא חוזרים עליהן בלי דיון מחדש. הטבלה המורחבת של החלטות-המוצר חיה ב-`CLAUDE.md`
> ("Project decisions" + "Design decisions"); כאן ההחלטות הפעילות + ההנמקה התמציתית.

## סשן 11/08/2026 (Fable) — עדכונים רוחביים (מנוע הוצאות/מסקוט/onboarding/expenses) + pruning

| החלטה / משימה | ערך | סטטוס + תוצר |
|---|---|---|
| מנוע ההוצאות = שכבת-ידע נפרדת, לא מחובר למחשבונים | `src/lib/expense-engine/` לא נוגע ב-`lib/calculators`/`lib/regulatory/deductions.ts`; חיווט לחישובי 1301/P&L בפועל = סבב עתידי | **נעול לסבב הזה** — golden test מוודא רק עקביות-צולבת מול `types.ts`, לא מיזוג |
| שחזור 3 שורות חסרות בקובץ-המקור (VEH-01/P001/DEP-01) | הקובץ המקורי חסר בעקביות את השורה-הראשונה בכל גיליון (באג-authoring באוגוסט); שוחזרו מקורות-צולבים **בתוך אותו קובץ** (PDF page 2, "% רכב" בגיליון professions, DEP-01 שקיים ב-rules_global אך לא בגיליון depreciation הנפרד) | **נעול** — מתועד בקוד (`RECONSTRUCTED_VEH_01`/`RECONSTRUCTED_P001` ב-generate.ts) + ב-STATUS. EB-001/EX-0001 **לא** שוחזרו — אין מקור-אימות, הספירה האמיתית 22/595 לא 23/596 |
| פלטת-צבעים בארטיפקטים של יוני (onboarding-v5, מוקאפ-הוצאות) | 3 פלטות שונות מהברנד-קיט הנעול | **לא הוחלף בשקט** — נשארנו עם Brand Kit הנעול; הארטיפקטים = רפרנס-UX בלבד. אם יוני מתכוון שפלטה חדשה תחליף את הקיים — דורש דיון מפורש (יש "ספר שפה עיצובית" שאין לנו?) |
| מסכי סיסמה+SMS OTP + "חברה בע״מ" בארטיפקט onboarding-v5 | הארטיפקט כלל auth כפול (מקביל ל-Google OAuth הקיים) ואופציית-חברה שסותרת "יחידים בלבד" | **לא הוטמעו** — Google OAuth בלבד נשאר; "חברה בע״מ" הוסר. אם auth-כפול הוא כוונה אמיתית, זו החלטת-ארכיטקטורה נפרדת (ספק SMS? איפה נשמרות סיסמאות?) שלא הוכרעה כאן |
| מסקוט חדש מחליף את "איתן" | `CHARACTER.name="שקל"` **placeholder**, `public/mascot/mascot.svg` **זמני** — שניהם ממתינים לתוכן אמיתי מיוני | **seam נעול** (`src/lib/agent/character.ts`) — שינוי עתידי = קובץ אחד. מזהי-קוד/routes/analytics לא שונו בכוונה |
| `/onboarding` מחליף את `/setup` כנקודת-כניסה | לפי `docs/specs/beta/onboarding.md` (נכתב 19/07, לא נבנה עד עכשיו) — ONB-1..ONB-11 מומשו, ONB-12..15 לא | **נעול, revisitable** — `/setup` נשאר כזרימת "השלמת פרטים לדוח" |
| Pruning: סקילים 18→8 | דמוט 10 תיקיות (expense-categorizer/receipt-scanner/hebrew-ocr-forms/ui-design-system/hebrew-tailwind-preset/ai-compliance-kit/id-validator/il-invoice-organizer/financial-reports/tax-withholding) — כולן נשארות ב-`skills-lock.json`, הפיך | **בוצע** — כל אחת אומתה קודם (יש שורה ב-lock + grep-רפרנסים) |
| Pruning: puppeteer→playwright ב-regulatory-watch | `scripts/regulatory-watch/report.ts` השתמש בפועל ב-puppeteer ל-PDF; לא נמחק בלי אימות-שימוש קודם | **בוצע** — אומת שימוש אמיתי לפני שינוי, smoke test 11/11 אחרי, CI workflow עודכן (`npx playwright install`) |
| Pruning: CLAUDE.md 372→160 שורות | לא cherry-pick עיוור מהענף התקוע (`system-beta-preparation-oiyzpy`) — עודכן במלואו לשקף את הסבב הזה | **בוצע** |
| תג `pre-prune-2026-08` | ניסיון push נכשל (403 — הרשאות-טוקן לא כוללות דחיפת-תגיות) | תג מקומי בלבד, לא נחוץ מעבר לזה — כל הקוד בפועל מחויב+נדחף כרגיל |

## סשן 03/08/2026 (Sonnet) — תיקון-באגים מה-Routine היומי; החלטות ומשימות שבוצעו

| החלטה / משימה | ערך | סטטוס + תוצר |
|---|---|---|
| Tranzila webhook signature — לא לממש בניחוש | חיפוש רשת לא אישש את סכמת-החתימה האמיתית של Tranzila בביטחון מספיק; ניחוש קריפטו על webhook תשלומים גרוע יותר מ-stub מתועד ו-fail-closed | **הוכרע סופית ע"י יוני (03/08 מאוחר): סגור/לא רלוונטי** — "אין לנו API של טרנזילה ולא יהיה בקרוב". הוסר ממעקב החוסמים; לא לשאול שוב אלא אם יוני יפתח מחדש |
| CLAUDE.md rewrite (מ-`claude/system-beta-preparation-oiyzpy`, 26/07) | שכתוב 372→142 שורות | **לא נמשך לענף הזה** — לא נבדק מספיק בזמן הסשן כדי לסמוך עליו בעיוורון; דורש עין לפני מיזוג. `regulatory-status.md` מאותו ענף כן נמשך (קובץ עצמאי, ראה שורה למטה) |
| דפוס-תיקון ל-persona בין-מכשירים/בין-משתמשים | `useRequiredPersona()` (חוסם) / `usePersona()` (לא חוסם) הם הדפוס הקנוני מעכשיו לכל דף שקורא persona — **לעולם לא `loadPersona()` סינכרוני ישיר** בדף חדש | נעול — 17/17 דפים תואמים עכשיו; `setup/page.tsx` הוא היוצא-מן-הכלל המתועד (merge logic, לא gate) |
| `/api/doc-link` — אכיפת-בעלות עסק | תוכן המסמך (סכומים/לקוח) נשאר client-trusted (משקף מה שהלקוח כבר מציג); רק `business.tradeName`/`osekType` נאכף מול ה-persona האמיתי בשרת | נעול — no-op עד `AUTH_GATING_ENABLED=true` |
| `regulatory-status.md` נמשך מהענף התקוע | קובץ עצמאי, ללא קוד, בלי קונפליקטים — 12 סעיפים רגולטוריים/משפטיים | נעול — נמשך. **10/12 סעיפים 🔴, כולל "סוקר משפטי חיצוני פעיל"** — יוני אישר (03/08 מאוחר) שעדיין אין סקירה משפטית. לא לשאול כל סשן, זה ידוע |
| **הדומיין הקנוני = `countmedemo-eight.vercel.app`** — לא `countmedemo.vercel.app` | תוקן פעמיים באותו יום: תחילה שונה ל-`countmedemo.vercel.app` לפי בקשת יוני בלי אימות (טעות שלי); יוני ביקש וידוא; `get_project` על **כל 9** פרויקטי Vercel בחשבון `yonilev2003s-projects` הראה חד-משמעית ש-`countmedemo.vercel.app` לא שייך לאף אחד מהם | **נעול, מאומת מול Vercel API בפועל (לא ניחוש)** — הוחזר ב-`layout.tsx`/`.env.template`/runbook. ראה טבלת-דומיינים למטה |
| טבלת דומיינים אמיתיים — 9 פרויקטי Vercel בחשבון `yonilev2003s-projects` (`get_project`, 03/08) | `countmedemo`→`countmedemo-eight.vercel.app` · `countme-crm`→`countme-crm.vercel.app` · `pillar`→`migdalim.vercel.app` (⚠️ שם-דומיין שונה לגמרי מהפרויקט!) · `countme`→`countme-tau.vercel.app` · `language-coach`→`language-coach-mocha.vercel.app` · `project-9340v`→אין alias נקי, דיפלוי אחרון **ERROR** · `accounting-mvp`→`accounting-mvp-seven.vercel.app` · `psycho-booking`→`psycho-booking.vercel.app` · `countme-demo`→`countme-demo.vercel.app` | עובדתי — אף אחד לא מחזיק `countmedemo.vercel.app`. `pillar`/`countme-crm`/`language-coach`/`accounting-mvp`/`psycho-booking` הם מוצרים **נפרדים ולא-קשורים** (Pillar יש לו PR/ריפו פעיל משלו) — **לא** "כפילויות" של countmedemo, לא למחוק בלי בדיקה פר-פרויקט מפורשת |
| מחיקת פרויקטי Vercel "לא רלוונטיים" | יוני ביקש למחוק "כל שאר הדברים הלא רלוונטיים" | **לא בוצע — נשאל קודם.** פעולה הרסנית/בלתי-הפיכה על תשתית משותפת; אף פרויקט אחר לא מחזיק את הדומיין המבוקש כך שמחיקה לא תשיג את המטרה, וחלקם מוצרים נפרדים אמיתיים. ממתין להיקף מדויק מיוני לפני כל פעולת מחיקה |
| `AUTH_GATING_ENABLED=true` + תקציב Anthropic | שני צעדים ידניים מהרשימה | **בוצעו ע"י יוני (03/08 מאוחר)** — אושר בפירוש, לא לשאול שוב |

## סשן 19/07/2026 (Fable) — פיבוט בטא לפי המסמך האסטרטגי של תומי

| החלטה / משימה | ערך | סטטוס + תוצר |
|---|---|---|
| מיקוד הבטא | חיי היומיום ולא הדו"ח — 5 רכיבי סעיף 3 במסמך תומי; משטחי 1301 נשארים בקוד, יוצאים מהזרקור | הוחלט (יוני+תומי); תוכנית: `docs/plans/2026-07-19-beta-sprint.md` |
| אבולוציה ולא בנייה מחדש | התשתית (auth/Supabase/מסמכים/brand/מחשבונים) נשארת; מוחלפים הפנים: שאלון, דשבורד, מסמכים | המלצת-AI שאומצה כברירת-מחדל, **revisitable ע"י יוני** |
| סריקת-עומק + מפרטים | 7 סוכני-recon ‏+ 5 סוכני-spec מקביליים; כל ממצא מאומת מול הקוד עם file:line | בוצע — ‏`docs/specs/beta/*` (5 קבצים, ‏1,481 שורות) |
| שורש הטקסט המשובש של איתן | חסר JSON.parse ב-eitan-insights.tsx (רגרסיה f8ddc05); תוקן יחד עם buffering | אובחן; תיקון = EIT-1, יום א׳ |
| הנחות ספרינט פתוחות ליוני | "חמישי"=23/07 · ‏/coach נסגר מאחורי login · אונבורדינג אחרי login · חנויות: PWA+‏Play-פנימי קודם, iOS נדחה | ברירות-מחדל מסומנות בסעיף 2ב של התוכנית — כל אחת הפיכה במילה של יוני |
| **הכרעות יוני 19/07 ערב** (AskUserQuestion) | (1) קישור ציבורי חתום למסמכים — **כן**, תפוגה 90 יום, ‏`DOC_LINK_SECRET` על יוני · (2) אסקלציית איתן: ‏support → ‏`countme5555@gmail.com`; ידע-מס חסר → ניסוח צנוע "מערכת חדשה, ייתכן שחסר לי" בלי הפניה לאדם · (3) ‏dueDate — **בלי ברירת-מחדל**, בחירה ידנית פר-מסמך · (4) רמת pre — **הצעות מחיר + מדריכים בלבד** | נחתם — מעדכן את DOC-7, EIT-10/11, DOC-3, ONB-14; מתועד גם בסעיף 2א של התוכנית |
| ‏Supabase MCP | עדיין מחובר לחשבון הלא-נכון (akfg/BlondeShell, ‏INACTIVE) — ‏hbsgz לא נגיש מסשני-AI. ‏SQL ממתין רוכז לקובץ הדבקה אחד: ‏`docs/launch/hbsgz-pending.sql` | ליומיום אין תלות ב-MCP (האפליקציה עובדת עם env keys); תיקון קבוע = חיבור מחדש של קונקטור Supabase בחשבון של hbsgz |
| **יום-א׳ של הספרינט בוצע** (ביצוע ישיר, ‏workflow נפל על תקלת-הרשאות) | ‏(1) תוקן הטקסט המשובש של איתן בדשבורד (JSON.parse+buffer ב-eitan-insights) · ‏(2) ‏PROTECTED_PREFIXES הושלם לכל 12 הנתיבים + ‏?next= ‏end-to-end · ‏(3) ‏SignOutButton חובר ל-/home ו-/dashboard; דף הבית מודע-סשן ("לאזור האישי") והפך ƒ-dynamic · ‏(4) ‏/privacy + /terms ‏(DRAFT) + קישורי הפוטר והלוגין · ‏(5) הוסר מספר-הקצאה המפוברק; עוסק פטור חסום מחשבונית-מס · ‏(6) תיקוני /setup: שחזור שנה, ביטול פברוק 12% ב"ל, קופי אחסון אמין, הבהרת ב"ל/בריאות · ‏(7) ‏src/lib/ai/models.ts + לוג ‏[ai-usage] בכל 5 נקודות-הקריאה · ‏(8) ‏manifest ממותג + אייקוני PNG (נוצרו ב-chromium) + 404 עברי + ‏OG/viewport · ‏(9) ‏CONTRIBUTING-HE.md · ‏tsc נקי, ‏101/101, ‏build ירוק | בוצע 19/07 ערב; נדחף ל-`claude/system-beta-preparation-oiyzpy` |

## סשן 05/07/2026 — מעבר לעבודה עצמאית (Sonnet/Opus); החלטות ומשימות שבוצעו

> מהסשן הזה והלאה: **כל משימה בכל סשן מתועדת כאן באותו סשן** — זה מה שמאפשר ליוני להמשיך ברצף
> בכל מודל. פורמט: שורה בטבלה, עם סטטוס (סופי/revisitable) והפניה לתוצר.

| החלטה / משימה | ערך | סטטוס + תוצר |
|---|---|---|
| תוכנית 7 ימים 05/07→12/07 | משימות אטומיות עם בעלים+DoD; גייטינג→ג'מיני/תשתית→UX→שער-מורכבות→MCP v0→FLAG(Roy) | בוצע — תדריך v4 ב-[[STATUS]]; סדר הימים revisitable ע"י יוני |
| מיפוי שבריריות המנוע | סריקה מרובת-סוכנים + אימות אדוורסרי מול הקוד; false-positive vectors מדורגים | בוצע — `memory/risk-gap.md`; ‏16 מאומתים, 29 ממתינים לאימות (מגבלת תקציב), 4 הופרכו (מהם 2 עם ציטוטים מפוברקים — מאשש את מדיניות האימות); לתחזק בכל שינוי מנוע |
| תדרוך מומחים חיצוניים | ‏stack + ‏8 דילמות אסטרטגיות + 5 משפטיות + שאלות לפגישה | בוצע — `docs/consultation-prep.md`; טיוטה עד עין של יוני/רועי |
| **הכרעת ה-MCP: אופציה א'** | ‏MCP server של countme שעוטף את המנוע (5 כלים read-only, ‏v0 ‏stdio ללא PII) | הוחלט, **revisitable ע"י יוני** — ‏spec: `docs/specs/countme-mcp-server.md`; ‏v1 remote רק אחרי גייטינג+PII |
| ‏PR #28 (tax-rules) | ‏main מוזג לתוך הענף; קונפליקטי memory/ הוכרעו לטובת main; ‏101/101 + build לפני push | בוצע — ממתין ל-CI+מיזוג יוני |
| אימות גייטינג מרחוק | ‏Vercel MCP מסשני-web = החשבון הנכון; בדיקת `/dashboard` חיה היא ה-DoD, לא הצהרות | תקף מעתה — הגייטינג אומת ככבוי 05/07 15:24 UTC |
| הפרויקט הכפול | הריפו בונה לשני פרויקטי Vercel (‏`prj_3IlH…` הקנוני, ‏`prj_mz1u…` בחשבון `countmes-projects`) | עובדה מאומתת (2 תגובות bot על #28); ניתוק = משימת יוני 07/07 |
| לינק Gemini | ‏share.gemini.google חסום מהקונטיינר (403 גם ב-proxy) — אין טעם לנסות שוב מסשן web | נעול טכנית — יוני מדביק טקסט; משבצת 07/07 שמורה |

### המשך-סשן 14/07 (אחרי שבוע ללא פעילות; באותו סשן Fable)

| החלטה / משימה | ערך | סטטוס + תוצר |
|---|---|---|
| סבב אימות ידני משלים | ‏11 ממצאי-★ מ-risk-gap §3 אומתו בקריאת קוד ישירה (בלי סוכני-משנה — מגבלת תקציב) | בוצע — ‏27/‏18/‏4 (מאומת/ממתין/הופרך); עודכן ב-`risk-gap.md` + ‏PR #29 |
| ממצא דחוף חדש-מאומת | פרומפטי איתן מפרים "LLM לא מחשב": הוראת חישוב תרומות X×0.35 + תקרת זעיר קשיחה 120,000 ₪ + חישוב משרד-ביתי | תיקון תוזמן כמשימה 6 ביום-ג' של התוכנית; עד אז ידוע-ולא-מתוקן |
| הזזת לוח התוכנית | שבוע 06–13/07 עבר ללא ביצוע — התוכנית נשארת, "יום ב'" = יום החזרה של יוני | עודכן ב-STATUS; ‏#28/#29 עדיין ממתינים למיזוג |

### המשך-סשן 15/07 ערב — מיזוג PR #28 + #29 (הרשאה מפורשת מיוני)

| החלטה / משימה | ערך | סטטוס + תוצר |
|---|---|---|
| מיזוג ‏#28+#29 ל-main | יוני אישר במפורש: "יכול להטמיע... תעשה bypass באישורי אם צריך" — הרשאה ישירה למזג, כולל bypass על אישורים/הגנת-ענף אם דרוש | בוצע — נבדקו קונפליקטים (0) + CI (ירוק) לפני מיזוג; לא נדרש bypass בפועל (הרשאות ה-token הספיקו); סדר: מנוע (#28) לפני תיעוד (#29) |
| אימות פוסט-מיזוג | ‏main מקומי מעודכן, טסטים+build ירוקים, פרודקשן ב-Vercel אומת כ-READY לשני ה-merge commits | תקן חדש: כל merge-בהרשאה חייב אימות build+test+deploy מיד אחרי, לא רק לפני |

### המשך-סשן 15/07 (מעבר בפועל ל-Sonnet 5; אותו סשן ממשיך)

| החלטה / משימה | ערך | סטטוס + תוצר |
|---|---|---|
| מודל הסשן | עבר מ-Fable ל-**Sonnet 5** (‏`/model claude-sonnet-5`) — זהו המעבר לעבודה עצמאית שתוכנן ב-05/07 | בפועל מעכשיו; הזיכרון (STATUS/decisions/progress) הוא שכבת הרציפות היחידה בין המודלים |
| סבב אימות מלא ל-risk-gap | ‏15 סוכנים אדוורסריים במקביל (Workflow), אחרי שהתברר שמגבלת התקציב החודשית התאוששה — אפס כשלים, אפס agents_error | בוצע — `risk-gap.md`: ‏35 מאומתים / 0 ממתינים / 6 הופרכו (2 הפרכות עם ניסוח-מתוקן שנוסף כממצא חדש) |
| ממצא חדש בחומרה הגבוהה ביותר בסבב | `estimateTaxLiability` לא יורש דגלי אי-ודאות מ-field044/field068 — `TaxEstimate` type חסר שדה confidence/assumptions לגמרי | תועד כממצא #24 + משימה 7 חדשה בתוכנית (הוספת `assumptions: string[]`) |
| דפוס נוסף שהתגלה | hints בסכימה (`schema.ts`) הם מחרוזות חופשיות לא-מקושרות ל-types.ts — 3 מקרים ידועים כבר לא-מסונכרנים (048/072/045-046), לא מקרה בודד | תועד כממצא #25 + משימה 8; מציע exhaustiveness test או hints דינמיים |
| לקח מתודולוגי מאושש | 2 מהממצאים ש"אושרו" בסבבים קודמים (form-footer-legal-note, threshold-6111-no-boundary-test) התבררו כמדויקים-חלקית באימות עמוק יותר — לא רק "True/False" בינארי | מעודכן ב-`risk-gap.md` §4; מחזק את מדיניות "אימות תמידי" מ-03/07 גם לגבי ממצאים שכבר אומתו |

## מדיניות ניהול סשנים (נעול 03/07/2026)

| החלטה | ערך | הנמקה |
|---|---|---|
| פתיחת סשן | ‏merge של memory-PR קודם → פרומפט קבוע: קרא STATUS, אשר הבנה ב-2 שורות, gaps בסוף כל פלט | ‏main מוגן והסשן החדש משכפל אותו — תדריך שלא מוזג לא קיים עבורו |
| ענף | ‏`claude/<topic>` אחד לסשן; אחרי merge — הענף מתאתחל מ-main | היסטוריה נקייה, ‏CI על כל כניסה ל-main |
| סשנים מקבילים | קבצים זרים בלבד; נגיעה ב-`lib/calculators`/`types.ts` מחייבת **ביקורת-צולבת על הקוד** (worktree + הרצת טסטים) לפני merge | מנע פינג-פונג "תיקונים" בין סשנים; עבד ב-03/07 (ענף tax-rules אומת ב-5 דק') |
| דיווחי-AI | שום דיווח (מכל מודל, כולל שלי) אינו אמת בלי אימות מול הריפו/מקור ראשי | פברוק ג׳מיני (פיצ'רים שלא קיימים) + 4 מספרי-מס שגויים "מאומתים" |
| "CONFIRMED" על קבוע-מס | רק מקור ראשי או ≥2 בלתי-תלויים, עם תאריך-תוקף (שיעורים משתנים באמצע שנה!) ורמת סבירות | ‏7.12% היה "מאושר" ממקור מסכם מיושן (תיקון 252 מפברואר 2025) |
| סודות | לעולם לא בצ'אט — רק password manager / שדות env של הקונפיג. הודבק בטעות → ‏revoke מיידי | תקרית הטוקן 02/07 (בוטל תוך דקות) |
| סגירת סשן | ‏wrap-up מלא (STATUS/progress/decisions/retro) + ‏PR ל-main; ‏retro פר-סשן-גדול | רציפות בין סשנים בלי אובדן ידע |

## ארכיטקטורה + אבטחה (נעול 02/07/2026, gate-approved)

| החלטה | ערך | הנמקה |
|---|---|---|
| חישובי מס | **דטרמיניסטי בלבד + golden tests חובה** | LLM לעולם לא מחשב — רק מסביר/אוסף. כל שינוי קבוע-מס חייב לעדכן golden test באותו commit. `npm test` לפני push |
| קבועי-מס | **רק** ב-`lib/calculators/types.ts` (year-keyed) | אפס literals בקומפוננטות/פרומפטים; תוקנו תרומות 207+תקרת 30%, ביטוח חיים 25% (מקורות בקוד) |
| auth ל-API | דגל **אחד** — `AUTH_GATING_ENABLED` סוגר דפים+API יחד | בלי דגל שני; הבטא נשארת פתוחה עד ההדלקה של יוני |
| rate limiting | in-memory מוקשח עכשיו; durable = Vercel WAF ואז Supabase-counter | לא מוסיפים dependency בלי לעדכן CLAUDE.md (מוסכמה 7) |
| next | patch-upgrades אבטחה מיידיים (16.2.10) | advisories של middleware bypass רלוונטיים ישירות ל-gating |
| מיצוב קופי | **"מחשבון מדויק. לא ייעוץ מס."** — `<LegalNote>` יחיד | לא overclaim ("מחליף רו"ח") ולא over-hedge ("הערכה" על חישוב מדויק). כל קופי משפטי DRAFT עד יעל |
| סוכן רגולציה | מציע patch כ-diff באישיו; **לעולם לא ממזג לבד** | human-in-the-loop על כל שינוי קבוע; auto-PR רק אחרי אישור מפורש (הרשאות workflow) |
| סקילים | סקיל = קלט, לא סמכות | הסקיל עצמו הכיל סולם מילואים שגוי; כל מספר מסקיל מוצלב מול מקור ראשי לפני שימוש |

## Brand Kit (נעול 03/06/2026)

| החלטה | ערך | הנמקה |
|---|---|---|
| פונט | **Assistant** בלבד (משתנה `--font-assistant`) | מחליף Heebo/Rubik — פונט יחיד לכל גופות האפליקציה, Hebrew+Latin, Google Fonts. סופי. |
| פלטת צבעים | נייבי `#083A4F` / בז' `#C8B59A` / טיל `#407E8C` | מחליף את צבעי amber הישנים. טוקנים ב-`globals.css`, לא לשנות ישירות בקומפוננטות. סופי. |
| Brand primitives | `src/components/brand/{logo,button,icons,status}.tsx` | מקור-אמת לכל UI מחוץ ל-`/demo`. אסור לכתוב כפתור/אייקון ישירות — רק דרך הפרימיטיבים. |
| `/demo` exempt | הפורם ב-`/demo` לא עבר ריברנד | gov.il faithful — החלטה נעולה מהתחלה. כל `gov-*` class בפורם — לא לגעת. |
| emoji = אסור | ללא emoji בכל האפליקציה | Brand Kit README אומר מפורשות "No emoji". רק אייקוני קו מ-`icons.tsx`. |
| Brand Kit מיקום | `Brand Kit/README.md` | קנוני לכל AI session עתידי — לקרוא לפני עבודת UI. |

## מודל שנות-המס (נעול 31/05/2026)

| החלטה | ערך | הנמקה |
|---|---|---|
| ברירת-מחדל לתצוגה | **2024** (`DEFAULT_VIEW_YEAR`) | יציבות לדמו EY — `/demo` ו-`/file` נוחתים על שנה מוכרת |
| שנה פתוחה להגשה | **2025** (`ACTIVE_FILING_YEAR`) | הדו"ח שמגישים *עכשיו* (היום 2026) הוא לשנת 2025 |
| מודל סטטוס | filed / open / future | 2024 הוגש (קריאה בלבד) · 2025 פתוח · 2026 עתידי/בצבירה |
| נתוני דמו ל-2025 | מיחזור 2024 (אותם 248,500/47,800) | להראות שנה פעילה מאוכלסת בלי נתונים חדשים |
| `TAX_YEAR_2026` | מהסקיל `israeli-tax-returns`, עם `TODO(Roy)` | מדרגות 2025 *שגויות* ל-2026 (חוק ההתייעלות) — עדיף ערך-סקיל מסומן מאשר ערך-שגוי שקט |

## פיילוט SaaS חי (נעול 10/06/2026)

| החלטה | ערך | הנמקה |
|---|---|---|
| Backend | **Supabase פרויקט `hbsgzelipeawkvtcazdr`** (לא akfg) | החשבון של יוני; הסכמה נבנתה שם מחדש. גובר על "Supabase יום 2+" — **חי עכשיו** |
| אחסון persona | `profiles.persona jsonb` (+ טבלאות מנורמליות) | lift מהיר ללא אובדן; הטבלאות לפיצ'רים טרנזקציוניים |
| auth | Google OAuth, **flag-gated** (`AUTH_GATING_ENABLED`) | gating כבוי כברירת-מחדל עד הדלקה ידנית — לא נועל את הדמו בטעות |
| סוג עוסק | **יחידים בלבד**: זעיר/פטור/מורשה (בלי חברה) | 1301 ליחידים; חברה=1214. אין "עוסק מורשה זעיר" |
| מיצוב | **"עובדות, לא עצות"** | הוסרו המלצות (חשיפה משפטית); איתן נוטרל |
| ברירת-מחדל שנת-מס | **2025** | מגישים עכשיו את 2025. גובר על `DEFAULT_VIEW_YEAR=2024` |
| מילואים | מדרגות **30/40/50** ימים (2025–2027) | תיקון 283; 20/45/60 = 2028+ (אומת audit+Gemini) |
| localStorage | **נשאר cache** + DB-authoritative (owner-stamp) | refactor DB-only שובר דמו אנונימי+~13 קוראים → דחוי |
| SDK | supabase-js **2.108** / ssr **0.12** | נדרש לטייפים המודרניים |

## עקרונות עבודה

- **לא משנים מספרי מס בשקט.** כל ערך שנתי-משתנה שאינו ודאי → `TODO(Roy)` עד אישור מול המקור
  הרשמי. הסקילים ה-`israeli-*` הם סמכות-הדומיין; לא להסתמך על ידע training לסכומים שמתעדכנים שנתית.
- **single-source לשנים:** כל rate/cap/rule זורם מ-`lib/calculators/types.ts` → אסור לקודד ערך
  בקומפוננטה/מחרוזת/דוח.
- **worktrees לאצוות עצמאיות בלבד.** פיצ'ר מצומד (קבועים→פרסונה→schema→UI) נעשה במסלול יחיד —
  worktrees מקבילים עליו רק ייצרו קונפליקטים.
- **חתימת commit:** נכשלת בתוך worktrees (שרת החתימה דורש את ה-checkout הראשי) → סוגרים קומיטים
  מהמאגר הראשי (cherry-pick), או `--no-gpg-sign` ב-worktree ואז cherry-pick.

## החלטות תיעוד (31/05/2026)

- **זיכרון הפרויקט = `memory/`** עם 4 מסמכים: `readme` · `status` · `progress` · `decisions`.
- **`HANDOFF.md` נמחק** — הוחלף ע"י `memory/STATUS.md` + `memory/progress.md`.
- **`CLAUDE.md`** נשאר ייעודי ל-Claude Code (הקשר ארכיטקטוני), לא לסטטוס-סשן.
- **טראקר המשימות** (`docs/meeting-records/yoni-tasks-27032026.md`) נשאר מקור-אמת ל-11 המשימות.

## רקע (מ-CLAUDE.md, לא לפתוח מחדש)

Stack: Next.js 16 + React 19 + TS + Tailwind 4 + Anthropic SDK · Vercel · Supabase (יום 2+) ·
מודל AI ברירת-מחדל `claude-sonnet-4-6` · עברית RTL בלבד · הטופס = רפרנס ויזואלי (לא rebuild 1:1) ·
פרסונה כ-JSON יחיד ב-`personas/dana-cohen.json`.
