---
tags: [decisions, countme]
related: "[[STATUS]] · [[progress]] · [[retro-2026-07-03]]"
---

# decisions — יומן החלטות נעולות

## סשן 06/09/2026 (סבב 6, Sonnet 5) — מתודולוגיית רמת-ודאות (4 דרגות), מחליפה את הכלל החד-שורתי הישן

**הקשר:** סבב-האימות של היום (§ risk-gap.md §7) חשף שהתיוג "CONFIRMED"/"מאומת" הופעל בפועל בצורה
לא-אחידה — למשל "אישור מצה"ל" למילואים תויג "confirmed_facts" למרות שהאימות היחיד היה synthesis
של ~10 תוצאות-חיפוש, **אפס fetch ישיר למקור ראשי** (ה-proxy הסביבתי חוסם gov.il/btl.gov.il/
miluim.idf.il). הכלל הישן ("ארכיטקטורה+אבטחה", 02/07) — "רק מקור ראשי או ≥2 בלתי-תלויים, עם
תאריך-תוקף ורמת סבירות" — נכון בכיוון אבל לא הבחין בין "fetch ראשי אמיתי" ל"ריבוי-מקורות-משניים
שמתכנסים". **נעול מעתה — 4 דרגות, לא בינארי:**

| דרגה | תנאי-סף | מה מותר לעשות איתה |
|---|---|---|
| **1 · CONFIRMED** | fetch/קריאה ישירה ממקור-ראשי בפועל (טקסט-חוק, עמוד gov.il/btl.gov.il שנקרא בפועל לא רק כותרת מ-snippet) **או** ≥2 מקורות-איכותיים בלתי-תלויים (לא שני בלוגים שמעתיקים זה-את-זה) שמתכנסים + תאריך-תוקף מוצהר + בדיקת-סתירות | לקבוע `confidence: "high"` בקוד, לכתוב כעובדה במוצר בלי הסתייגות |
| **2 · STRONGLY-SUPPORTED** | ≥3 מקורות עצמאיים מתכנסים (רו"ח/kolzchut/גופים), **בלי** fetch ישיר למקור-ראשי (חסום/לא-זמין) — **זה המצב האמיתי של רוב "confirmed_facts" מהסבבים האחרונים, כולל מסמך-המילואים** | מותר להשתמש בתכנון/קוד, אבל **חובה** לתייג "תמיכה-נסיבתית-חזקה, לא ציטוט-ישיר" בכל תיעוד, ולתזמן אימות-חוזר (ראה /loop למטה) |
| **3 · UNVERIFIED/INFERRED** | מקור יחיד, ניחוש-הקשרי בלי חיפוש בפועל, או מקורות סותרים | **אסור** לכתוב כעובדה למשתמש/ת או כ-`confidence:"high"` בקוד — `TODO(Roy)`/flag בלבד |
| **4 · REFUTED** | נבדק ונמצא שגוי/מפוברק | להסיר/לתעד בטבלת-הפרכות (`risk-gap.md` §4) |

**החלה-לאחור על היום:** "אישור מצה"ל" למילואים (§ risk-gap.md §7) יורד מ"CONFIRMED" ל-**דרגה 2** —
תמיכה-נסיבתית-חזקה (~10 מקורות רו"ח), לא ציטוט-ישיר. אותו דבר לגבי רוב ה-"confirmed_facts" של
סבב-3010/מסמכי-ההנחיה — **לתייג את כל השורות הרלוונטיות ב-`risk-gap.md`/`docs/plans/2026-09-06-*`
בדרגה 2, לא 1**, בסבב-העדכון הבא.

**/loop הוקם (06/09) לאימות-חוזר תקופתי מול הטבלאות** — ראו STATUS לפרטי-ההרצה. מטרתו: לשדרג
פריטי-דרגה-2 לדרגה-1 (או לגלות סתירה/עדכון-רגולטורי) בכל הרצה, לא רק "לבדוק שאף דבר לא השתנה".

## סשן 06/09/2026 (Sonnet 5) — CPA-BNG: לא שותפות; היקף צרפתית+מסמכי-מילואים

מקור מלא: `docs/meeting-records/cpa-bng-collab-06092026.md` + `docs/plans/2026-09-06-pre-cpa-bng-demo-sprint.md`.

| החלטה | ערך | הנמקה |
|---|---|---|
| **מעמד CPA-BNG** | לא שותפות פעילה כרגע, גם אם הם מעוניינים — המטרה היא להציג דמו מרשים בלבד | יוני, מפורש: "הם לא יהיו שותפים, למרות שהם רוצים" |
| **היקף ממשק-צרפתית** | "בסיסי" — dictionary/overlay על מסכים נבחרים, **לא** תשתית i18n מלאה לכל 160+ הקבצים | יוני אמר "ממשק בסיסי" במפורש; אילו מסכים בדיוק — עדיין פתוח, לא לפתוח לבד |
| **מנגנון "איזה מסמך להעלות" (מילואים לדוגמה)** | הרחבה של תשתית ה-upload/OCR הקיימת (סלוט חמישי מותנה), לא מנוע-checklist גנרי חדש | יש כבר תשתית מוכחת ב-`api/upload/route.ts`; להתחיל קונקרטי (מילואים) ולהכליל רק אחרי 2+ מופעים |
| **"טופס 3010" למילואים — לא מאומת, לא לבנות עליו** | הסקיל `israeli-bituach-leumi` מכיר רק 501/502/509 למילואים — "3010" לא בקטלוג הרשמי | לא משנים/כותבים מספרי-טופס בשקט (עיקרון-ברזל קיים); ממתין לאישור יוני על השם/מספר/URL המדויק |

## סשן 06/09/2026 (המשך, Sonnet 5) — Workflow 5-סוכנים: אימות-3010 חלקי, הכרעת registry-vs-קונקרטי

מקור מלא: `docs/plans/2026-09-06-pre-cpa-bng-demo-sprint.md` (עודכן משמעותית). Workflow של 4
אודיטים מקבילים + מבקר-שלמות אחד מצא סתירות אמיתיות בין הדוחות — כל אחת הוכרעה במפורש, לא הושארה.

| החלטה | ערך | הנמקה |
|---|---|---|
| **טופס 3010 — אומת חלקית** | כן קיים, "אישור על שירות מילואים", **מונפק ע"י צה"ל** (לא ב"ל — זה הסביר למה הסקיל לא הכיר אותו), הפקה עצמית ב-miluim.idf.il | חיפוש-רשת ישיר מצא מקורות (kolzchut, miluim.idf.il הרשמי). **עדיין לא מאומת:** האם הטופס מפריד ימי-לוחם מסך-כל-הימים — לא לבנות פרומפט-OCR שמניח זאת בלי בדיקה נוספת |
| **מנגנון-מסמכים: קונקרטי לפני registry כללי** | הרחבת `SlotConfig` הקיים (2 שדות: `applicableIf`/`ocrRequired`) ל-2-3 מופעים (ב"ל+קרן-השתלמות כ-quick-win, מילואים שלישי) — **לא** `lib/documents/registry.ts` נפרד | סתירה אמיתית בין 2 דוחות: אחד הציע registry ל-13 מצבים בבת-אחת, שני ציטט את ההחלטה הקיימת "start concrete, generalize later" מאותה תוכנית. נבחר להישאר עם ההחלטה הקיימת |
| **כלי-עיצוב לא נוגעים ב-`form-preview.tsx`** | תיקון להמלצה שגויה של דוח-העיצוב שכלל את `/demo` בפס Figma | `form-preview.tsx` נעול gov.il-faithful ב-CLAUDE.md — אסור לגעת ויזואלית |
| **טיוטת-onboarding ב-localStorage דורשת החלטת-פרטיות מיוני לפני מימוש** | לא להניח שהחלטה טכנית (לשמור טיוטה למניעת אובדן-נתונים) פותרת את עצמה — ת.ז./בנק ב-localStorage לא-מוצפן מתנגש עם תיקון-13 | מבקר-השלמות תפס שהתיקון להמלצה #1 (draft persistence) לא התייחס לפרטיות בכלל |
| **"סקייל" דו-משמעי — נשאלה שאלה מפורשת ליוני, לא נוחש** | תשתית (רוב כבר בנוי) מול היקף-תרחישי-מסמכים (השאלה הסבירה יותר בהקשר) | המבקר: הטרמינולוגיה של הריפו כבר נועלת "סקייל"=תשתית, אבל ההקשר שיוני השתמש בו (אותה נשימה עם onboarding+מסמכים) מרמז אחרת |
| **⚠️ תזכורת שלישית (לא חדשה): `rate_limit_buckets`/WAF עדיין לא מאומתים בפרודקשן** | נכשל-פתוח אם המיגרציה לא חלה; WAF נבדק 19/08 והראה 0/40 חסימות בפועל | דווח פעמיים קודם (03/08, 18/08) ועדיין לא נסגר — MCP-פעולה של יוני, לא קוד |

## סשן 06/09/2026 (סבב 6, Sonnet 5) — ביצוע-קוד: 4 מ-5 תיקוני-עדיפות, כל אחד עם שער מלא

יוני אישר "תתחיל לתקן". 4 תיקוני-קוד נכנסו (5 קומיטים, `claude/cpa-bng-count-collab-h25ga3`), פרטים
מלאים ב-`memory/risk-gap.md` §8. שתי החלטות-עיצוב ששווה לתעד כאן:

| החלטה | ערך | הנמקה |
|---|---|---|
| **טבלת-ילדים-מתוקנת מיושמת בקוד, לא רק בהערה** | STRONGLY-SUPPORTED לפי המתודולוגיה, מתויגת ככזו בקומנטים — לא ממתינה יותר "עד אישור רועי" כדי להיכנס לקוד | הטבלה הישנה הייתה **ודאית-לא-שלמה** (מתועדת בקוד עצמו כ"PRE-2024 base"), לא רק "לא-מאומתת" — המצב הקודם (0.75-0.88 ביטחון) עדיף על המצב הישן (0% נכון לפי החוק) |
| **משרד-ביתי נשאר במכוון לא-ממודל** | לא הומצא אחוז-קבוע לניכוי משרד-ביתי | דורש שדה-יחס-שטח שלא קיים ב-Persona; המצאת-אחוז הייתה חוזרת בדיוק על מחלקת-הבאג שהתיקון סוגר |
| **שדה `healthTaxAnnualPaid` — תוספתי, לא rename** | לא לשנות/למחוק `annualPaid` הקיים | המוצר חי בפרודקשן; rename ידרוש migration לנתונים אמיתיים — שינוי תוספתי הוא בטוח-לגמרי |

## סשן 06/09/2026 (סבב 4, Sonnet 5) — יוני הכריע 5 החלטות; ⚠️ "טופס 3010" התברר כמסמך הלא-נכון

יוני אישר: (1) שמירת-טיוטה — כן, בהחלט. (2) OCR למילואים + מסמכים-נלווים. (3) עיצוב (tweakcn→
daisyUI→Figma) — מאושר. (4) "סקייל" = הרבה משתמשים, לא היקף-תרחישים. בעקבות סעיף 2, מחקר-המשך
העלה תיקון מהותי:

| החלטה | ערך | הנמקה |
|---|---|---|
| **מדיניות-פרטיות לטיוטת-onboarding — הוכרעה עצמאית** | לשמור הכל **חוץ מ**-ת.ז./בנק, TTL 48-72 שעות, מחיקה מיידית אחרי submit | יוני אמר "חייבים" בלי לענות על תת-השאלה — הכרעתי לפי עקרון-מזעור-PII הקיים כבר בפרויקט |
| **⚠️ "טופס 3010" הוא כנראה המסמך הלא-נכון לזיכוי שאנחנו מחשבים** | הזיכוי (תיקון 283) דורש מסמך נפרד — "אישור נקודות זיכוי לשנת [שנה]" מ-miluim.idf.il, מוגש עם טופס 101 למעסיק (שכיר); 3010 הוא הצירוף לטופס-502 (תביעת-פיצוי מול ב"ל), נושא אחר. תמיכה-נסיבתית חזקה (3 מקורות רו"ח + btl.gov.il) שהם לא אותו מסמך ושרק ה"נקודות-זיכוי" מפריד ימי-לוחם | לא לבנות OCR סביב 3010 לצורך הזיכוי — לבנות סביב "נקודות זיכוי לשנת [שנה]" במקום. **עדיין לא מאומת 100%** (fetch ישיר לטפסים חסום ע"י ה-proxy הסביבתי) — שווה אימות-נקודתי נוסף לפני כתיבת פרומפט-OCR סופי |
| **Supabase MCP — נבדק שוב, עדיין רואה חשבון לא-נכון** | `list_projects` מחזיר רק `megamodel2000@gmail.com`'s Project (לא-קשור), לא hbsgz | אותה בעיה מ-03/08/05/07 — לא נפתרה; מומלץ ליוני לחבר-מחדש את ה-connector, לא רק env keys |

## סשן 06/09/2026 (סבב 5, Sonnet 5) — Workflow 7-סוכנים: עדכון-סטטוס מלא ל-risk-gap.md + 6 ממצאים חדשים

יוני ביקש הבנה עמוקה של "מה דורש אימות נוסף מכל הדאטה החשבונאי" — הרחבתי מעבר לשאלת-המילואים
לסבב-אימות מלא של `memory/risk-gap.md` (35 ממצאים, לא עודכן מ-15/07) מול הקוד של היום. מקור מלא:
`memory/risk-gap.md` §7 (עודכן במקום, לא נמחקה היסטוריה).

| החלטה/ממצא | ערך | הנמקה |
|---|---|---|
| **מילואים — אומת סופית, ברמת-ודאות גבוהה** | "אישור מצה"ל" (בלי מספר-טופס רשמי), מאשר לוחם/ת+ימים, מצורף ישירות לטופס 1301 ע"י עצמאי/ת בסוף השנה — לא 3010, לא טופס 101 | תיקון 283/סעיף 39ב, ~10 מקורות רו"ח עצמאיים מתכנסים לאותה תמונה |
| **⚠️ נוסחת-המילואים בקוד (`MILUIM_CREDIT_TIERS_2026`) מעולם לא הושוותה לחוק** | לא הוכרז שבור — רק לא-נבדק | פער-מתודולוגי שהמבקר תפס: אימתנו את זהות-המסמך, לא את נכונות-המתמטיקה |
| **2 חדשים ב-HIGH: נק'-זיכוי-ילדים חסרות + ניכוי-חלקי-לפי-קטגוריה לעולם לא מיושם** | ילדים: טבלת-2024 לא-מלאה בקוד עצמו (FLAG(Roy) קיים, מוסתר כי לדמו אין ילדים) · ניכוי-חלקי (רכב/טלפון/משרד-ביתי): 100%-ניכוי תמיד בפועל, גם ב-`/expenses/new` שהוא ה-reference-implementation של CLAUDE.md | תועד ב-`risk-gap.md` §7.4; לא תוקן היום, רק תועד |
| **CLAUDE.md item 5 תוקן** | 2/3 מפריטי "FLAG(Roy) burn-down" כבר פתורים בפועל (תקרות-פנסיה, רצפת-תרומות) — נשאר רק §45א ביטוח-חיים | תוקן במקום, כמו התקדימים הקודמים בקובץ עצמו |



> החלטות שלא חוזרים עליהן בלי דיון מחדש. הטבלה המורחבת של החלטות-המוצר חיה ב-`CLAUDE.md`
> ("Project decisions" + "Design decisions"); כאן ההחלטות הפעילות + ההנמקה התמציתית.

## סשן 22/08/2026 (Sonnet 5) — עקרון-עבודה: הפרדות קשיחות בין פיצ'רים לפי סוג עוסק

**נעול (יוני, מפורש, אחרי מסירת דוח מע"מ תקופתי):** כשפיצ'ר רלוונטי רק לחלק מסוגי-העוסק, ההפרדה
צריכה להיות **קשיחה**, לא רק תיאורית/בעיצוב — ובבדיקה **כפולה**: גם בשכבת-הנתונים/לוגיקה (הפונקציה
עצמה מסרבת/מחזירה not-applicable לפרסונה הלא-רלוונטית) וגם בשכבת-הניווט (קישור-הכניסה לא מוצג
בכלל למי שלא רלוונטי לו — לא רק שהתוכן מאחוריו ריק). הדוגמה הקונקרטית שנעלה את הכלל: דוח מע"מ
תקופתי (`lib/vat-report/index.ts`) — `applicable` תלוי אך ורק ב-`osekType==="morshe"` (**לא**
ב-`isOsekZeir` — מורשה-זעיר עדיין רואה דוח, כי מע"מ הוא עניין osekType בלבד, בעוד שהמסלול זעיר
הוא עניין מס-הכנסה נפרד); קישור-הניווט ב-`dashboard/pro` מותנה אותו תנאי בדיוק, כך שעוסק פטור
(כולל פטור-זעיר) לא רואה קישור בכלל, ואם מגיע לעמוד ישירות ב-URL רואה כרטיס-הסבר, לעולם לא מספרים.

**דפוסים קיימים בקוד שכבר עקביים עם הכלל הזה** (לא צריך להמציא מחדש, רק להמשיך): `allowedDocTypesFor
(osekType)` בג'נרטור-החשבוניות · `isMursheZeir` על `CeilingAlert` (מבחין בין מורשה-זעיר לפטור-זעיר
בקופי-האזהרה) · `osekLabel` בכמה מקומות. **לא לשכפל תנאי osekType/isOsekZeir בכמה call sites בעיצוב
חדש** — לרכז בפונקציה/hook יחיד (כמו `computeVatReport`/`computeCeilingAlert`) ולקרוא ממנו בכל שכבה.

## סשן 20/08/2026 (המשך, Fable) — "השאר מחובר/ת" נעול; עובדה טכנית קבועה על @supabase/ssr

**נעול (יוני, מפורש):** ברירת-המחדל היא **התנתקות אחרי כל שימוש** — לא נשאר מחובר בין ביקורים
אלא אם המשתמש מסמן במפורש "השאר אותי מחובר/ת" ב-`/login`. מומש ב-`src/lib/auth/session-preference.ts`.

**עובדה טכנית לתעד לכל סשן עתידי שיגע ב-auth/cookies — לא לנסות שוב:** `@supabase/ssr` בגרסה
המותקנת (0.12.0) **קושח את ה-Max-Age של cookie-האימות ל-400 יום תמיד** —
`applyServerStorage`/`applyBrowserStorage` ב-`cookies.js` בונים את אפשרויות-ה-cookie כ-
`{...DEFAULT_COOKIE_OPTIONS, ...cookieOptions, maxAge: DEFAULT_COOKIE_OPTIONS.maxAge}` — כלומר
**דורסים כל `cookieOptions.maxAge` שמעבירים אחרי הפריסה**, בכוונה. אין דרך נתמכת (נכון ל-0.12.0)
לגרום ל-cookie האימות עצמו להיות session-only (נעלם בסגירת דפדפן) דרך ה-API הרשמי. **פתרון
שנבחר:** מדיניות ברמת-אפליקציה מעל ה-cookie (sessionStorage לזיהוי "עדיין אותו סשן" +
localStorage ל"זכור אותי" מפורש) — לא לנסות לפצח את ה-cookie עצמו שוב. אם גרסה עתידית של
`@supabase/ssr` תשנה את ההתנהגות הזו, שווה לבדוק אם אפשר לפשט, אבל **לא להניח** — לאמת מול קוד-
המקור כמו שנעשה כאן (`node_modules/@supabase/ssr/dist/main/cookies.js`), לא מול תיעוד ישן.

## סשן 18/08/2026 לילה (Fable) — פיבוט: הרשמה פתוחה לכולם + מוכנות לסקייל

מקור מלא + רשימת המשימות המחודשת: `docs/plans/2026-08-18-master-task-list-v2.md`.

| החלטה | ערך | סטטוס |
|---|---|---|
| **הרשמה פתוחה לכולם** | בלי Test Users, בלי הגבלות. המסלול הקריטי: קניית דומיין אמיתי → אימות Search Console → העברת מסך ההסכמה של Google ל-Production (אי-אפשר על vercel.app) | **נעול כיעד; דחוי בפועל.** יוני אישר (18/08 לילה מאוחר): **אין דומיין כרגע, ממשיכים על `vercel.app` עד עדכון מפורש**. חומת ה-100 של Testing mode בתוקף לכל הבטא |
| **חינמי עם מכסים קשיחים** | נשארים Hobby+Free; תקרות שימוש קשיחות בקוד (פר-משתמש + גלובלית עם degradation אוטומטי); מעבר לתשלום רק כשההתרעות מראות צמיחה אמיתית | **נעול לעכשיו.** מודע לסיכון: Hobby אסור מסחרית — חובה Pro ברגע שגובים כסף |
| **התרעות כמויות במייל** | דייג'סט יומי (Vercel Cron + Resend) + מייל מיידי בחציית סף (בדיקה inline, לא cron) | **נעול.** Resend = תלות חדשה מאושרת |
| **אוטומציה: הגנות+עומס+canary** | פעולות-הגנה אוטומטיות (תמיד מדווחות במייל) + סקריפטי k6 נגד Preview + canary מבוסס feature-flag (ל-Hobby אין מובנה) | **נעול** |

⚠️ ממצא-על מהסריקה (18/08 לילה): ייתכן שה-rate-limiting העמיד **בכלל לא פעיל** בפרודקשן —
`checkRateLimitDurable` נכשל-פתוח אם מיגרציית `durable_rate_limit` לא הודבקה ב-hbsgz, ו-`hbsgz-pending.sql`
לא כולל אותה. אימות זה משימה 0.1, לפני הכול. בנוסף: כלי-הידע שורפים סבב Sonnet לכל שיחה רק כדי
לקבל "לא זמין" (המיגרציה לא חלה), וזליגת ה-Testing-mode היא חומת-100 קשיחה.

## סשן 18/08/2026 ערב (Fable) — משוב רוי + הודעת-החברים; 4 הכרעות יוני (AskUserQuestion)

מקור מלא: `docs/feedback/2026-08-18-roy-beta-feedback-tasks.md`.

| החלטה | ערך | סטטוס |
|---|---|---|
| **מסך-סיום /setup: מסלול אחד שתמיד שומר** | רוי איבד נתונים כי "כניסה ללוח הבקרה" ניווט בלי לשמור; יוני: "אסור שאנשים יירשמו כמה פעמים" | **נעול.** כפתור-סיום יחיד = התחברות+שמירה, עם חיווי "נשמר בענן". חוזה `decidePersonaOwnership()` נשמר כמו-שהוא |
| **"פירוט הכנסות/הוצאות בעמודים שלו" = קישור /invoices ו-/expenses מ-/demo** | מימוש ההבטחה הקיימת "לחץ לראות פירוט" על שדות 150/238; שימוש-חוזר בעמודים בנויים | **נעול** (ההערה המודגשת של יוני) |
| **אפליקציה נייטיבית: Capacitor — שתי החנויות** | מחליף את ברירת-המחדל הרכה מ-19/07 ("PWA+Play קודם, iOS נדחה"); iOS במקביל | **נעול, revisitable.** בסבב-הבטא: רק תיעוד + ביקורת browser-only APIs; סקאפולד ios//android/ בסשן ייעודי |
| **היסטוריית שיחות: כמה שיחות + רשימת-צד** | כמו במוקאפ "שיחות אחרונות"; לא ה-jsonb המת ב-profiles (מיועד למחיקה, WS7) | **נעול.** `chat_threads`+`chat_messages` עם RLS; אנונימי ב-/coach = בלי רשימה, זיכרון-בלבד (ברירת-מחדל הפיכה) |

## סשן 18/08/2026 (Fable+Sonnet) — יום-בטא: אבטחה, RAG, ביצועים, מסלול מורשה; החלטות נעולות

| החלטה / משימה | ערך | סטטוס + תוצר |
|---|---|---|
| **RAG = Claude-native בלבד, אפס ספקים בתשלום** | יוני שלל מפורשות OpenAI/embeddings ("אני יודע שזה בתשלום"); הציע Obsidian כבסיס | **נעול.** כספת `knowledge/` (66 רשומות) + `pg_trgm`+FTS ב-Postgres הקיים + RPC היברידי + TOC ב-prompt-cache לשקל. שום embedding model, שום מפתח חיצוני. אם הקורפוס יגדל משמעותית (אלפי צ'אנקים) — לשקול מחדש, לא לפני כן |
| **שכבת-ידע מופרדת לגמרי מ-DB-לקוחות** | יוני: "ככל שנגדל זה יהיה גדול יותר, צריך לנהל חכם" | **נעול.** `knowledge_chunks` בסכימה נפרדת, RLS deny-all, נגיש רק ל-service_role; לעולם לא JOIN עם טבלאות פרסונה/לקוחות |
| **מספרים אף פעם לא ברשומות הידע** | סיכון: ידע-סטטי שמצטט שיעור/תקרה יתיישן ויסתור את המחשבונים | **נעול.** כל רשומה מפנה למחשבון/שדה, לעולם לא כותבת ₪/% מוחלט. נאכף גם ב-review וגם ב-README של הכספת |
| **"graphify" לשיחות שקל = גרף דטרמיניסטי, לא GraphRAG** | נבדק ונדחה: LLM-extraction graph (Microsoft GraphRAG וכו') יקר, לא-דטרמיניסטי, סותר את הכלל "LLM לא מחשב" | **נעול.** `client-graph.ts` — פונקציות טהורות על הפרסונה (persona→typed nodes/edges), אפס LLM בבנייה, נחשף כ-3 כלים חדשים (top customers/מי-חייב/פילוח לפי קטגוריה) |
| **תקרת עוסק פטור/זעיר — ניסוח כפול, לא רק "פטור"** | יוני: מיתוג הפרד-ומשול, זעיר ופטור הם מסלולים נפרדים שיווקית | **נעול.** `computeCeilingAlert` גוזר תווית אחת מה-persona (`isOsekZeir`), כל הצרכנים (dashboard/alerts/coach) קוראים ממנה — לא לפצל string ידנית בשום call site |
| **מתג שנת-מס: בולט לוותיקים, שקוף לחדשים** | יוני: ברירת-מחדל למשתמש חדש = השנה הנוכחית, בלי להטריד; ותיק עם היסטוריה — לחיצה אחת | **נעול.** `YearSwitch` pill קטן, לא מודאל/onboarding נפרד |
| **תשתית `getFormSchema(formId, year)` — עכשיו pass-through, בנויה לעתיד** | יוני: "אם הטופס נראה בול אותו דבר אז אין למה, אבל בנוי לאופציה העתידית" | **נעול.** מחזירה את אותה סכימה סטטית לכל שנה היום (reference-equality מאומת בטסט); תוספת ניסוח-שנתי עתידי = שורה אחת ברג'יסטרי, לא ריפקטור |
| **Vercel region → fra1 (בוצע)** | יוני ביצע ידנית לפי הוראות; חינמי ב-Hobby | **נעול, בוצע** |
| **Supabase region נשאר בסינגפור לעכשיו (מעבר ל-eu-central-1 נדחה בכוונה)** | יוני: לא לשלם עד אחרי הבטא, שני המעברים חינמיים בעצמם אבל מעבר-DB דורש התחברות-מחדש של המשתמשים | **נעול לעכשיו — משימה #26 ב-task list, להעלות "אחרי הבטא" או כשיוני יגיד** |
| **Google OAuth — נאכף `prompt=select_account`** | QA דיווח "מוקאפ, בלי סיסמה" — נבדק מול לוגי Supabase: OAuth אמיתי בכל פעם, מה שנצפה הוא SSO שקט (session-Google חי מעבר ל-logout של countme) | **נעול.** לא מוקאפ — תוקן כך שבורר-החשבונות תמיד מוצג, למניעת כניסה-שקטה במחשב משותף |
| **דליפת-session בין חשבונות (ממצא #17) — חוזה בעלות יחיד** | QA דיווח F5 מחליף משתמש בלי logout; אושרר קריטי | **נעול.** `decidePersonaOwnership()` הוא מקור-האמת היחיד לכתיבה/קריאה של פרסונה בכל נתיב; שינוי עתידי לחוזה הזה חייב סקירה, לא תיקון-נקודתי |

## סשן 03/08/2026 (Sonnet) — תיקון-באגים מה-Routine היומי; החלטות ומשימות שבוצעו

| החלטה / משימה | ערך | סטטוס + תוצר |
|---|---|---|
| Tranzila webhook signature — לא לממש בניחוש | חיפוש רשת לא אישש את סכמת-החתימה האמיתית של Tranzila בביטחון מספיק; ניחוש קריפטו על webhook תשלומים גרוע יותר מ-stub מתועד ו-fail-closed | **הוכרע סופית ע"י יוני (03/08 מאוחר): סגור/לא רלוונטי** — "אין לנו API של טרנזילה ולא יהיה בקרוב". הוסר ממעקב החוסמים; לא לשאול שוב אלא אם יוני יפתח מחדש |
| CLAUDE.md rewrite (מ-`claude/system-beta-preparation-oiyzpy`, 26/07) | שכתוב 372→142 שורות | **לא נמשך לענף הזה** — לא נבדק מספיק בזמן הסשן כדי לסמוך עליו בעיוורון; דורש עין לפני מיזוג. `regulatory-status.md` מאותו ענף כן נמשך (קובץ עצמאי, ראה שורה למטה) |
| דפוס-תיקון ל-persona בין-מכשירים/בין-משתמשים | `useRequiredPersona()` (חוסם) / `usePersona()` (לא חוסם) הם הדפוס הקנוני מעכשיו לכל דף שקורא persona — **לעולם לא `loadPersona()` סינכרוני ישיר** בדף חדש | נעול — 17/17 דפים תואמים עכשיו; `setup/page.tsx` הוא היוצא-מן-הכלל המתועד (merge logic, לא gate) |
| `/api/doc-link` — אכיפת-בעלות עסק | תוכן המסמך (סכומים/לקוח) נשאר client-trusted (משקף מה שהלקוח כבר מציג); רק `business.tradeName`/`osekType` נאכף מול ה-persona האמיתי בשרת | נעול — no-op עד `AUTH_GATING_ENABLED=true` |
| `regulatory-status.md` נמשך מהענף התקוע | קובץ עצמאי, ללא קוד, בלי קונפליקטים — 12 סעיפים רגולטוריים/משפטיים | נעול — נמשך. **10/12 סעיפים 🔴, כולל "סוקר משפטי חיצוני פעיל"** — יוני אישר (03/08 מאוחר) שעדיין אין סקירה משפטית. לא לשאול כל סשן, זה ידוע |
| **הדומיין הקנוני = `countmedemo-eight.vercel.app`** — לא `countmedemo.vercel.app` | תוקן פעמיים באותו יום: תחילה שונה ל-`countmedemo.vercel.app` לפי בקשת יוני בלי אימות (טעות שלי); יוני ביקש וידוא; `get_project` על **כל 9** פרויקטי Vercel בחשבון `yonilev2003s-projects` הראה חד-משמעית ש-`countmedemo.vercel.app` לא שייך לאף אחד מהם | **נעול, מאומת מול Vercel API בפועל (לא ניחוש)** — הוחזר ב-`layout.tsx`/`.env.template`/runbook. ראה טבלת-דומיינים למטה |
| טבלת דומיינים אמיתיים — 9 פרויקטי Vercel בחשבון `yonilev2003s-projects` (`get_project`, 03/08) | `countmedemo`→`countmedemo-eight.vercel.app` · `countme-crm`→`countme-crm.vercel.app` · `pillar`→`migdalim.vercel.app` (⚠️ שם-דומיין שונה לגמרי מהפרויקט!) · `countme`→`countme-tau.vercel.app` · `language-coach`→`language-coach-mocha.vercel.app` · `project-9340v`→אין alias נקי, דיפלוי אחרון **ERROR** · `accounting-mvp`→`accounting-mvp-seven.vercel.app` · `psycho-booking`→`psycho-booking.vercel.app` · `countme-demo`→`countme-demo.vercel.app` | עובדתי — אף אחד לא מחזיק `countmedemo.vercel.app`. `pillar`/`countme-crm`/`language-coach`/`accounting-mvp`/`psycho-booking` הם מוצרים **נפרדים ולא-קשורים** (Pillar יש לו PR/ריפו פעיל משלו) — **לא** "כפילויות" של countmedemo, לא למחוק בלי בדיקה פר-פרויקט מפורשת |
| מחיקת פרויקטי Vercel "לא רלוונטיים" | יוני ביקש למחוק "כל שאר הדברים הלא רלוונטיים" | **לא בוצע — נשאל קודם.** פעולה הרסנית/בלתי-הפיכה על תשתית משותפת; אף פרויקט אחר לא מחזיק את הדומיין המבוקש כך שמחיקה לא תשיג את המטרה, וחלקם מוצרים נפרדים אמיתיים. ממתין להיקף מדויק מיוני לפני כל פעולת מחיקה |
| `AUTH_GATING_ENABLED=true` + תקציב Anthropic | שני צעדים ידניים מהרשימה | **בוצעו ע"י יוני (03/08 מאוחר)** — אושר בפירוש, לא לשאול שוב |

## סשן 19/07/2026 (Fable) — פיבוט בטא לפי המסמך האסטרטגי של תומי

| החלטה / משימה | ערך | סטטוס + תוצר |
|---|---|---|
| מיקוד הבטא | חיי היומיום ולא הדו"ח — 5 רכיבי סעיף 3 במסמך תומי; משטחי 1301 נשארים בקוד, יוצאים מהזרקור | הוחלט (יוני+תומי); תוכנית: `docs/plans/2026-07-19-beta-sprint.md` |
| אבולוציה ולא בנייה מחדש | התשתית (auth/Supabase/מסמכים/brand/מחשבונים) נשארת; מוחלפים הפנים: שאלון, דשבורד, מסמכים | המלצת-AI שאומצה כברירת-מחדל, **revisitable ע"י יוני** |
| סריקת-עומק + מפרטים | 7 סוכני-recon ‏+ 5 סוכני-spec מקביליים; כל ממצא מאומת מול הקוד עם file:line | בוצע — ‏`docs/specs/beta/*` (5 קבצים, ‏1,481 שורות) |
| שורש הטקסט המשובש של איתן | חסר JSON.parse ב-eitan-insights.tsx (רגרסיה f8ddc05); תוקן יחד עם buffering | אובחן; תיקון = EIT-1, יום א׳ |
| הנחות ספרינט פתוחות ליוני | "חמישי"=23/07 · ‏/coach נסגר מאחורי login · אונבורדינג אחרי login · חנויות: PWA+‏Play-פנימי קודם, iOS נדחה | ברירות-מחדל מסומנות בסעיף 2ב של התוכנית — כל אחת הפיכה במילה של יוני |
| **הכרעות יוני 19/07 ערב** (AskUserQuestion) | (1) קישור ציבורי חתום למסמכים — **כן**, תפוגה 90 יום, ‏`DOC_LINK_SECRET` על יוני · (2) אסקלציית איתן: ‏support → ‏`countme5555@gmail.com`; ידע-מס חסר → ניסוח צנוע "מערכת חדשה, ייתכן שחסר לי" בלי הפניה לאדם · (3) ‏dueDate — **בלי ברירת-מחדל**, בחירה ידנית פר-מסמך · (4) רמת pre — **הצעות מחיר + מדריכים בלבד** | נחתם — מעדכן את DOC-7, EIT-10/11, DOC-3, ONB-14; מתועד גם בסעיף 2א של התוכנית |
| ‏Supabase MCP | עדיין מחובר לחשבון הלא-נכון (akfg/BlondeShell, ‏INACTIVE) — ‏hbsgz לא נגיש מסשני-AI. ‏SQL ממתין רוכז לקובץ הדבקה אחד: ‏`docs/launch/hbsgz-pending.sql` | ליומיום אין תלות ב-MCP (האפליקציה עובדת עם env keys); תיקון קבוע = חיבור מחדש של קונקטור Supabase בחשבון של hbsgz |
| **יום-א׳ של הספרינט בוצע** (ביצוע ישיר, ‏workflow נפל על תקלת-הרשאות) | ‏(1) תוקן הטקסט המשובש של איתן בדשבורד (JSON.parse+buffer ב-eitan-insights) · ‏(2) ‏PROTECTED_PREFIXES הושלם לכל 12 הנתיבים + ‏?next= ‏end-to-end · ‏(3) ‏SignOutButton חובר ל-/home ו-/dashboard; דף הבית מודע-סשן ("לאזור האישי") והפך ƒ-dynamic · ‏(4) ‏/privacy + /terms ‏(DRAFT) + קישורי הפוטר והלוגין · ‏(5) הוסר מספר-הקצאה המפוברק; עוסק פטור חסום מחשבונית-מס · ‏(6) תיקוני /setup: שחזור שנה, ביטול פברוק 12% ב"ל, קופי אחסון אמין, הבהרת ב"ל/בריאות · ‏(7) ‏src/lib/ai/models.ts + לוג ‏[ai-usage] בכל 5 נקודות-הקריאה · ‏(8) ‏manifest ממותג + אייקוני PNG (נוצרו ב-chromium) + 404 עברי + ‏OG/viewport · ‏(9) ‏CONTRIBUTING-HE.md · ‏tsc נקי, ‏101/101, ‏build ירוק | בוצע 19/07 ערב; נדחף ל-`claude/system-beta-preparation-oiyzpy` |

## סשן 05/07/2026 — מעבר לעבודה עצמאית (Sonnet/Opus); החלטות ומשימות שבוצעו

> מהסשן הזה והלאה: **כל משימה בכל סשן מתועדת כאן באותו סשן** — זה מה שמאפשר ליוני להמשיך ברצף
> בכל מודל. פורמט: שורה בטבלה, עם סטטוס (סופי/revisitable) והפניה לתוצר.

| החלטה / משימה | ערך | סטטוס + תוצר |
|---|---|---|
| תוכנית 7 ימים 05/07→12/07 | משימות אטומיות עם בעלים+DoD; גייטינג→ג'מיני/תשתית→UX→שער-מורכבות→MCP v0→FLAG(Roy) | בוצע — תדריך v4 ב-[[STATUS]]; סדר הימים revisitable ע"י יוני |
| מיפוי שבריריות המנוע | סריקה מרובת-סוכנים + אימות אדוורסרי מול הקוד; false-positive vectors מדורגים | בוצע — `memory/risk-gap.md`; ‏16 מאומתים, 29 ממתינים לאימות (מגבלת תקציב), 4 הופרכו (מהם 2 עם ציטוטים מפוברקים — מאשש את מדיניות האימות); לתחזק בכל שינוי מנוע |
| תדרוך מומחים חיצוניים | ‏stack + ‏8 דילמות אסטרטגיות + 5 משפטיות + שאלות לפגישה | בוצע — `docs/consultation-prep.md`; טיוטה עד עין של יוני/רועי |
| **הכרעת ה-MCP: אופציה א'** | ‏MCP server של countme שעוטף את המנוע (5 כלים read-only, ‏v0 ‏stdio ללא PII) | הוחלט, **revisitable ע"י יוני** — ‏spec: `docs/specs/countme-mcp-server.md`; ‏v1 remote רק אחרי גייטינג+PII |
| ‏PR #28 (tax-rules) | ‏main מוזג לתוך הענף; קונפליקטי memory/ הוכרעו לטובת main; ‏101/101 + build לפני push | בוצע — ממתין ל-CI+מיזוג יוני |
| אימות גייטינג מרחוק | ‏Vercel MCP מסשני-web = החשבון הנכון; בדיקת `/dashboard` חיה היא ה-DoD, לא הצהרות | תקף מעתה — הגייטינג אומת ככבוי 05/07 15:24 UTC |
| הפרויקט הכפול | הריפו בונה לשני פרויקטי Vercel (‏`prj_3IlH…` הקנוני, ‏`prj_mz1u…` בחשבון `countmes-projects`) | עובדה מאומתת (2 תגובות bot על #28); ניתוק = משימת יוני 07/07 |
| לינק Gemini | ‏share.gemini.google חסום מהקונטיינר (403 גם ב-proxy) — אין טעם לנסות שוב מסשן web | נעול טכנית — יוני מדביק טקסט; משבצת 07/07 שמורה |

### המשך-סשן 14/07 (אחרי שבוע ללא פעילות; באותו סשן Fable)

| החלטה / משימה | ערך | סטטוס + תוצר |
|---|---|---|
| סבב אימות ידני משלים | ‏11 ממצאי-★ מ-risk-gap §3 אומתו בקריאת קוד ישירה (בלי סוכני-משנה — מגבלת תקציב) | בוצע — ‏27/‏18/‏4 (מאומת/ממתין/הופרך); עודכן ב-`risk-gap.md` + ‏PR #29 |
| ממצא דחוף חדש-מאומת | פרומפטי איתן מפרים "LLM לא מחשב": הוראת חישוב תרומות X×0.35 + תקרת זעיר קשיחה 120,000 ₪ + חישוב משרד-ביתי | תיקון תוזמן כמשימה 6 ביום-ג' של התוכנית; עד אז ידוע-ולא-מתוקן |
| הזזת לוח התוכנית | שבוע 06–13/07 עבר ללא ביצוע — התוכנית נשארת, "יום ב'" = יום החזרה של יוני | עודכן ב-STATUS; ‏#28/#29 עדיין ממתינים למיזוג |

### המשך-סשן 15/07 ערב — מיזוג PR #28 + #29 (הרשאה מפורשת מיוני)

| החלטה / משימה | ערך | סטטוס + תוצר |
|---|---|---|
| מיזוג ‏#28+#29 ל-main | יוני אישר במפורש: "יכול להטמיע... תעשה bypass באישורי אם צריך" — הרשאה ישירה למזג, כולל bypass על אישורים/הגנת-ענף אם דרוש | בוצע — נבדקו קונפליקטים (0) + CI (ירוק) לפני מיזוג; לא נדרש bypass בפועל (הרשאות ה-token הספיקו); סדר: מנוע (#28) לפני תיעוד (#29) |
| אימות פוסט-מיזוג | ‏main מקומי מעודכן, טסטים+build ירוקים, פרודקשן ב-Vercel אומת כ-READY לשני ה-merge commits | תקן חדש: כל merge-בהרשאה חייב אימות build+test+deploy מיד אחרי, לא רק לפני |

### המשך-סשן 15/07 (מעבר בפועל ל-Sonnet 5; אותו סשן ממשיך)

| החלטה / משימה | ערך | סטטוס + תוצר |
|---|---|---|
| מודל הסשן | עבר מ-Fable ל-**Sonnet 5** (‏`/model claude-sonnet-5`) — זהו המעבר לעבודה עצמאית שתוכנן ב-05/07 | בפועל מעכשיו; הזיכרון (STATUS/decisions/progress) הוא שכבת הרציפות היחידה בין המודלים |
| סבב אימות מלא ל-risk-gap | ‏15 סוכנים אדוורסריים במקביל (Workflow), אחרי שהתברר שמגבלת התקציב החודשית התאוששה — אפס כשלים, אפס agents_error | בוצע — `risk-gap.md`: ‏35 מאומתים / 0 ממתינים / 6 הופרכו (2 הפרכות עם ניסוח-מתוקן שנוסף כממצא חדש) |
| ממצא חדש בחומרה הגבוהה ביותר בסבב | `estimateTaxLiability` לא יורש דגלי אי-ודאות מ-field044/field068 — `TaxEstimate` type חסר שדה confidence/assumptions לגמרי | תועד כממצא #24 + משימה 7 חדשה בתוכנית (הוספת `assumptions: string[]`) |
| דפוס נוסף שהתגלה | hints בסכימה (`schema.ts`) הם מחרוזות חופשיות לא-מקושרות ל-types.ts — 3 מקרים ידועים כבר לא-מסונכרנים (048/072/045-046), לא מקרה בודד | תועד כממצא #25 + משימה 8; מציע exhaustiveness test או hints דינמיים |
| לקח מתודולוגי מאושש | 2 מהממצאים ש"אושרו" בסבבים קודמים (form-footer-legal-note, threshold-6111-no-boundary-test) התבררו כמדויקים-חלקית באימות עמוק יותר — לא רק "True/False" בינארי | מעודכן ב-`risk-gap.md` §4; מחזק את מדיניות "אימות תמידי" מ-03/07 גם לגבי ממצאים שכבר אומתו |

## מדיניות ניהול סשנים (נעול 03/07/2026)

| החלטה | ערך | הנמקה |
|---|---|---|
| פתיחת סשן | ‏merge של memory-PR קודם → פרומפט קבוע: קרא STATUS, אשר הבנה ב-2 שורות, gaps בסוף כל פלט | ‏main מוגן והסשן החדש משכפל אותו — תדריך שלא מוזג לא קיים עבורו |
| ענף | ‏`claude/<topic>` אחד לסשן; אחרי merge — הענף מתאתחל מ-main | היסטוריה נקייה, ‏CI על כל כניסה ל-main |
| סשנים מקבילים | קבצים זרים בלבד; נגיעה ב-`lib/calculators`/`types.ts` מחייבת **ביקורת-צולבת על הקוד** (worktree + הרצת טסטים) לפני merge | מנע פינג-פונג "תיקונים" בין סשנים; עבד ב-03/07 (ענף tax-rules אומת ב-5 דק') |
| דיווחי-AI | שום דיווח (מכל מודל, כולל שלי) אינו אמת בלי אימות מול הריפו/מקור ראשי | פברוק ג׳מיני (פיצ'רים שלא קיימים) + 4 מספרי-מס שגויים "מאומתים" |
| "CONFIRMED" על קבוע-מס | רק מקור ראשי או ≥2 בלתי-תלויים, עם תאריך-תוקף (שיעורים משתנים באמצע שנה!) ורמת סבירות | ‏7.12% היה "מאושר" ממקור מסכם מיושן (תיקון 252 מפברואר 2025) |
| סודות | לעולם לא בצ'אט — רק password manager / שדות env של הקונפיג. הודבק בטעות → ‏revoke מיידי | תקרית הטוקן 02/07 (בוטל תוך דקות) |
| סגירת סשן | ‏wrap-up מלא (STATUS/progress/decisions/retro) + ‏PR ל-main; ‏retro פר-סשן-גדול | רציפות בין סשנים בלי אובדן ידע |

## ארכיטקטורה + אבטחה (נעול 02/07/2026, gate-approved)

| החלטה | ערך | הנמקה |
|---|---|---|
| חישובי מס | **דטרמיניסטי בלבד + golden tests חובה** | LLM לעולם לא מחשב — רק מסביר/אוסף. כל שינוי קבוע-מס חייב לעדכן golden test באותו commit. `npm test` לפני push |
| קבועי-מס | **רק** ב-`lib/calculators/types.ts` (year-keyed) | אפס literals בקומפוננטות/פרומפטים; תוקנו תרומות 207+תקרת 30%, ביטוח חיים 25% (מקורות בקוד) |
| auth ל-API | דגל **אחד** — `AUTH_GATING_ENABLED` סוגר דפים+API יחד | בלי דגל שני; הבטא נשארת פתוחה עד ההדלקה של יוני |
| rate limiting | in-memory מוקשח עכשיו; durable = Vercel WAF ואז Supabase-counter | לא מוסיפים dependency בלי לעדכן CLAUDE.md (מוסכמה 7) |
| next | patch-upgrades אבטחה מיידיים (16.2.10) | advisories של middleware bypass רלוונטיים ישירות ל-gating |
| מיצוב קופי | **"מחשבון מדויק. לא ייעוץ מס."** — `<LegalNote>` יחיד | לא overclaim ("מחליף רו"ח") ולא over-hedge ("הערכה" על חישוב מדויק). כל קופי משפטי DRAFT עד יעל |
| סוכן רגולציה | מציע patch כ-diff באישיו; **לעולם לא ממזג לבד** | human-in-the-loop על כל שינוי קבוע; auto-PR רק אחרי אישור מפורש (הרשאות workflow) |
| סקילים | סקיל = קלט, לא סמכות | הסקיל עצמו הכיל סולם מילואים שגוי; כל מספר מסקיל מוצלב מול מקור ראשי לפני שימוש |

## Brand Kit (נעול 03/06/2026)

| החלטה | ערך | הנמקה |
|---|---|---|
| פונט | **Assistant** בלבד (משתנה `--font-assistant`) | מחליף Heebo/Rubik — פונט יחיד לכל גופות האפליקציה, Hebrew+Latin, Google Fonts. סופי. |
| פלטת צבעים | נייבי `#083A4F` / בז' `#C8B59A` / טיל `#407E8C` | מחליף את צבעי amber הישנים. טוקנים ב-`globals.css`, לא לשנות ישירות בקומפוננטות. סופי. |
| Brand primitives | `src/components/brand/{logo,button,icons,status}.tsx` | מקור-אמת לכל UI מחוץ ל-`/demo`. אסור לכתוב כפתור/אייקון ישירות — רק דרך הפרימיטיבים. |
| `/demo` exempt | הפורם ב-`/demo` לא עבר ריברנד | gov.il faithful — החלטה נעולה מהתחלה. כל `gov-*` class בפורם — לא לגעת. |
| emoji = אסור | ללא emoji בכל האפליקציה | Brand Kit README אומר מפורשות "No emoji". רק אייקוני קו מ-`icons.tsx`. |
| Brand Kit מיקום | `Brand Kit/README.md` | קנוני לכל AI session עתידי — לקרוא לפני עבודת UI. |

## מודל שנות-המס (נעול 31/05/2026)

| החלטה | ערך | הנמקה |
|---|---|---|
| ברירת-מחדל לתצוגה | **2024** (`DEFAULT_VIEW_YEAR`) | יציבות לדמו EY — `/demo` ו-`/file` נוחתים על שנה מוכרת |
| שנה פתוחה להגשה | **2025** (`ACTIVE_FILING_YEAR`) | הדו"ח שמגישים *עכשיו* (היום 2026) הוא לשנת 2025 |
| מודל סטטוס | filed / open / future | 2024 הוגש (קריאה בלבד) · 2025 פתוח · 2026 עתידי/בצבירה |
| נתוני דמו ל-2025 | מיחזור 2024 (אותם 248,500/47,800) | להראות שנה פעילה מאוכלסת בלי נתונים חדשים |
| `TAX_YEAR_2026` | מהסקיל `israeli-tax-returns`, עם `TODO(Roy)` | מדרגות 2025 *שגויות* ל-2026 (חוק ההתייעלות) — עדיף ערך-סקיל מסומן מאשר ערך-שגוי שקט |

**⚠️ סתירה פתוחה שהתגלתה 18/08/2026 (audit) — טרם הוכרעה:** ההחלטה למטה ("ברירת-מחדל שנת-מס: 2025",
נעולה 10/06) לא עודכנה מאז ש-`/setup` שונה (commit `5b54464`, 17/08) לברירת-מחדל של **השנה הקלנדרית
הנוכחית** במקום 2025 קבוע — בלי לסמן את ההחלטה הזו כ"נס"וגה". זו לא טעות בקוד: יש כאן שתי שאלות שונות
שמתנגשות כי `income.year` משרת את שתיהן היום:
1. **"איזו שנה אני מגיש/ה עכשיו?"** — עצמאי בישראל מגיש בשנה N את הדוח השנתי של שנה N-1 (באוגוסט 2026
   עדיין מגישים 2025). זה מה שההחלטה המקורית תיארה.
2. **"כמה הרווחתי השנה עד עכשיו?"** — מודל ה-YTD בדשבורד הקל (נעול 16/08, ראו למטה) מניח שה"שנה" היא
   השנה הקלנדרית הרצה, וש-`totalRevenue` גדל בזמן אמת עם כל מסמך חדש.
תקלה בשטח (18/08): פרסונה שנוצרה בחלון שבו ברירת המחדל הייתה 2025-קבוע נשארת נעולה שם *לצמיתות* —
הכנסה אמיתית מאוגוסט 2026 ממשיכה להצטבר נגד תקרת ה-2025 (120,000) במקום תקרת 2026 (122,833), בלי
שהמשתמש/ת יודע/ת על איזו שנה הוא/היא מסתכל/ת (ראו `risk-gap.md` #7). **תוקן חלקית 18/08:** כל התראת
תקרה ותצוגת דשבורד/צ'אט מציגות עכשיו את `income.year` במפורש. **עדיין לא הוכרע:** האם `income.year`
צריך להתפצל לשני שדות (שנת-הגשה נפרדת משנת-מעקב), ומתג שנה נגיש למשתמש (`ARCHITECTURE.md`/משימת
הסשן #16) — לא לפתור חד-צדדית, זו החלטת מוצר.

**עדכון 19/08 (סבב-2) — הסתירה עכשיו גם ברמת קוד, לא רק ברמת-מוצר:** `YearSwitch` קיבל פרופ
`persist` (`src/components/dashboard/year-switch.tsx`). הדשבורד הרגיל (`/dashboard`) משתמש
ב-`persist={false}` — בחירת-שנה שם היא פילטר-תצוגה בלבד, `persona.income.year` האמיתי לא זז.
`/dashboard/pro` ממשיך עם `persist={true}` (ברירת-המחדל) — שם בחירת-שנה **כן** משנה את השנה
המוצהרת ונשמרת. שני העמודים חיים היום עם התנהגות מכוונת-הפוכה במפורש (תועד בהערת-קוד בשני
הקבצים) — זו לא טעות, זו דחיית ההכרעה עד שהשאלה למעלה תיפתר. **כשההכרעה תתקבל: לאחד את שני
הדשבורדים לאותו `persist` value, לא להשאיר את הפיצול.**

## פיילוט SaaS חי (נעול 10/06/2026)

| החלטה | ערך | הנמקה |
|---|---|---|
| Backend | **Supabase פרויקט `hbsgzelipeawkvtcazdr`** (לא akfg) | החשבון של יוני; הסכמה נבנתה שם מחדש. גובר על "Supabase יום 2+" — **חי עכשיו** |
| אחסון persona | `profiles.persona jsonb` (+ טבלאות מנורמליות) | lift מהיר ללא אובדן; הטבלאות לפיצ'רים טרנזקציוניים |
| auth | Google OAuth, **flag-gated** (`AUTH_GATING_ENABLED`) | gating כבוי כברירת-מחדל עד הדלקה ידנית — לא נועל את הדמו בטעות |
| סוג עוסק | **יחידים בלבד**: זעיר/פטור/מורשה (בלי חברה) | 1301 ליחידים; חברה=1214. אין "עוסק מורשה זעיר" |
| מיצוב | **"עובדות, לא עצות"** | הוסרו המלצות (חשיפה משפטית); איתן נוטרל |
| ברירת-מחדל שנת-מס | **2025** | מגישים עכשיו את 2025. גובר על `DEFAULT_VIEW_YEAR=2024` |
| מילואים | מדרגות **30/40/50** ימים (2025–2027) | תיקון 283; 20/45/60 = 2028+ (אומת audit+Gemini) |
| localStorage | **נשאר cache** + DB-authoritative (owner-stamp) | refactor DB-only שובר דמו אנונימי+~13 קוראים → דחוי |
| SDK | supabase-js **2.108** / ssr **0.12** | נדרש לטייפים המודרניים |

## עקרונות עבודה

- **לא משנים מספרי מס בשקט.** כל ערך שנתי-משתנה שאינו ודאי → `TODO(Roy)` עד אישור מול המקור
  הרשמי. הסקילים ה-`israeli-*` הם סמכות-הדומיין; לא להסתמך על ידע training לסכומים שמתעדכנים שנתית.
- **single-source לשנים:** כל rate/cap/rule זורם מ-`lib/calculators/types.ts` → אסור לקודד ערך
  בקומפוננטה/מחרוזת/דוח.
- **worktrees לאצוות עצמאיות בלבד.** פיצ'ר מצומד (קבועים→פרסונה→schema→UI) נעשה במסלול יחיד —
  worktrees מקבילים עליו רק ייצרו קונפליקטים.
- **חתימת commit:** נכשלת בתוך worktrees (שרת החתימה דורש את ה-checkout הראשי) → סוגרים קומיטים
  מהמאגר הראשי (cherry-pick), או `--no-gpg-sign` ב-worktree ואז cherry-pick.

## החלטות תיעוד (31/05/2026)

- **זיכרון הפרויקט = `memory/`** עם 4 מסמכים: `readme` · `status` · `progress` · `decisions`.
- **`HANDOFF.md` נמחק** — הוחלף ע"י `memory/STATUS.md` + `memory/progress.md`.
- **`CLAUDE.md`** נשאר ייעודי ל-Claude Code (הקשר ארכיטקטוני), לא לסטטוס-סשן.
- **טראקר המשימות** (`docs/meeting-records/yoni-tasks-27032026.md`) נשאר מקור-אמת ל-11 המשימות.

## RAG — ביקורת 18/08/2026: לא קיים, ואין החלטה מתועדת שאישרה אותו

בדיקה שלמה (grep על `pgvector|embedding|match_documents|rag|vector` בכל `src/`, `supabase/migrations/`,
ו-`memory/*.md`) לא מצאה שום מימוש retrieval, ושום שורת החלטה שמאשרת "light RAG: Supabase pgvector +
Claude" — למרות שזה הוצג בשיחה כ"ההחלטה המאושרת". שקל (`/coach`, `/api/coach`) וה-demo chat
(`/api/chat`) עונים היום אך ורק דרך: (א) system prompt קבוע, (ב) 4 כלים דטרמיניסטיים שעוטפים את
`lib/calculators`/`lib/deadlines`/`lib/alerts/ceiling` — אין שום אינדוקס/שליפה של טקסט (סקילים,
מסמכים, נהלים). לכל שאלה מחוץ ל-8 השדות-כוכב + 4 הכלים — אין קרקוע, רק ה-guardrail הרך "אל תמציא".

**מה כן נעשה 18/08:** חוזק ה-guardrail בשני המקומות (`lib/agent/tools.ts`, `src/app/api/coach/route.ts`)
להנחיה מפורשת של סירוב-ולא-ניחוש מחוץ לכיסוי הכלים, ונוספה הערת "לא ייעוץ מס" ל-`/api/chat` (היחיד
שהיה חסר לגמרי אחת).

**מה לא נעשה, ולמה:** לא הותקנה שכבת RAG. בניית pgvector אמיתית דורשת ספק embeddings חדש (Anthropic
אין לו endpoint embeddings; Voyage AI / OpenAI הן האופציות המעשיות) — זו החלטת ספק+תלות חדשה
(עלות, וגם כלל העבודה ב-CLAUDE.md נגד הוספת תלות בלי לתעד קודם) שלא שלי לקחת חד-צדדית. סקיצת מימוש
(סכימה + ingestion + retrieval היברידי + rerank) קיימת ותועדה בפלט הביקורת — ממתינה להחלטת יוני/רוי
אם ולאיזה ספק להתחייב, לפני שנבנה.

## רקע (מ-CLAUDE.md, לא לפתוח מחדש)

Stack: Next.js 16 + React 19 + TS + Tailwind 4 + Anthropic SDK · Vercel · Supabase (יום 2+) ·
מודל AI ברירת-מחדל `claude-sonnet-4-6` · עברית RTL בלבד · הטופס = רפרנס ויזואלי (לא rebuild 1:1) ·
פרסונה כ-JSON יחיד ב-`personas/dana-cohen.json`.
