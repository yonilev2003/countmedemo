# איך תורמים ל-countme בלי לשבור כלום — מדריך לתומי, לרועי, ולכל מי שלא מתכנת

> המדריך הזה נכתב למי שעובד עם סשן AI (‏Claude) ולא כותב קוד בעצמו.
> אם עושים רק את מה שכתוב כאן — אי אפשר לגרום נזק אמיתי. באמת.

## הרעיון בגדול

הריפו בנוי כמו מסעדה: יש מטבח (הענף הראשי — `main`) שממנו מוגש האתר האמיתי לכולם,
ויש עמדות עבודה אישיות (ענפים — branches) שבהן מבשלים בשקט. אף אחד לא מבשל
ישירות במטבח. מבשלים בעמדה שלך, מזמינים את יוני לטעום (Pull Request), ורק
אחרי אישור זה נכנס לתפריט.

## המסלול הזהוב (כל פעם מחדש, בדיוק בסדר הזה)

1. **פותחים סשן AI חדש** על הריפו `yonilev2003/countmedemo`.
2. **מבקשים מה-AI לפתוח ענף חדש** עם שם בפורמט: `claude/<שם שלך>-<נושא קצר>`
   (למשל `claude/tomi-onboarding-copy`). אומרים לו במפורש:
   *"תעבוד על ענף חדש בשם כזה-וכזה, אל תיגע ב-main"*.
3. **מסבירים מה רוצים** בשפה חופשית. עדיף משימה אחת ממוקדת לסשן, לא חמש.
4. לפני סיום מבקשים מה-AI: *"תריץ `npm run build && npm test` ותוודא שהכל ירוק"*.
   אם משהו אדום — מבקשים ממנו לתקן לפני שממשיכים.
5. **מבקשים מה-AI לדחוף (push) ולפתוח Pull Request** עם תיאור של עד 3 נקודות:
   מה השתנה, למה, ומה כדאי לבדוק.
6. **מחכים.** ה-CI (בדיקות אוטומטיות) ירוץ לבד, ויוני יעבור על השינוי וימזג.
   לא ממזגים לבד — גם אם יש כפתור ירוק מפתה.

## איך רואים את התוצאה לפני שממזגים

על כל Pull Request עולה **Preview** — גרסה חיה של האתר עם השינוי שלך.
בעמוד ה-PR בגיטהאב מחפשים את התגובה של `vercel[bot]` ולוחצים על הלינק Visit Preview.

> שימו לב: כרגע עשויות להופיע **שתי** תגובות bot (יש פרויקט Vercel כפול ישן).
> הלינק הקובע הוא של הפרויקט `countmedemo` תחת `yonilev2003s-projects`.
> אם מופיעה רק אחת — היא הנכונה.

בודקים את ה-Preview **בטלפון**, לא רק במחשב — הקהל שלנו חי בטלפון.

## רשימת האסור (זה כל מה שצריך לזכור)

- **אסור להדביק סודות בצ'אט עם ה-AI.** אף פעם. לא מפתח API, לא סיסמה, לא
  טוקן. אם הודבק בטעות — מספרים ליוני מיד (מחליפים את המפתח, לא נורא, אבל מיד).
- **אסור לגעת ב-`src/lib/calculators/`** או בכל מספר של מס (מדרגות, תקרות,
  אחוזים) בלי אישור מפורש מיוני + רועי. כל מספר כזה מחויב בבדיקה כפולה.
- **אסור לערוך את תיקיית `memory/`** — היא מתעדכנת רק בסגירת סשן מסודרת.
- **אסור להוסיף ספריות חדשות** (dependencies) — אם ה-AI מציע להתקין משהו חדש,
  עוצרים ושואלים את יוני.
- **אסור למזג לבד** את ה-PR של עצמך.
- כמובן: לא עובדים ישירות על `main`, אף פעם.

## הצ'קליסט של יוני למיזוג (פחות מ-10 דקות)

1. **CI ירוק?** שני הצ'קים (`build-and-unit`, `e2e`) עברו. אדום = לא ממזגים.
2. **Preview נבדק?** לחיצה על הלינק, מעבר על המסכים שהשתנו — בטלפון.
3. **סריקת diff מהירה** (לשונית Files changed) — מחפשים שלושה דגלים אדומים:
   - קבצים שנוגעים ב-`lib/calculators/` או במספרי מס → עצירה, בדיקה עם רועי.
   - שינוי ב-`package.json` (ספרייה חדשה) → עצירה, למה זה שם?
   - מחרוזות שנראות כמו סוד (מפתח, טוקן) → עצירה מיידית + החלפת המפתח.
4. **מיזוג:** כפתור **Squash and merge** (תמיד squash — היסטוריה נקייה),
   מחיקת הענף כשמוצע, ואחרי זה `git pull` על main בסשן המקומי אם רלוונטי.
5. אם יש קונפליקט (GitHub יגיד "This branch has conflicts") — לא נלחמים ידנית:
   מבקשים מסשן AI *"תמזג את main לתוך הענף ותפתור את הקונפליקטים"* ובודקים שוב.

## מה עושים כשמשהו נשבר באתר החי

1. לא נכנסים לפאניקה — יש כפתור החזרה לאחור.
2. מספרים ליוני. הוא נכנס ל-Vercel → Deployments → הפריסה הקודמת שעבדה →
   **Promote to Production**. האתר חוזר לגרסה הקודמת תוך דקות.
3. את התיקון עצמו עושים אחר-כך בנחת, בענף חדש, במסלול הזהוב הרגיל.

## חלוקת עבודה לספרינט הנוכחי (בטא 23/07)

| מי | על מה | איפה זה מוגדר |
|---|---|---|
| יוני | פיתוח (סשני AI), מיזוגים, משימות ידניות (Vercel/Supabase) | `docs/plans/2026-07-19-beta-sprint.md` |
| תומי | קופי השאלון, מסמך הטון של איתן, נוסחי תזכורות, בדיקת QA בטלפון | `docs/plans/2026-07-19-team-brief.md` |
| רועי | אימות רגולטורי: מסמכים, בסיס-הידע של איתן, שאלת ביטוח לאומי | `docs/plans/2026-07-19-team-brief.md` |

שאלות? כותבים בקבוצה או שואלים את הסשן — הוא מכיר את הריפו טוב מכולנו.
