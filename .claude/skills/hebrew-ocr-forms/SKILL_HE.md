# OCR לטפסים בעברית

## הנחיות

### שלב 1: לזהות את סוג הטופס
| סוג טופס | מקור | מזהים עיקריים | שדות נפוצים |
|----------|------|---------------|-------------|
| נסח טאבו | לשכת רישום מקרקעין | "נסח טאבו", "לשכת רישום המקרקעין" | גוש, חלקה, בעלים, שעבודים |
| טופס 106 | רשות המסים | "טופס 106", "דו"ח שנתי למעביד" | שכר, מס, מעביד |
| אישור ניכוי | רשות המסים | "אישור ניכוי מס במקור" | שיעור מס, תוקף, ת.ז. |
| טופס 857 | רשות המסים | "טופס 857", "רווח הון" | עסקה, רווח, מס |
| אישור זכאויות | ביטוח לאומי | "אישור זכאויות", "ביטוח לאומי" | סוג גמלה, סכום |
| טופס 100 | ביטוח לאומי | "טופס 100", "דין וחשבון" | עובדים, שכר |
| רישיון רכב | רישוי רכב | "רישיון רכב" | לוחית, בעלים, תוקף |

### שלב 2: עיבוד מקדים של התמונה הסרוקה

תסתכלו על `scripts/preprocess_image.py` לפייפליין העיבוד המקדים המלא. השלבים העיקריים:
1. המרה לגווני אפור
2. יישור - טפסים ישראליים לפעמים נסרקים בסיבוב קל
3. בינאריזציה עם סף מסתגל - מטפל בתאורה לא אחידה מסורקים
4. הסרת רעש עם פעולות מורפולוגיות

### שלב 3: הרצת OCR בעברית עם Tesseract

תסתכלו על `scripts/extract_form_fields.py` לפייפליין החילוץ המלא.

**הגדרות Tesseract לטפסים בעברית:**
```python
config = (
    '--oem 1 '          # LSTM neural net (best for Hebrew)
    '--psm 6 '          # Assume uniform block of text
    '-l heb+eng '       # Hebrew + English (forms have both)
    '-c preserve_interword_spaces=1'  # Keep spacing for field alignment
)
```

- לטפסים טבלאיים (טאבו, טופס 106) תשתמשו ב-PSM 4 במקום PSM 6
- תמיד במצב LSTM (--oem 1) לדיוק הטוב ביותר בעברית
- תכללו גם heb וגם eng כי טפסים מכילים גם עברית וגם אנגלית/מספרים

### שלב 4: חילוץ שדות לפי סוג הטופס

**נסח טאבו - שדות עיקריים:**
- מספר גוש: מחפשים "גוש" ואחריו ספרות
- מספר חלקה: מחפשים "חלקה" ואחריו ספרות
- שם בעלים: אחרי "בעלים" או "שם הבעלים"
- מספר זהות (ת.ז.): אחרי "ת.ז." או "מספר זהות", 9 ספרות

**טופס מס (טופס 106) - שדות עיקריים:**
- שנת מס: אחרי "שנת מס"
- מספר מעביד: אחרי "מספר מעביד" או "מס׳ עוסק", 9 ספרות
- שכר ברוטו: אחרי "שכר ברוטו" או "הכנסה חייבת"
- מס שנוכה: אחרי "מס שנוכה" או "ניכוי מס"

### שלב 5: טיפול ב-RTL וטקסט דו-כיווני

```python
import unicodedata

def normalize_bidi_text(text):
    """Normalize bidirectional text from Hebrew OCR output."""
    lines = text.split('\n')
    normalized = []
    for line in lines:
        # Strip bidi control characters
        clean = ''.join(
            c for c in line
            if unicodedata.category(c) != 'Cf'
        )
        clean = ' '.join(clean.split())
        if clean:
            normalized.append(clean)
    return '\n'.join(normalized)
```

הסירו ניקוד לפני התאמת מחרוזות מדויקת. הפונקציה `normalize_bidi_text()` מסירה רק תווים מקטגוריית Unicode מסוג `Cf` (פורמט). ניקוד עברי (סימני תנועה, U+0591 עד U+05C7) שייך לקטגוריה `Mn` (סימן לא-רווח), אז הוא שורד את הסינון הזה. אם טופס סרוק מכיל במקרה טקסט מנוקד, או שמנוע ה-OCR ממציא סימנים תועים, התאמת מחרוזת מדויקת או regex של מילת מפתח כמו `"גוש"` תיכשל בשקט מול `"גּוּשׁ"`. כדאי להסיר ניקוד במפורש לפני התאמת מילות מפתח:

```python
import re

NIKUD_RE = re.compile(r'[֑-ׇ]')

def strip_nikud(text):
    """Remove Hebrew diacritics so keyword regexes match reliably."""
    return NIKUD_RE.sub('', text)
```

נרמלו אותיות סופיות בהתאמה. בעברית יש חמש אותיות בצורה סופית: ך ם ן ף ץ. אלה אותן אותיות כמו כ מ נ פ צ אבל מקודדות כנקודות קוד נפרדות. בדרך כלל OCR שומר אותן נכון, אבל לוגיקת התאמת מילות מפתח והתאמה מקורבת עלולה להישבר: תווית שזוהתה עם צורה סופית לא תהיה שווה למונח חיפוש שנכתב עם הצורה הרגילה, ולהפך. כשמתאימים תוויות שדה, נרמלו את שני הצדדים לצורה אחת קודם:

```python
SOFIT_MAP = {'ך': 'כ', 'ם': 'מ', 'ן': 'נ', 'ף': 'פ', 'ץ': 'צ'}

def normalize_sofit(text):
    """Fold final-form letters to their medial forms for matching."""
    return ''.join(SOFIT_MAP.get(ch, ch) for ch in text)
```

הפעילו את `strip_nikud()` ואת `normalize_sofit()` רק על מפתח ההתאמה, אף פעם לא על הערך שאתם שומרים או מציגים, האותיות הסופיות והניקוד הם חלק מעברית תקינה וחייבים להישמר בפלט.

### שלב 6: אימות הנתונים שחולצו

אחרי החילוץ, תאמתו את שדות המפתח:
- **מספרי ת.ז.:** תריצו אלגוריתם אימות ת.ז. ישראלית (ספרת ביקורת)
- **תאריכים:** תאמתו פורמט DD/MM/YYYY וטווחי תאריכים תקינים
- **סכומים:** תבדקו שהשדות הנומריים פוענחו נכון, בלי ארטיפקטים של OCR בספרות
- **גוש/חלקה:** תאמתו פורמט מספרי וטווחים הגיוניים
- **הצלבה:** אם ת.ז. מופיעה בכמה שדות, תוודאו שהיא עקבית

## דוגמאות

### דוגמה 1: עיבוד נסח טאבו
המשתמש אומר: "חלץ נתונים ממסמך טאבו סרוק"
תוצאה: עיבוד מקדים של התמונה, הרצת OCR בעברית, זיהוי שזה נסח טאבו, חילוץ שדות גוש/חלקה/בעלים/זכויות, אימות ת.ז., החזרת JSON מובנה.

### דוגמה 2: עיבוד אצוות של טפסי מס
המשתמש אומר: "יש לי 50 טפסי 106 סרוקים - חלץ נתוני שכר ומס"
תוצאה: מקימים פייפליין אצווה - עיבוד מקדים לכל תמונה, OCR בעברית+אנגלית, חילוץ שדות טופס 106, אימות, פלט ל-CSV/JSON עם ציוני ביטחון.

### דוגמה 3: בעיות איכות OCR
המשתמש אומר: "ה-OCR לא קורא את הטקסט בעברית נכון"
תוצאה: אבחון של העיבוד המקדים - בודקים רזולוציית תמונה (מומלץ מינימום 300 DPI), מאמתים יישור, מתאימים סף בינאריזציה, מנסים מצבי PSM שונים, בודקים שחבילת השפה העברית מותקנת.

## משאבים מצורפים

### סקריפטים
- `scripts/preprocess_image.py` - הכנת תמונות של טפסים ישראליים סרוקים ל-OCR: המרה לגווני אפור, יישור סריקות מסובבות, בינאריזציה מסתגלת לתאורה לא אחידה, הסרת רעש מורפולוגית, שיפור ניגודיות CLAHE אופציונלי, והסרת שוליים. הרצה: `python scripts/preprocess_image.py --help`
- `scripts/extract_form_fields.py` - הרצת Tesseract OCR בעברית על תמונות טפסים מעובדות וחילוץ שדות מובנים לפי סוג הטופס. תומך בזיהוי אוטומטי של טאבו, טופס 106 וטפסי ממשלה ישראליים נוספים. פלט JSON עם השדות שחולצו ואימות ת.ז. ישראלית. הרצה: `python scripts/extract_form_fields.py --help`

### קובצי עזר
- `references/israeli-form-types.md` - קטלוג מפורט של סוגי טפסים ממשלתיים ישראליים (טאבו/מקרקעין, טפסי רשות המסים, מסמכי ביטוח לאומי) עם תיאורי שדות, ביטויי regex לחילוץ, כללי אימות ת.ז., פורמטי תאריך/מטבע, וטיפים ל-OCR לפי פריסת הטופס. תסתכלו בו כשאתם מזהים טופס לא מוכר או בונים לוגיקת חילוץ שדות למסמך מסוים.

## מנועי OCR אפשריים

המנוע `Tesseract` הוא בסיס חינמי טוב לדפוס עברי, אבל ממשקי Vision מודרניים בענן משתווים אליו או עדיפים עליו בסריקות רועשות ובטפסים חתומים. תבחרו לפי השימוש:

| מנוע | דפוס עברי | כתב יד | מודל עלות | מתי להשתמש |
|------|-----------|-------|-----------|-------------|
| Tesseract (heb+eng, LSTM) | טוב לסריקות נקיות ב-300 DPI+ | חלש | חינם, ריצה מקומית | ברירת מחדל לצינורות מקומיים, לנתונים רגישים לפרטיות |
| Google Cloud Vision – Document Text Detection | טוב מאוד, עמיד לרעש | חלקי (כתב יד דמוי דפוס בלבד) | תשלום לפי בקשה | סריקות באיכות מעורבת, אצווה גדולה, טפסי PDF |
| AWS Textract (AnalyzeDocument) | טוב; חזק יותר בטפסים וטבלאות | חלקי | תשלום לפי עמוד | טפסים עם שדות מובנים, חילוץ מפתח-ערך |
| Azure AI Vision – Read API | טוב מאוד, מודע לפריסה | חלקי | תשלום לפי טרנזקציה | סביבות Azure ארגוניות, PDF חתום |
| Claude Vision (claude-sonnet-4-6 / claude-opus-4-7) | טוב מאוד, מודע להקשר | טוב (עם הנחיה בפרומפט) | לפי טוקנים | פריסות טפסים חריגות, אימות צולב של שדות, כשרוצים שהמודל גם ינתח את הנתונים |

הערה: אף אחד מהמנועים אינו אמין לכתב יד קורסיבי על טפסים כמו נסחי טאבו ישנים. לאלה תסמנו לבדיקה ידנית במקום לנסות חילוץ אוטומטי.

## מלכודות נפוצות
- דיוק OCR בעברית יורד משמעותית בטקסט בכתב יד, במיוחד באותיות ו', ז' ו-י' שנראות דומות. תמיד תכללו ציוני ביטחון ותסמנו תווים עם ביטחון נמוך.
- טפסים ממשלתיים ישראליים סרוקים לפעמים מכילים טקסט מעורב עברית וערבית. סוכנים עלולים לזהות את החלקים הערביים כעברית או לדלג עליהם לגמרי. שתי השפות RTL אבל בטווחי Unicode שונים.
- מספרי תעודת זהות ומספרי טלפון שחולצו ב-OCR צריכים אימות עם אלגוריתמי ספרת ביקורת אחרי החילוץ, כי שגיאות OCR של ספרה בודדת נפוצות.
- הרבה טפסים ישראליים משתמשים בהדפסת מטריצת נקודות (dot-matrix) בעברית, שיש לה דיוק OCR נמוך יותר מטקסט מודפס בלייזר. סוכנים עלולים לדווח על ביטחון גבוה מדי במסמכים ממשלתיים ישנים.
- Tesseract עלול לשנות סדר של רצפי ספרות בתוך שורת RTL. מספר כמו ת.ז. בת 9 ספרות או סכום בחשבונית שמודפס בתוך משפט עברי עלול לחזור עם קבוצות הספרות מעורבבות, גם כשכל ספרה בנפרד זוהתה נכון. אל תסמכו על הסדר הגולמי משמאל לימין של מספרים שחולצו משורה מעורבת עברית/ספרות, עגנו כל מספר מחדש לתווית השדה שלו ואמתו מחדש עם כללי ספרת ביקורת או טווח.
- הסקריפטים המצורפים מקבלים רק קובץ תמונה בודד. הרבה טפסים ישראליים הם קובצי PDF רב-עמודיים (חבילת טופס 106, נסח טאבו רב-דפי). המירו כל עמוד PDF לתמונה קודם, למשל עם `pdf2image` (`convert_from_path`) או `pdftoppm`, ואז הריצו עיבוד מקדים ו-OCR לכל עמוד ומזגו את השדות שחולצו. אל תעבירו PDF גולמי ל-`preprocess_image.py` או ל-`extract_form_fields.py`.

## קישורי עזר

| מקור | כתובת | מה לבדוק |
|------|-------|----------|
| תיעוד Tesseract | https://tesseract-ocr.github.io/tessdoc/ | התקנה, חבילות שפה, מצב LSTM, ערכי PSM |
| נתוני אימון עברית ל-Tesseract | https://github.com/tesseract-ocr/tessdata | מודל עברית ל-tesseract |
| Google Cloud Vision – Documents | https://cloud.google.com/vision/docs/fulltext-annotations | תיעוד API לזיהוי טקסט במסמכים |
| AWS Textract – AnalyzeDocument | https://docs.aws.amazon.com/textract/latest/dg/how-it-works-analyzing.html | חילוץ טפסים וטבלאות |
| Azure AI Vision – Read API | https://learn.microsoft.com/en-us/azure/ai-services/computer-vision/overview-ocr | OCR רב-שפתי כולל עברית |
| מפרט ספרת ביקורת של ת.ז. | https://he.wikipedia.org/wiki/מספר_זהות_(ישראל) | אלגוריתם לאימות מספר זהות ישראלי בן 9 ספרות |

## פתרון בעיות

### שגיאה: "חבילת שפה עברית של Tesseract לא נמצאה"
סיבה: קובץ traineddata בעברית לא מותקן
פתרון: תתקינו עם `sudo apt-get install tesseract-ocr-heb` (Ubuntu) או `brew install tesseract-lang` (macOS). אימות עם `tesseract --list-langs`.

### שגיאה: "פלט ה-OCR משובש או הפוך"
סיבה: בעיה בסדר של טקסט דו-כיווני
פתרון: תשתמשו בפונקציה `normalize_bidi_text()`. תוודאו ש-Tesseract משתמש ב-`--oem 1` (מצב LSTM). תבדקו שהתמונה לא מראתית.

### שגיאה: "דיוק נמוך בחלקים מסוימים של הטופס"
סיבה: איכות סריקה ירודה, חותמות/חתימות חופפות לטקסט, רקעים צבעוניים
פתרון: תגבירו את העיבוד המקדים - הסרת רעש חזקה יותר, חיתוך לאזורים ספציפיים בטופס, חילוץ מבוסס ROI לפריסות טפסים מוכרות.
