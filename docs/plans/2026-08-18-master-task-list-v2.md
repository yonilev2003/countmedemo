# רשימת המשימות v2 — פתיחת הרשמה לכולם + מוכנות לסקייל (18/08/2026 לילה)

מחליפה את סדר-העדיפויות של `docs/feedback/2026-08-18-roy-beta-feedback-tasks.md` (שם נשאר
התיעוד המלא של סבב-רוי; מה שבוצע שם מסומן למטה). כל שורה כאן מקורקעת בקוד — המקורות
בדוח הסריקה שנערך הערב (rate-limit.ts, models.ts, oauth-branding.md, hbsgz-pending.sql ועוד).

## הכרעות יוני (18/08 לילה, AskUserQuestion) — נעולות

| החלטה | ערך |
|---|---|
| **הרשמה פתוחה לכולם** — בלי רשימת Test Users, בלי הגבלות | מחליף את מגבלת ה-100 של Testing mode |
| **חינמי עם מכסים קשיחים** | נשארים על Vercel Hobby + Supabase Free; תקרות שימוש קשיחות בקוד; מעבר לתשלום רק כשההתרעות מראות צמיחה אמיתית |
| **התרעות: מייל** | סיכום יומי + מייל מיידי בחציית סף |
| **אוטומציה: כל השלושה** | פעולות-הגנה אוטומטיות + בדיקות עומס + פריסות canary הדרגתיות |

---

## שלב 0 — אימותים דחופים (לפני הכול; רובם ידניים של יוני)

| # | משימה | למה זה ראשון | בעלים |
|---|---|---|---|
| 0.1 | **לאמת ב-hbsgz שקיימות `rate_limit_buckets` + פונקציית `check_rate_limit`** (SQL editor: `select * from pg_proc where proname='check_rate_limit'`) | `checkRateLimitDurable` **נכשל-פתוח** על כל שגיאה (`rate-limit.ts:162-166`). אם המיגרציה לא הודבקה — כל ההגנה העמידה כבויה בשקט והקוד נראה כאילו הבעיה פתורה. זה הסיכון השקט הכי גדול במערכת | יוני (קונסולה) |
| 0.2 | **לתקן את חיבור ה-Supabase MCP לחשבון של hbsgz** (רואה היום רק akfg/BlondeShell — `memory/decisions.md:62`) | כל עוד זה שבור, אף סשן AI לא יכול לאמת/להחיל כלום — הכול עובר בהדבקות ידניות. זה שורש רוב הבלגן בשלב 4 | יוני |
| 0.3 | **להפעיל את חוקי ה-WAF** לפי הראנבוק (`docs/runbooks/2026-07-02-yoni-supabase-waf.md` §2): 30/דקה ל-4 נתיבי ה-AI, 10/דקה ל-upload | ההגנה היחידה שדוחה תנועה **לפני** שלמבדה עולה, והיחידה ששורדת נפילת Supabase | יוני (Vercel) |
| 0.4 | Supabase Auth → Redirect URLs חייב לכלול `countmedemo-eight.vercel.app/auth/callback` (פתוח מ-12/08, `memory/STATUS.md:188-190`) | חצי מ"נרשמתי כמה פעמים" של רוי | יוני (קונסולה) |

## שלב 1 — פתיחת ההרשמה לכולם (המסלול הקריטי)

הקיר: Google מסרב להעביר ל-Production אפליקציה על דומיין משותף (`vercel.app`) — נדרש
דומיין אמיתי מאומת ב-Search Console (`docs/launch/oauth-branding.md`). אין דרך קוד לעקוף.

| # | משימה | בעלים |
|---|---|---|
| 1.1 | **לקנות דומיין** (countme.co.il / countme.ai — עשרות שקלים לשנה) ולחבר ל-Vercel (חינם) | יוני 💳 |
| 1.2 | לאמת את הדומיין ב-Google Search Console ולהגיש את מסך ההסכמה ל-**Production** (בלי לוגו בשלב זה — לוגו מפעיל Brand Verification קפדני) | יוני |
| 1.3 | קוד: לעדכן דומיין קנוני בכל המקומות — `layout.tsx`, `.env.template`, Supabase Site URL + Redirect URLs, Google Authorized redirect URI | AI |
| 1.4 | **ביניים עד שהדומיין חי:** נשארים ב-Testing; יוני מוסיף כל נרשם ידנית. ההתרעה על "מתקרבים ל-100" נכנסת לדייג'סט היומי (שלב 3) | — |
| 1.5 | ⚠️ בסיכון מודע: Vercel Hobby אסור לשימוש מסחרי (`memory/plan-pilot.md:386`). כל עוד הבטא חינמית לגמרי זה תפס — ברגע שגובים שקל, Pro חובה. נשאר ברשימת ההתרעות | תיעוד |

## שלב 2 — מגן עלויות ושימוש בקוד (AI מבצע; זה ה"מכסים קשיחים")

| # | משימה | קרקוע |
|---|---|---|
| 2.1 | **טבלת `ai_usage` + כתיבה מכל 6 נקודות ה-AI** במקום `console.log` בלבד (`models.ts:31` — היום אין שום צבירה). עמודות: user_id, route, model, input/output/cache tokens, עלות משוערת | הבסיס לכל ההתרעות והמכסים |
| 2.2 | **תקרה יומית פר-משתמש** (למשל 30 שיחות/יום, 20 העלאות/יום) — בדיקה זולה מול `ai_usage` אחרי ה-rate-limit; חריגה → 429 עם הודעה עברית ידידותית | פעולת-הגנה אוטומטית #1 |
| 2.3 | **תקרת-חירום גלובלית יומית** (₪/יום משוער): חצייה → degradation אוטומטי (צ'אט עובר Sonnet→Haiku, ואז השבתה זמנית של צ'אט/העלאות עם באנר "עומס — נסו מאוחר יותר") + מייל מיידי אליך | פעולת-הגנה #2; ההחלטה "חינמי עם מכסים" מחייבת את זה |
| 2.4 | **לקצץ את מכפיל-העלות של הצ'אט**: היסטוריה בשליטת הלקוח עד ~80K תווים לבקשה על Sonnet ×5 קריאות (`chat/route.ts:36-38,144,156`; מוערך ×7-15). לקשוח ל-8K תווים כולל, ולתכנן מעבר להיסטוריה צד-שרת (chat_messages כבר קיים) | הסיכון הכספי הכי גדול |
| 2.5 | **rate-limit לפי user ולא רק IP** ב-4 הנתיבים היקרים — resolveClientKey כבר תומך, אף נתיב לא מעביר (`memory/STATUS.md:112-114`); דורש סידור מחדש auth→limit או מנגנון דו-שלבי | משרד/NAT חולקים דלי אחד היום |
| 2.6 | **parse-expense: קיצוץ PDF בשרת לעמוד 1** — היום "רק בהנחיה למודל", קובץ 100 עמודים מחויב במלואו (`route.ts:17-19`) | דליפת עלות אמיתית |
| 2.7 | קשיחויות קטנות: rate-limit ל-`/s/[id]` (service-role בלי שום הגנה) + השבתת `/api/billing/webhook` בפועל כל עוד BILLING כבוי (היום endpoint כתיבה לא-מאומת חי) | סגירת שוליים |
| 2.8 | **כיבוי כלי-הידע עד שהמיגרציה חלה**: כל פנייה ל-search_knowledge שורפת סבב Sonnet מלא כדי לקבל "לא זמין" (`tools.ts:202-231`) — feature-flag או בדיקת-קיום ב-cold start | מכפיל עלות שקט חי היום |

## שלב 3 — התרעות ונראות (ההחלטה: מייל יומי + סף מיידי)

| # | משימה | הערות |
|---|---|---|
| 3.1 | **תלות חדשה: Resend** לשליחת מייל מהאפליקציה (free tier 100/יום; RESEND_API_KEY ב-Vercel; תיעוד ב-CLAUDE.md לפי הנוהל) | הכי פשוט; חלופה: SMTP של ג'ימייל |
| 3.2 | **דייג'סט יומי** — Vercel Cron (Hobby: פעם ביום) → route שמסכם מ-events+ai_usage: נרשמים חדשים, מסתכמים פעילים, קריאות AI + עלות משוערת, נפח DB, מרחק מ-100 המשתמשים של Testing, וסטטוס בריאות (כולל "האם ה-rate-limit העמיד באמת פעיל") → מייל אליך | הלב של "תתריע לי על כמויות" |
| 3.3 | **התרעות סף מיידיות inline** — אחרי כל קריאת AI, בדיקת ספים זולה (X קריאות/שעה גלובלית, Y נרשמים/יום, תקרת ₪) עם דה-דופ יומי → מייל מיידי. לא תלוי ב-cron | Hobby לא נותן cron שעתי — לכן inline |
| 3.4 | **עמוד `/admin` מינימלי** (מוגן — רק המייל שלך): המספרים של הדייג'סט בזמן-אמת + מתגי-החירום של 2.3 להפעלה ידנית | משלים, לא תחליף למייל |
| 3.5 | ריטנשן: purge ל-events (>90 יום) ולקבלות מעבר לחלון (היום שום דבר לא נמחק לעולם — `receipts_storage.sql:9-11`); ה-cron היומי של 3.2 מריץ גם את זה | Free tier בלי גיבויים — הטבלאות רק גדלות |

## שלב 4 — תשתית DB מוכנה לסקייל

| # | משימה | קרקוע |
|---|---|---|
| 4.1 | **לרענן את `docs/launch/hbsgz-pending.sql`** — חסרות בו 7+ מיגרציות שקיימות בריפו (כולל durable_rate_limit!); לייצר קובץ-הדבקה אחד מלא ומסודר + סדר החלה | הקובץ היום מסוכן-בחסרונו |
| 4.2 | יוני מדביק הכול ב-SQL editor (או — אחרי 0.2 — אני מחיל דרך MCP) ומאשר; כולל chat_threads/messages ו-knowledge_chunks | פותח גם את היסטוריית הצ'אט וגם את כלי-הידע |
| 4.3 | **תוכנית פיצול ה-persona blob**: כל ההוצאות/חשבוניות/תזרים חיים ב-jsonb יחיד שנכתב כולו מחדש בכל עריכה מ-8+ מקומות (`persona-repository.ts:54-74`) — בלי פג'ינציה ובלי אינדוקס. תכנון מעבר לטבלאות expenses/invoices אמיתיות, מיושם אחרי הבטא או כשמשתמש ראשון חוצה ~מאות רשומות | החוב הארכיטקטוני המרכזי לסקייל אמיתי |
| 4.4 | פג'ינציה/limit ל-chat history (loadMessages/listThreads בלי גבול היום) + הזזת הכתיבות מהדפדפן ל-API route כדי שיעברו rate-limit | `history.ts:74-124` |
| 4.5 | מעבר Supabase לאירופה — נשאר "אחרי הבטא" (החלטה קיימת #26); כל בקשת AI משלמת היום 2 round-trips פרנקפורט↔סינגפור לפני שקלוד בכלל נקרא — עוד סיבה לא לדחות לנצח | `memory/decisions.md:34` |

## שלב 5 — אוטומציה מתקדמת (הכרעת יוני: כל השלושה)

| # | משימה | הערות |
|---|---|---|
| 5.1 | **מטריצת פעולות-הגנה** (מסמך + מימוש 2.2-2.3): איזה סף ⇒ איזו פעולה ⇒ איזו הודעה אליך. כל פעולה אוטומטית תמיד מדווחת במייל | "המערכת מגיבה לבד ומדווחת" |
| 5.2 | **בדיקות עומס**: סקריפט k6 בריפו (תרחישים: 50 משתמשים בו-זמנית באשף, 20 שיחות צ'אט מקבילות, התקפת-עלות מדומה על /api/chat) + יעדים מוגדרים; ריצה נגד Preview deployment, לא פרודקשן | מאמת את שלב 2 לפני שמשתמשים אמיתיים נתקלים |
| 5.3 | **Canary בקוד**: feature-flag הדרגתי לפי hash של user_id (למשל `NEXT_PUBLIC_ROLLOUT_PCT`) לעטיפת פיצ'רים מסוכנים; ל-Vercel Hobby אין canary מובנה — זה התחליף הריאלי | רלוונטי מ~מאות פעילים; לבנות את התשתית עכשיו כשהיא זולה |

## נגררים מרשימת v1 (סבב-רוי) — סטטוס

✅ **בוצע ונדחף** (קומיטים f317a8b, 16f106e, 5f99eac): מסלול-שמירה יחיד + חוסן OAuth, תיקון הבהוב-דשבורד, התנתקות בכל עמוד, מובייל (טופס+צ'אט+כותרת), מעברי-מסך, פסיקים, קישור לפירוט, היסטוריית-צ'אט (קוד; ממתין ל-SQL), טסטים 030/137, ולידציית aliyahDate, ביקורת Capacitor, 24 e2e ירוקות כולל 5 מסעות-מובייל.

⏳ **נשאר פתוח, בעדיפות אחרי שלבים 0-3:**
| מקור | משימה |
|---|---|
| v1 #14 | הרחבת מצלמה לסלוטים של /setup (דורש נתיב-image ב-backend) |
| v1 #15 | סקאפולד Capacitor (ios/android) — סשן ייעודי; הצ'קליסט ב-`docs/mobile/` |
| v1 #18-20 | שאלות-מס לרוי: לוח Tikun 262 לשדה 044 · קרן השתלמות תחת עוסק זעיר · isReturningResident/isEilatResident |
| ותיק | CSP enforce (היום report-only) · סקירה משפטית חיצונית · IS 5568 |

## מי עושה מה — סיכום

**רק יוני (קונסולות/כסף):** 0.1-0.4, 1.1-1.2, 4.2 (הדבקה), מעקב מיילים.
**AI (סשנים הבאים, לפי הסדר):** 2.1→2.4→2.2→2.3→3.1-3.3→2.5-2.8→4.1→3.4-3.5→5.2→5.3→1.3.
**עיקרון מנחה:** שום פיצ'ר חדש לא נכנס לפני ששלבים 0+2+3 סגורים — קודם רשת-ביטחון, אחר-כך צמיחה.
