# תוכנית ספרינט בטא — א׳ 19/07 → ה׳ 23/07/2026

> **מסמך-האם:** התוכנית האסטרטגית של תומי (יולי 2026) — "מהמוצר הרזה לפלטפורמה מלאה".
> **העיקרון המנחה:** הדגש הוא לא על הדו"ח — על **בהירות וחיי היומיום** (הנחיית יוני 19/07).
> **מפרטי היישום:** `docs/specs/beta/` — onboarding · dashboard · documents-receivables · eitan · collab-and-qa.
> כל מזהי המשימות (ONB/DSH/DOC/REC/EIT/COL/QA/MET) מוגדרים שם עם DoD מלא.

## 1. מה הבטא (תכולה נעולה לספרינט)

חמשת רכיבי הליבה של סעיף 3 בתוכנית האסטרטגית + תיקוני החובה מהסריקה (19/07):

| # | רכיב (סעיף במסמך תומי) | מצב היום | ספק |
|---|---|---|---|
| 1 | הרשמה קלילה ≤3 דק׳ + גיימיפיקציה (3.1) | אשף 7 שלבים כבד-דוח — מוחלף ב-`/onboarding` חדש | onboarding.md |
| 2 | דשבורד קליל: 3 מספרים + 4 כפתורים (3.2, 5) | דשבורד עשיר-1301 — עובר ל-`/dashboard/pro`; `/dashboard` נבנה מחדש | dashboard.md |
| 3 | הפקת מסמכים: חשבון עסקה / קבלה / הצעת מחיר (3.3) | קיימים רק חשבונית מס-קבלה + קבלה; שני סוגים חדשים + מטריצת עוסק-מסמך | documents-receivables.md |
| 4 | איתן — מסביר בגובה העיניים (3.4) | צ׳אט חי; נוספים: בסיס-ידע, אסקלציה, FAB, תיקון "LLM לא מחשב", ארכיטקטורת עלויות | eitan.md |
| 5 | מי לא שילם לי (3.6) | לא קיים — מודל סטטוסים + מסך + תזכורות מדורגות | documents-receivables.md |
| — | המלצות הוצאות לפי מקצוע (3.5) | קיים `/business-expenses` (4 פרופילים) — **נשאר כמות-שהוא בבטא**; הרחבה ל-10–12 מקצועות = מיד אחרי חמישי, תלוית-תוכן מתומי/רועי | — |

**באגים/אבטחה שנסגרים בלי קשר לפיצ׳רים (יום 1):** פרסור SSE שבור בתובנות-הדשבורד (המקור לטקסט המשובש של איתן) · השלמת `PROTECTED_PREFIXES` (חסרים `/invoices`, `/coach`, `/deadlines`, `/alerts`, `/file`) · כפתור התנתקות + כותרות מודעות-סשן + ניקוי localStorage בהתנתקות · `?next=` אחרי login · עמודי `/privacy` + `/terms` (DRAFT) + תיקון הצהרת-הלוגין הכוזבת · 404 עברי · favicon/OG/manifest · לוג usage על כל קריאת Anthropic · הסרת מספר-ההקצאה המפוברק ממסמכים · חסימת חשבונית-מס לעוסק פטור.

## 2א. הכרעות שנחתמו (יוני, 19/07 ערב)

1. **קישור ציבורי חתום למסמכים — כן**, עם תפוגה (90 יום). יוני מוסיף `DOC_LINK_SECRET` + `NEXT_PUBLIC_APP_URL` ל-Vercel.
2. **אסקלציה של איתן:** שאלות support/מוצר → מייל `countme5555@gmail.com`. שאלות ידע-מס שאיתן לא בטוח בהן → **אין הפניה לאדם**; ברירת-מחדל צנועה: איתן מסביר שייתכן שהמידע שלו חסר כי המערכת חדשה. (מעדכן את EIT-10/EIT-11 — אין צורך בערוץ וואטסאפ.)
3. **dueDate — בלי ברירת מחדל.** המשתמש בוחר תאריך-יעד ידנית פר-מסמך; "באיחור" נגזר רק כשנקבע תאריך.
4. **רמת pre (אין תיק עוסק): הצעות מחיר + מדריכים בלבד.** מסך "פתח תיק — ואז הכל נפתח".

## 2ב. הנחות עבודה (ברירות מחדל שנבחרו; יוני יכול להפוך כל אחת)

1. **אבולוציה, לא בנייה מחדש** — משתמשים בתשתית הקיימת (auth, Supabase, מנוע מסמכים, brand, מחשבונים כשכבה שקטה).
2. **"חמישי הבא" = 23/07.** יעד: מוכנות רביעי בערב. אם הכוונה ל-30/07 — שבוע הרזרבה נבלע ב"מיד אחרי" (סעיף 7).
3. משטחי ה-1301 (`/demo`, `/file/*`, `/dashboard/pro`) **נשארים בקוד אך יוצאים מאור הזרקורים** — לא בניווט הראשי של הבטא.
4. מדריכי פתיחת-תיק מלאים = שלב 2 (כמו במסמך תומי). בבטא: שאלת-סינון בשאלון + עמוד הכוונה עובדתי (DRAFT) עם קישורים רשמיים.
5. חנויות: חמישי = PWA מותקן + מסלול Google Play פנימי (TWA) מיד אחרי; iOS = פרויקט המשך (חוסמי אפל: 4.2, Sign-in-with-Apple, OAuth-בתוך-webview).
6. איתן נשאר Sonnet 4.6; תובנות-דשבורד הופכות לתבנית דטרמיניסטית (אפס LLM); המסווג הרגולטורי → Haiku+Batch (היגיינה, מותר להחליק).
7. ~~שיתוף מסמכים: בכפוף לאישור יוני~~ → **אושר** (סעיף 2א-1).
8. `/coach` נסגר מאחורי login כשהגייטינג עולה (לא טיזר פתוח).

## 3. תוכנית יום-יום (עבודת AI; ראה סעיף 4 לצוות)

### יום א׳ 19/07 (היום, המשך) — יסודות ואבטחה
תיקון SSE ‏(EIT-1) · מזהי מודל מאוחדים + לוג usage ‏(EIT-2, EIT-3) · השלמת PROTECTED_PREFIXES + התנתקות + כותרות-סשן + ‎?next=‎ · ‏/privacy + /terms + פוטר ‏(QA-2) · manifest + 404 ‏(QA-3) · תיקוני /setup ‏(ONB-10) · הסרת הקצאה מפוברקת + חסימת פטור ממסמכי מס (החלק הקריטי מ-DOC-4/DOC-3) · CONTRIBUTING-HE.md ‏(COL-1, COL-2).
**DoD יומי:** build+test ירוקים; סריקת אינקוגניטו של 13 קישורי הפוטר לא מציגה דאטה כשהדגל דלוק ב-preview.

### יום ב׳ 20/07 — שאלון + דשבורד (שני מסלולים מקבילים)
מסלול א: ONB-1 → ONB-2 → ONB-3 → ONB-4..9 (שאלון מלא + חגיגה + אנליטיקס).
מסלול ב: DSH-1 (ספריית summary) → DSH-2 (העברה ל-pro) → DSH-3..6 (מסך חדש + מצב ריק + איתן דטרמיניסטי + פס סדר).
**DoD יומי:** משתמש חדש עובר שאלון בפחות מ-3 דק׳ בסלולר ונוחת בדשבורד ריק תקין; משתמש ותיק לא נשבר (ONB-11).

### יום ג׳ 21/07 — מסמכים + מי לא שילם לי
DOC-1 → DOC-2 → DOC-3, DOC-4 (השלמה), DOC-6, DOC-7 (בכפוף לאישור יוני) · REC-1 → REC-2 → REC-3 → REC-4 · DSH-7 (צ׳יפ בדשבורד) · ANA-1 · ONB-12 + DSH-11 (e2e).
**ערב ג׳: self-test מייסדים** — יוני+תומי מריצים את הזרימה המלאה בטלפון.
**DoD יומי:** הפקה ושיתוף של כל 4 סוגי המסמכים; "מי לא שילם לי" מציג סכום-בחוץ נכון; אין כפל-הכנסה בהמרות.

### יום ד׳ 22/07 — איתן, מדידה, ליטוש, QA
EIT-4 (ניקוי חישובים מהפרומפט) → EIT-5 (12 שאלות ידע ראשונות) → EIT-7, EIT-8 (cache) → EIT-9 (תובנות דטרמיניסטיות) → EIT-10 (אסקלציה) → EIT-12 (FAB) · MET-2, MET-3 (מדדי 3.7) · EIT-13 (אימות עלויות לפני/אחרי) · QA-1 (self-check.md) · ליטוש עיצוב: אנימציות דף-הבית, קונטרסט-נגישות, אייקוני PWA · **QA-4: ריצת self-check מלאה של תומי על טלפון אמיתי, על הדומיין הקנוני.**
**DoD יומי:** הצ׳קליסט המלא עובר; מדדי ההפעלה נקראים ב-SQL; הגייטינג דלוק ומאומת באינקוגניטו.

### יום ה׳ 23/07 — בוקר הבטא
ריצה מהירה של self-check → תיקוני-בזק בלבד → תומי מתחילה עם המשתמשים. אני במצב תגובה לתקלות.

### סדר ויתור (אם נגמר הזמן — חותכים בסדר הזה, לעולם לא את מצב-הריק ואת האבטחה)
DOC-5 (זיהוי-קולי ל-4 סוגים) → DOC-8 (פילטרים ברשימה) → DOC-7 מתקפל ל-wa.me טקסט-בלבד → EIT-14 (Batch) נדחה → DSH-6 (פס הסדר) נדחה → EIT-15 (השלמה ל-60–80 שאלות) ממשיך תוך כדי בטא.

## 4. מה נדרש מהצוות (עם דדליינים)

### יוני (ידני — רק אתה יכול)
| מתי | משימה |
|---|---|
| **היום/מחר** | הדבקת `docs/launch/hbsgz-pending.sql` (קובץ אחד, אידמפוטנטי) ב-SQL Editor של hbsgz — **חוסם את כל מדידת 3.7** (MET-1). ה-MCP עדיין רואה חשבון לא-נכון, לכן ידני |
| ~~עד ב׳ בערב~~ | ✔ **נחתם 19/07** — ראה סעיף 2א (קישור חתום, אסקלציה, dueDate, רמת pre) |
| **עד ג׳** | הוספת `DOC_LINK_SECRET` (מחרוזת אקראית ארוכה) + `NEXT_PUBLIC_APP_URL` ל-Vercel + ל-`.env.template` |
| **עד ג׳** | ניתוק פרויקט ה-Vercel הכפול (`countmes-projects`) + לוודא שאין שם מפתחות · הכרעת דומיין קנוני |
| **עד ד׳ בצהריים** | ‏`AUTH_GATING_ENABLED=true` ‏(Production+Preview) + ‏redeploy + אימות אינקוגניטו — **חוסם בטא** · תקרת תקציב + התראה ב-Anthropic Console · הוספת `DOC_LINK_SECRET`/`NEXT_PUBLIC_APP_URL` ל-Vercel |

### תומי
| מתי | משימה |
|---|---|
| עד ג׳ | מסמך טון ואישיות לאיתן (שלד ממני) + נוסח disclaimer ‏(EIT-11) — חוסם אסקלציה |
| עד ג׳ | קופי 8 מסכי השאלון + מיקרו-קופי שגיאות ‏(ONB-13) |
| עד ד׳ צהריים | 3 נוסחי תזכורת + קופי מצבי-ריק ‏(TOMI-1) · dry-run של מדריך התרומה ‏(COL-3) |
| ד׳ בערב | ריצת self-check מלאה בטלפון אמיתי ‏(QA-4) + רשימת משתמשי חמישי |

### רועי (אימות רגולטורי — כל מה שלא מאומת עד המועד עולה כ-DRAFT או מוסתר)
| מתי | משימה |
|---|---|
| **עד ג׳ בערב (חוסם)** | שדות-חובה ומספור לחשבון עסקה/הצעת מחיר · מטריצת עוסק-מסמך · סף מספרי הקצאה נכון ליולי 2026 · מה מותר לצטט מחוק מוסר תשלומים ‏(ROY-1) |
| עד ג׳ | אימות 12 תשובות-הידע הראשונות של איתן ‏(EIT-6) · הסברי סוגי עוסק + עובדות פתיחת-תיק ‏(ONB-15) · הכרעת פיצול ב"ל/מס-בריאות |

## 5. מדדי הצלחה (3.7) — איך נדע
`docs/qa/beta-metrics.sql`: הפעלה (% שמסיימים שאלון ומפיקים מסמך תוך 48ש׳, יעד >40%) · חזרתיות שבוע-2 (יעד >30%, מדיד רק מ-31/07 — לא להסיק 0% ביום השלישי) · שיחות איתן + % ללא-אסקלציה · דופק יומי. תלות קשיחה: MET-1 (המיגרציה) — בלעדיה `track()` נבלע בשקט ולא יהיו מספרים לראיונות העומק.

## 6. מה במפורש לא בבטא (לפי מפת הדרכים של תומי)
מנוע הוצאות אינטראקטיבי, התראות חכמות, מדריכי-רשויות מלאים, ביט/פייבוקס (שלב 2) · בנצ׳מארק, קישור-רשויות (שלב 3) · דוח שנתי מקצה-לקצה (שלב 4, קיים היום כ-`/file` ונשאר מחוץ לזרקור) · הקלטה קולית מלאה, ווידג׳טים, share-sheet (עיצוב שלב 2) · טיקטוק/רשימת-המתנה (שיווק — במקביל, לא בקוד הספרינט).

## 7. מיד אחרי חמישי (שבוע הבטא)
EIT-15 (השלמה ל-60–80 שאלות, ~15/שבוע באימות רועי) · הרחבת פרופילי הוצאות ל-10–12 מקצועות (3.5) · TWA ל-Google Play (מסלול פנימי) · EIT-14 ‏(Batch API) · צמצום PII ‏(WS7 — sanitizeForDb) + מחיקת-חשבון/ייצוא · ראיונות עומק של תומי.

## 8. פערים משפטיים/רגולטוריים פתוחים (מדווח בכל פלט)
1. אין סוקר משפטי מאז 02/07 — כל קופי משפטי (privacy/terms/disclaimer/תזכורות/הסברי-עוסק) עולה DRAFT.
2. צמצום PII ‏(WS7) טרם יושם — ת"ז/בנק/ת"ז-צדדים-שלישיים גלויים ב-DB; מתוזמן מיד אחרי חמישי, **לפני הרחבת משתמשים**.
3. אין מנגנון מחיקה/ייצוא (סע׳ 13–14) ואין נוהל-אירוע 72ש׳ — מתוזמן אחרי חמישי.
4. אין DPA מתועד מול Supabase/Vercel/Anthropic (בעלים: רועי, L6).
5. פורמטי המסמכים החדשים — DRAFT עד אימות רועי/רו"ח (ROY-1); מה שלא אושר עד רביעי מוסתר.
6. הקישור הציבורי החתום = ‏capability URL עם PII — דורש אישור מפורש של יוני.
