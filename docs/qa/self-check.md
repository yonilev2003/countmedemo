# צ'קליסט בדיקה עצמית — לפני הבטא ואחרי כל דיפלוי

> שתי רמות: **ריצה מלאה** (בוקר הבטא + אחרי שינוי גדול, ~20 דק', בטלפון אמיתי)
> ו**ריצה מהירה** (אחרי כל דיפלוי, ~5 דק'). מריצים על **הדומיין הקנוני** בלבד.
> כל כישלון = עוצרים, מתקנים, מריצים שוב. לא "נסתדר".

## ריצה מהירה (אחרי כל דיפלוי)

- [ ] דף הבית נטען, אין שגיאות בקונסול, הלוגו והפונט תקינים
- [ ] התחברות Google עובדת ומחזירה לעמוד שממנו הגעת
- [ ] הדשבורד נטען עם המספרים הנכונים (או מצב-ריק תקין)
- [ ] הפקת קבלה אחת עוברת מקצה לקצה
- [ ] הטקסט של איתן בדשבורד נקי (בלי `""` באמצע מילים, בלי `\n`)
- [ ] **שפיות מדדים:** ‏`select count(*) from events where created_at > now() - interval '1 hour'` ב-hbsgz מחזיר יותר מ-0 (אם 0 — המיגרציה לא הוחלה או שהאירועים נשברו)

## ריצה מלאה (בוקר הבטא / אחרי שינוי גדול)

### חשבון וסשן
- [ ] התחברות ראשונה בגלישה רגילה → נוחתים במקום הגיוני
- [ ] סגירת טאב → פתיחה מחדש → עדיין מחוברים
- [ ] לחיצה על הלוגו מכל עמוד → הכותרת מציגה "לאזור האישי" (לא "כניסה")
- [ ] התנתקות → הדאטה נעלם מהמסך; ‏DevTools → Application → localStorage ריק מ-`countme_persona`
- [ ] התחברות מחדש → הנתונים חוזרים מהשרת

### אבטחה (בגלישה בסתר, בלי להתחבר)
- [ ] כל 13 קישורי הפוטר: אף עמוד לא מציג נתונים אישיים; עמודים מוגנים מפנים ל-`/login`
- [ ] ‏`/api/chat` ישירות מחזיר 401 (כשהגייטינג דלוק)
- [ ] ‏`/terms` ו-`/privacy` נפתחים (הם ציבוריים בכוונה)

### הזרימה המרכזית (בטלפון אמיתי!)
- [ ] שאלון/הזנת נתונים מלאה — עם סטופר: מתחת ל-3 דקות
- [ ] דשבורד: שלושת המספרים + ארבעת הכפתורים על מסך אחד בלי גלילה (iPhone רגיל)
- [ ] מצב-ריק: משתמש בלי נתונים רואה מסך חם ומכוון, לא אפסים עצובים
- [ ] הפקת כל אחד מ-4 סוגי המסמכים; לעוסק פטור — אין אופציית חשבונית מס
- [ ] הצעת מחיר לא משנה את מספר ההכנסות בדשבורד; קבלה כן
- [ ] שיתוף מסמך בוואטסאפ: הקישור נפתח בדפדפן ללא login ומציג את המסמך
- [ ] "מי לא שילם לי": חשבון עסקה פתוח מופיע, תזכורת נפתחת עם נוסח תקין, "סמן כשולם" מעדכן את הסכום
- [ ] איתן: שאלה בסיסית ("מה זה מקדמות?") מקבלת תשובה ברורה בלי מספרים מומצאים
- [ ] איתן על שאלה אזוטרית: מודה שייתכן שחסר לו מידע (לא ממציא)

### מסביב
- [ ] כתובת שגויה (‏`/xyz`) → עמוד 404 עברי ממותג
- [ ] התקנת PWA מהדפדפן בטלפון → אייקון ¢ נכון, נפתח על `/home`
- [ ] תנאים/פרטיות נגישים מהפוטר ומעמוד הלוגין
- [ ] אין אימוג'י באף מסך (כלל מותג)

### אחרי הריצה
מדווחים בקבוצה: מה עבר, מה נפל (עם צילום מסך), על איזה מכשיר ודפדפן.
