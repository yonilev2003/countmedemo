# מה לעשות מחר ב-9:00

תוכנית עבודה ליום המחרת. כל סעיף מתחיל ב-**זמן משוער** ו-**אם זה תלוי בקלט מיוני**.

---

## פתיחת הסשן (10 דק')

1. **לעדכן את context-mode** (~5 דק') — `mksglu/context-mode` הוא MCP server שנדחה אתמול. דורש קונפיגורציה ידנית של hooks. נתקין יחד.
2. **לסקור את הריפו** (~5 דק') — הצצה בכל מה שנבנה אתמול בלילה: `personas/`, `src/lib/`, `src/components/`, `src/app/demo`. מריצים `npm run dev` ופותחים `localhost:3000/demo`.

## חיבורים — תלוי בקלט של יוני (15 דק')

3. **Anthropic API key** — יוני יוסיף כ-`ANTHROPIC_API_KEY` ב-Vercel environment variables (Settings → Environment Variables).
4. **חשבון Vercel** — יוני יחבר את הריפו `countmedemo` לפרויקט Vercel חדש (Import Git Repository → countmedemo). מייל המיזם, לא האישי.
5. **חשבון Supabase** — יוני ייצור פרויקט חדש (Free tier מספיק). שמירה של URL + anon key ב-`.env.local` ו-Vercel.

## פיתוח — תלוי בלוח הזמנים (5-7 שעות)

6. **wire Claude API** (~1.5 שעות) [תלוי ב-3]
   - `src/app/api/chat/route.ts` — להחליף את ה-501 ב-Anthropic SDK
   - להעביר את ה-`Persona` והתוצרים של ה-calculators כקונטקסט
   - להפעיל prompt caching על system prompt + persona block (חיסכון ~90% טוקנים בכל הודעה)
   - לעדכן `src/components/agent/chat-panel.tsx` ל-`fetch('/api/chat')` במקום mock
   - לבדוק עם Sonnet 4.6 כברירת מחדל

7. **שיפור התאמה ויזואלית לטופס האמיתי** (~2 שעות)
   - לקחת את התמונות מ-`demo-references/` (כשיוני יעלה דרך GitHub) ולהשוות מסך לצד מסך
   - לשפר צבעים, טיפוגרפיה, רווחים של `gov-form` styles ב-`globals.css`
   - **אופציה:** Playwright screenshot diff — להפיק screenshot של הדמו, להציג ליוני לצד התמונות, לאיים פיקסל בפיקסל

8. **בניית מסך הקלט (calculators)** (~2 שעות)
   - דף חדש ב-`src/app/setup/page.tsx`
   - טפסי קלט מודולריים: פרטים אישיים, פרטי עסק, טבלת הכנסות, טבלת הוצאות
   - אימות ת"ז דרך הסקיל `israeli-id-validator`
   - שמירה ל-`localStorage` בלעת עתה (Supabase מחר/בהמשך)
   - Wizard-style (מסך-אחר-מסך) או single-page? — להחליט ביחד

9. **Playwright golden-path test** (~45 דק')
   - Test יחיד שעוקב אחרי הפלואו: דף הבית → דמו → לחיצה על שדה 150 → אימות שהtooltip מציג את הנתונים הנכונים → שאלה לסוכן → אימות שמוחזרת תשובה
   - מטרה: שלא ייפול ביום ההצגה ב-EY

10. **דיפלוי ראשון ל-Vercel** (~30 דק') [תלוי ב-4]
    - `vercel --prod` או דיפלוי אוטומטי דרך push ל-`main`
    - לוודא שהכתובת זמינה
    - לבדוק שהדמו עובד בענן בדיוק כמו לוקאלית

## מטלות אופציונליות (אם יש זמן)

11. **Supabase Auth setup** — login/signup flow כך שמשתמש יכול לחזור (~1 שעה)
12. **שמירת persona ב-Supabase** במקום JSON סטטי (~1 שעה)
13. **fewer-permission-prompts** סקיל (~10 דק') — לרוץ פעם אחת ולקנפג allowlist שיחסוך לי הפרעות
14. **session-start-hook** סקיל — להגדיר hook שטוען לי את ההקשר אוטומטית בכל סשן חדש

---

## דברים שיוני יבקש לעשות *לפני* התחלת היום

- [ ] להעלות את 16 התמונות של הטופס ל-`demo-references/` בריפו (דרך GitHub web UI, drag & drop)
- [ ] להוסיף את `ANTHROPIC_API_KEY` ב-Vercel
- [ ] לחבר את הריפו ל-Vercel project
- [ ] ליצור פרויקט Supabase
- [ ] להחליט: האם הסשן של מחר על שני המסכים (יוני + claude) או יוני בולע את הפלט באופן a-sync?

## הסיפור שאנחנו מספרים ב-EY (להזכרה)

עצמאי בישראל מתחת לגיל 35:
- מפחד מרשויות המס
- משלם 1,200₪ לרואה חשבון לכל דו"ח
- מקבל שירות איטי ומלא בטעויות אנוש

countme פותר את שלושת הבעיות בבת אחת — AI שמכיר את העסק שלך, פתוח 24/7, חינמי, וטועה פחות מבן אדם. הדמו של היום הוא הפלואו הראשון: דו"ח שנתי 1301.

## רשימת השאלות הפתוחות (אם הזמן ידחק)

- האם הצ'אט בדמו צריך לדבר *לדנה* בגוף שני נקבה, או נייטרלי? (אישה ב-EY → נקבה צבועה יותר)
- האם להציג את ה-confidence ("ביטחון גבוה/בינוני/נמוך") ב-EY או רק high? (מציג בגרות, אבל גם פגיעות)
- חבילת ההסבר על "החיסכון לעומת רואה חשבון" — האם מספיק 1,200₪ או להוסיף "וגם חסכת 4 שעות של חיפוש"?
